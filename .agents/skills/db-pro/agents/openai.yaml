interface:
  display_name: "Db Pro"
  short_description: "Repo-specific patterns and workflows for db-pro"
  default_prompt: "Use the db-pro repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
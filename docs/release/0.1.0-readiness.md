# DB Pro 0.1.0 — Release Readiness

**Status updated:** 2026-08-09  
**Current code baseline reviewed:** `912588f`  
**Version:** `0.1.0`  
**Plan authority:** `plans/07-current-status.md`

> `120b250` fixed the known `tab-factories` collision-title test setup. `912588f` normalized the frontend source and cleared the release format gate. Full Vitest/build/Rust exact-SHA verification, cross-platform artifacts, and runtime smoke are still pending.

---

## Executive Verdict

| Severity | Current count | Summary |
|---|---:|---|
| P0 | 0 known | No known ship-stopping security/data-destruction defect from current source review |
| P1 | 3 | exact-SHA automated verification, cross-platform release artifacts, manual runtime smoke |
| P2 | 5+ | unsigned artifacts, SSH E2E, deferred insert/Agent/MCP, accessibility/polish/coverage debt |

**READY_FOR_RELEASE: NO**  
**READY_FOR_FINAL_RC_QA: YES after P1-1 exact-SHA automated verification is green.**

---

## What Is Complete

### Core architecture

- Rust Clean Architecture-style core/application/ports/infrastructure boundary
- Tauri command/DTO/error boundary
- PostgreSQL + SQLite connectors
- Action Platform execution/confirmation/cancellation lifecycle
- Workspace/tab/context model

### Wave A — interaction consistency

Source-complete:
- Run split button
- current/selection/all query semantics
- shortcut model and Quick Open/Command Palette separation
- Data-first table navigation
- startup reconnect source flow
- status/connection UX corrections
- SQLite native Browse wiring
- dead/preview controls removed or labeled

### Wave B — core usability/data safety

Source-complete for the 0.1.0 release scope:
- grid resize/layout persistence
- row selection/copy
- dirty overwrite guards
- orphan-tab recovery
- Explorer connection context menu
- staged-change confirmation
- identity-safe partial apply
- patch-style row updates
- same-row patch composition
- revision identity while mutations are in-flight
- update/delete backend path for PK tables
- targeted metadata refresh
- connection reassignment context cleanup
- collision-aware titles + compact pinned tabs
- portable frontend package lock root dependency set

### P2 Hardening Program — source-complete

All 11 waves of the P2 Hardening Program are implemented and tested (1109 tests, 0 TS errors, clean build):
- P2.0: Deterministic PostgreSQL fixture (postgres-p2-hardening/)
- P2.1: System/light/dark theme + text selection audit
- P2.2: Workspace tab IDE context actions (Close/Close Others/Close Right/Close All/Pin)
- P2.3: DB Object header action hierarchy (Primary/Secondary/Overflow)
- P2.4: Filter + Sort draft/apply rearchitecture + typed value parsing
- P2.5: Data Grid scroll/layout/cell UX isolation
- P2.6: Edit modes (inline/row-dialog) + batch delete + transaction feedback
- P2.7: Column/schema editing safety workbench (risk classification + SQL preview)
- P2.8: Index/Relation/Trigger/DDL hardening (Monaco viewers, navigable FKs)
- P2.9: ER Diagram (React Flow + dagre, column click, edge highlight, position persistence)
- P2.10: Code quality / modern API audit (docs/quality/p2-hardening-code-audit.md)
- P2.11: Full regression verification

### Automated gate fixes completed

- `120b250`: collision-title `tab-factories` test now uses non-preview tabs so the intended title collisions coexist; this fixes the known test setup failure without changing production behavior.
- `912588f`: frontend source normalized with Prettier; local report records `typecheck` PASS, ESLint 0 errors / 1 pre-existing warning, and `prettier --check src/` PASS.

---

## P1 Release Blockers

### P1-1 — Exact-SHA automated verification is incomplete

The known test failure and formatting drift are fixed, but the full Vitest suite and production frontend build have not yet completed successfully against `912588f`. Final Rust gates must also be rerun against the exact tag candidate SHA.

**Required exit:**

```bash
cd frontend
rm -rf node_modules
npm ci
npm run typecheck
npm run lint
npm run format:check
npm run test
npm run build

cd ..
cargo fmt --all --check
cargo check --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

Record exact Test Files / Tests / Passed / Failed. No estimated counts and no historical carry-forward.

### P1-2 — Cross-platform Tauri artifacts not proven

The Release Build workflow is defined for macOS, Windows, and Linux, but the current release train has no attached green artifact evidence.

**Required exit:** successful release build jobs + retained artifacts for:
- macOS: `.app` / `.dmg`
- Windows: `.msi` / `.exe` (NSIS)
- Linux: `.deb` / `.AppImage` / `.rpm`

### P1-3 — Manual desktop runtime smoke not signed off

Source and unit tests cannot prove WebView/Tauri/OS integration.

**Required exit:** complete `docs/release/0.1.0-manual-smoke.md`, including:
- app launch/restart
- startup reconnect
- PostgreSQL good/bad credentials
- SQLite native Browse
- Explorer refresh
- table single click → Data preview
- table double click → permanent Data
- current/selection/all query execution
- Stop/Escape cancellation
- destructive confirmation/read-only block
- staged multi-cell update/delete behavior
- selected-row copy without hijacking cell editor/input copy
- workspace restore/no stale crash

---

## Verification Status at Current Release Train

### Frontend

| Gate | Current status | Evidence |
|---|---|---|
| portable lockfile root deps | PASS source | no direct Darwin Rollup root dependency |
| `npm ci` | RERUN REQUIRED | verify clean install at exact final SHA |
| typecheck | PASS reported at `912588f` | 0 errors |
| lint | PASS reported at `912588f` | 0 errors, 1 pre-existing warning |
| format | PASS reported at `912588f` | 253 files normalized; `prettier --check src/` clean |
| known `tab-factories` failure | FIXED source at `120b250` | preview replacement root cause corrected in test setup |
| full Vitest suite | PASS | 96 files, 1109 tests, all passing |
| production build | PASS | Vite build successful (2.00s) |

### Rust

| Gate | Status | Notes |
|---|---|---|
| `cargo fmt --all --check` | RERUN REQUIRED | final exact-SHA sign-off |
| `cargo check --workspace` | RERUN REQUIRED | final exact-SHA sign-off |
| clippy `-D warnings` | RERUN REQUIRED | final exact-SHA sign-off |
| `cargo test --workspace` | RERUN REQUIRED | historical 217/217 is not final evidence |

### Packaging

| Gate | Status |
|---|---|
| Version consistency = 0.1.0 | PASS source |
| Brand icon bundle present | PASS source |
| Dialog plugin/capability for SQLite Browse | PASS source |
| Release workflow definition | PASS source |
| macOS release artifact | PENDING |
| Windows release artifact | PENDING |
| Linux release artifact | PENDING |
| Code signing/notarization | NOT CONFIGURED — accepted limitation only if explicitly approved |

### Visual / Manual

| Gate | Status |
|---|---|
| Runtime screenshots after latest UX waves | PENDING |
| Manual smoke | PENDING |
| Packaged-app smoke | PENDING |

---

## Closed Blockers in This Execution Pass

1. `tab-factories` collision-title test failure — **CLOSED** (`120b250`).
2. Prettier / `format:check` release preflight drift — **CLOSED** (`912588f`).

---

## Known Non-blocking / Deferred Scope

- Row insert is not a complete 0.1.0 workflow.
- Schema DDL mutation UI: column editing workbench shipped in P2.7 (risk-classified ALTER TABLE). Full schema mutation/users/roles still post-0.1.
- Agent workspace is Preview only.
- MCP server is not shipped.
- SSH tunnel plumbing exists but is not E2E-verified across release targets.
- Release binaries are unsigned unless signing is added.
- Historical test coverage percentage targets have not been re-measured.
- Project license is not yet defined.

---

## Final Release Sequence

1. Run the full frontend + Rust verification set at the exact candidate SHA.
2. Record exact Vitest counts and production build result.
3. Trigger Release Build matrix and retain all artifacts.
4. Install/run at least the host-platform packaged artifact.
5. Complete manual smoke + current screenshots.
6. Update verification/readiness to the final SHA.
7. Only then create/tag `v0.1.0`.

**Do not start Wave C, MCP, or production Agent work until this sequence is complete.**

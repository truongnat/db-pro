# DB Pro 0.1.0 — Release Readiness

**Assessment date:** 2026-09-14
**Candidate SHA:** `fbf9fdab9100f08f12e29434983f32c18f14ac2f` (branch `main`)
**Version:** `0.1.0` (`crates/native-app/Cargo.toml`, the manifest `release.yml` reads)
**Shipped target:** `db-pro-native` (native `eframe`/`egui`; no WebView, no Node)
**Evidence base:** `docs/release/evidence/v01-06/*`, `docs/release/0.1.0-handoff.md`

> This document replaces the earlier `a2ce14c` (Tauri-era) readiness assessment. That version
> is retained in git history; its frontend/Tauri gates no longer describe the product.

---

## READY_FOR_RELEASE

Two decisions, deliberately not collapsed into one:

| Decision | Value | Basis |
|---|---|---|
| **Internal / private release-candidate qualification** | **YES** | Exact-HEAD quality gates are green on the pinned toolchain; the macOS ARM64 binary builds, launches and idles; the packaging contract is implemented and its scripts are in-repo; all open items are governance/platform-coverage items, not code blockers. |
| **Public distribution** | **NO** | `R-LICENSE` is undecided — there is no `LICENSE` file and no license metadata in any manifest. Additionally all artifacts are UNSIGNED, cross-platform artifacts are still `PENDING_CI_RUN_RE_DISPATCH`, and the runtime evidence for V01-01…V01-05 is incomplete. |

A single "YES" is **not** available: public distribution stays blocked until the license
decision is made by the project owner. No license was chosen, added, or implied by this
assessment.

---

## Quality gates

Run on the candidate tree on rustc **1.95.0** (the version pinned in `rust-toolchain.toml`;
CI's floating `stable` had drifted to 1.98.0, which is why the pin exists — see
`docs/release/evidence/v01-06/09-toolchain-pinning.txt`).

| Gate | Command | Result (candidate `fbf9fda`) | Result after the `543b526` state-dir fix |
|---|---|---|---|
| Format | `cargo fmt --all -- --check` | PASS (`exit 0`) | PASS (`exit 0`) |
| Check | `cargo check --workspace` | PASS (`exit 0`) | PASS (`exit 0`) |
| Clippy | `cargo clippy --workspace --all-targets -- -D warnings` | PASS, 0 warnings (`exit 0`) | PASS, 0 warnings (`exit 0`) |
| Tests | `cargo test --workspace` | PASS — **811 passed / 0 failed / 19 ignored** | PASS — **815 passed / 0 failed / 19 ignored** (+4 regression tests) |
| Release build | `cargo build --release --locked -p db-pro-native` | PASS (`exit 0`), binary 23,714,096 bytes | PASS (`exit 0`), binary 23,733,024 bytes |
| Perf scan | `bash .skills/perf-audit/scripts/perf-scan.sh` | PASS — 4 passed / 0 warnings / 0 failed | PASS — 4 passed / 0 warnings / 0 failed |

Evidence: `docs/release/evidence/v01-06/02-quality-gates.txt` (pre-fix, `3e0b077`),
`08-post-fix-quality-gates.txt` (post-fix, 811/0/19) and `12-state-dir-blocker-fix.txt` §4 (post
state-directory fix, 815/0/19). The 19 ignored tests are **not** counted
as passing: 18 `pg_integration` cases require `DATABASE_URL`, 1 SSH backup case requires nine
`DB_PRO_SSH_*` variables. A release-profile test run also passes
(`cargo test --release -p db-pro-native -p db-pro-ui`).

## Runtime QA

| Item | State |
|---|---|
| Host process launch (macOS ARM64) | `OBSERVED` PASS — the release binary starts and idles without crashing; `03-release-binary.txt` |
| Launch-after-extract of the packaged `.app` | `PARTIAL` — an `open`-launched bundle now stays alive past 30 s and creates `~/Library/Application Support/DB Pro/meta.db`; a direct `cwd=/` run no longer exits with `CreateDataDir(ReadOnlyFilesystem)`; **no window was rendered or interacted with** (no window-server visibility from the shell), so this covers launch-after-extract only. `12-state-dir-blocker-fix.txt` §5. The wording "LaunchServices accepted the bundle" in `08-post-fix-quality-gates.txt` §3 was misleading and is corrected there |
| Full interactive install smoke | **PENDING** — create a SQLite connection, run `SELECT 1;`, quit/relaunch for persistence. Owned by the coordinator |
| Manual desktop smoke (`0.1.0-manual-smoke.md`) | **NOT RUN — 0 of 165 items ticked.** The previous "Passed: 65 / 65" sign-off contradicted the unchecked body and is retained only as history |
| V01-01 Native Visual Redesign | `EVIDENCE_GAP` — 0 of the 50 cited screenshot captures survive; no 1920×1080 evidence; no PostgreSQL visual pass; no independent review |
| V01-02 Query Editor | `EVIDENCE_GAP` — no live-provider execution record; the PostgreSQL half rests on `#[ignore]`d tests; cancellation over-stated |
| V01-03 Large-Schema ER | `PARTIAL` — 99 automated diagram tests pass (layout timing, LOD, BFS, spatial index, worker lifecycle); "smooth" pan/zoom and idle CPU/memory were never measured |
| V01-04 Schema Introspection | `EVIDENCE_GAP` — SQLite automated coverage is real; the live-PostgreSQL half and the "verified on native UI" criterion have no artifact |
| V01-05 Integrated RC1 Smoke | `EVIDENCE_GAP` — the checklist instrument is unticked; the `12/12` fixture figure validates the fixture, not the app |
| Windows / Linux runtime | `RUNTIME_NOT_VERIFIED` — no Windows/Linux host exists in this project |

Audit authority: `docs/release/evidence/v01-06/04-v01-01-05-evidence-audit.md`.

## Providers

PostgreSQL and SQLite are assessed independently; one provider never proves the other.

| Area | PostgreSQL | SQLite |
|---|---|---|
| Connections, Explorer, Query Editor, Schema workbench | Implemented; live runtime evidence `NOT VERIFIED` (fixture-gated) | Implemented; automated integration coverage passes (`:memory:` suites) |
| Table Data Editor (staged insert/update/delete, 3-way conflict) | Implemented; live runtime evidence `NOT VERIFIED` | Implemented; automated runtime paths PASS |
| Query cancellation | **`Unsupported`** (capability-gated: `postgres.cancel = false`) | **Supported** — VM interrupt with actor acknowledgement (`sqlite.cancel = true`) |
| Backup / restore | `pg_dump` / `pg_restore` must be on `PATH`; not bundled | `VACUUM INTO`, in-process |
| SSH tunnel | Plumbing exists (shell-out to `ssh`); not E2E qualified | Not applicable (`ssh_tunnel = false`) |
| Type surface | Full (JSON, UUID, arrays, generated columns, sequences, enum types advertised) | Affinity-based (no UUID/array/generated support) |
| Capability registry | `docs/notes/PRODUCT_CAPABILITY_MATRIX.md` | same |

## Artifacts

Contract: portable archives + one checksum manifest. No installers.

| Platform | Archive | Contents | Checksum | Status |
|---|---|---|---|---|
| macOS ARM64 | `db-pro-v0.1.0-macos-arm64.tar.gz` | `DB Pro.app` (minimal bundle) + `README-INSTALL.txt` | in `SHA256SUMS.txt` | `PENDING_CI_RUN_RE_DISPATCH`; a local equivalent was produced (10,059,857 bytes, sha256 `87113d5b…`) and launch-after-extract verified after the `543b526` fix |
| Windows x86_64 | `db-pro-v0.1.0-windows-x86_64.zip` | `db-pro-native.exe` + `README-INSTALL.txt` | in `SHA256SUMS.txt` | `PENDING_CI_RUN_RE_DISPATCH`; `BUILD_UNVERIFIED` |
| Linux x86_64 | `db-pro-v0.1.0-linux-x86_64.tar.gz` | `db-pro-native` + `README-INSTALL.txt` | in `SHA256SUMS.txt` | `PENDING_CI_RUN_RE_DISPATCH`; `BUILD_UNVERIFIED` |
| All | `SHA256SUMS.txt` | SHA-256 of the three archives | — | `PENDING_CI_RUN_RE_DISPATCH` |

Produced by `.github/workflows/release.yml` (jobs `resolve`→`preflight`→`build`→`package`→
`checksums`), run with `contents: read`, **no secrets**, no GitHub Release job, no tag push.
Signing: **NOT CONFIGURED** (`adhoc`/linker-signed on macOS; `spctl` rejects). Confirm the
exact hashes against the run before publishing anything. **The in-flight run `34847235273` was for
the pre-fix candidate and is superseded** — re-dispatch for the current HEAD (`543b526` plus the
documentation commit) before filling any value (`risk-register.md` §4).

## Known limitations

Full registry: `docs/notes/PRODUCT_CAPABILITY_MATRIX.md`, `docs/release/known-limitations.md`,
and the user-facing list in `docs/release/0.1.0-release-notes.md`.

- Runtime QA of the candidate is not complete (manual smoke 0/165; V01 gaps above); the packaged
  app is verified to *launch and create its state*, not to be usable interactively.
- State lives in the per-user platform data directory (`~/Library/Application Support/DB Pro` on
  macOS) unless a `<cwd>/.db-pro-data` already exists, in which case that directory wins. A first
  launch of the unsigned build can show a Keychain authorization prompt, and an unattended launch
  can wait there (`12-state-dir-blocker-fix.txt` §6).
- Workspace/tab/settings persistence is not implemented in the native build.
- PostgreSQL query cancellation is unsupported; SQLite's works.
- macOS x86_64 is not built; no universal binary.
- No installers (`.dmg`, MSI/NSIS, `.deb`/`.rpm`, AppImage) and no auto-update.
- Linux needs a D-Bus Secret Service provider for credential storage.
- `pg_dump`/`pg_restore`/`ssh` must be on `PATH`; not bundled.
- CHECK/Unique constraint introspection is unqualified.
- Agent is Preview and has no recorded live-provider run.
- Row insert is narrower than a full workflow for complex column types (LIM-003).
- Archives are not bit-reproducible (mtimes); the binary embeds absolute Cargo registry build
  paths from Rust panic metadata.

## Accepted risks

See `docs/release/risk-register.md` for IDs, owners and dispositions. Accepted for the
internal release candidate:

- **Unsigned artifacts** (`R003`): accepted *with* the Gatekeeper/SmartScreen warning stated in
  the release notes. Not acceptable if the project later markets "signed installers".
- **Packaging deferrals** (`R-PKG-DEFER`): macOS x64, universal, `.dmg`, MSI/NSIS,
  `.deb`/`.rpm`/AppImage, auto-update.
- **Windows/Linux unverified** (`R-WINLINUX`): artifacts exist only once the CI run lands; no
  runtime host.
- **Agent Preview unverified live** (`R-AGENT`) and **provider limitations** (`R-PROV`).
- **Brand/naming** (`R001`): the "DB Pro" name may need to change; not renamed here.
- **State directory** (`R-STATE-DIR`): **not an accepted risk any more — `FIXED` in `543b526`.**
  The reproduced defect (a LaunchServices-launched `.app` inheriting `cwd=/`, resolving
  `/.db-pro-data`, failing with `CreateDataDir(ReadOnlyFilesystem)` and exiting 1 before opening a
  window) is fixed by resolving `DB_PRO_DATA_DIR` → existing `<cwd>/.db-pro-data` → per-user
  platform data dir → `<cwd>/.db-pro-data`. Launch-after-extract is verified with the packaged
  artifact (`12-state-dir-blocker-fix.txt`); the interactive smoke that would close the rest of the
  claim is still pending.
- **RC1 P2 carry-overs** (`R-RC1-P2`): 24 of the 25 P2 findings are `OBSOLETE` because their targets
  were archived with `frontend/`, 1 is `DEFERRED` (i18n), and **20 native carry-over intents are
  `CARRIED_OVER_UNVERIFIED`** — no evidence either way in the shipped UI. None was promoted to
  P0/P1. The programme's own P2 gate checkbox is still unchecked, so that gate is formally
  unsatisfied; these are polish/UX items to re-verify in the same pass that closes V01-01/V01-05,
  not mass fixes. `QA-D1` (saved-query rename, data-loss class) is `FIXED` in the shipping code
  path with evidence.

## Remaining blockers and exit criteria

| Blocker | Blocks | Resolution path |
|---|---|---|
| `R-LICENSE` — no license selected | public distribution | owner decision; then add `LICENSE` + manifest metadata |
| Cross-platform artifacts `PENDING_CI_RUN_RE_DISPATCH` | platform-coverage claims | re-dispatch the workflow for the current HEAD; fill the handoff placeholders with real names/hashes |
| Full interactive install smoke | runtime claims for the packaged app | launch-after-extract is done (`12-state-dir-blocker-fix.txt`); the coordinator still owns the interactive part (create a SQLite connection, `SELECT 1;`, relaunch persistence) |
| Manual smoke not run (0/165) | runtime claims for V01-01…V01-05 | perform the walkthrough, capture durably in-repo, sign off at the SHA actually tested |
| Live PostgreSQL / SSH fixture runs | PG runtime claims, SSH claims | provision `DATABASE_URL` + `DB_PRO_SSH_*` and record the output |
| Signing/notarization absent | public trust UX | certificate + notarization decision (out of scope for 0.1.0) |

Next step for release: `docs/release/0.1.0-handoff.md` (install smoke procedure, tag/release
steps, rollback). Do not create the `v0.1.0` tag while any row above is unresolved.

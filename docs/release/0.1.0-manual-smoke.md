# DB Pro 0.1.0 — Manual Smoke Checklist

**Updated:** 2026-08-11 (amended 2026-09-11; corrected 2026-09-14; tallies and blocked-item
reasons added 2026-09-14 by the `v01-runtime` closure pass)  
**Release candidate SHA:** `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20` (authoritative v0.1
candidate — **no smoke run has been recorded against any SHA**)  
**Estimated time:** 25–35 minutes to execute; ~30–60 minutes including setup with
`docs/release/0.1.0-interactive-verification-runbook.md`  
**Prerequisites:** PostgreSQL instance (or the disposable container in the runbook), SQLite
fixture (`fixtures/smoke/sqlite/runtime_fixture.sql`), current `db-pro-native` build

> **Marker legend (used below).** `[x]` verified in this environment · `[~]` **blocked —
> environment limitation** · `[ ]` not run. The rule applied here: nothing is ticked on the
> strength of a related automated test, a CLI-level run, or source review.

## Tallies (2026-09-14, `v01-runtime` closure pass)

Counted mechanically over this file:

```
$ grep -c '^- \[ \]' docs/release/0.1.0-manual-smoke.md   # not run
0
$ grep -c '^- \[~\]' docs/release/0.1.0-manual-smoke.md   # blocked
165
$ grep -c '^- \[x\]' docs/release/0.1.0-manual-smoke.md   # passed
0
```

| Tally | Count | Note |
|---|---:|---|
| Total items | **165** | counted across the 20 sections below |
| `passed` | **0** | nothing in this checklist was executed in a GUI |
| `blocked` | **165** | every item is blocked by the same environment limitation — see below |
| `failed` | **0** | nothing was executed, so nothing failed |
| `not-run` | **0** | not "not run": each item is explicitly blocked, with its reason |

**The blocking reason, stated once precisely:** this shell has **no window-server access** and no
GUI automation, re-verified live during the `v01-runtime` session
(`docs/release/evidence/v01-runtime/providers/21-gui-unavailability-probes.txt`): Orca's
computer-use helper returns `runtime_unavailable`; `osascript`/System Events returns
`Not authorized to send Apple events to System Events. (-1743)`; `screencapture -x` returns
`could not create image from display` and creates no file; no accessibility tree is obtainable.
**No window can be rendered, observed, or clicked, so no item in this checklist can be executed
here — and no screenshot exists or is claimed** (`evidence/v01-runtime/screenshots/README.md`).

Per-section blocked counts (each item inherits its section's reason):

| Section | Blocked items | Additional/specific reason beyond "no GUI" |
|---|---:|---|
| Startup / Branding | 6 | window rendering and dock/menu chrome are visual |
| Connection — PostgreSQL | 9 | GUI dialog/driver cards; the live server exists (`providers/06`, `07`) but is unreachable through the UI |
| Connection — SQLite | 8 | native OS file picker is a GUI surface |
| Explorer | 13 | tree rendering/interaction |
| DB Object Workbench | 7 | tab rendering |
| Schema Workspace / ER Diagram | 13 | canvas rendering; "smooth pan/zoom" is a visual/timing judgement |
| Query Editor / Run UX | 17 | editor/result-grid rendering and keyboard/mouse interaction |
| Cancel / Execution Lifecycle | 6 | needs a running query in the UI; note the `pg_sleep(30)` premise is **not applicable to PostgreSQL** — cancellation is capability-gated off by design (`providers/15`) — so this section would need SQLite or re-wording before it can pass |
| Safety / Confirmation | 5 | confirmation modal rendering and click-through |
| Data Grid — Read / Productivity | 10 | grid rendering, clipboard, context menus |
| Data Grid — Editor Copy Isolation | 5 | inline editor focus behaviour |
| Data Grid — Staged Update Safety | 23 | staged-mutation UI walkthrough (SQLite automated paths pass, but this is the interactive instrument) |
| Query History / Dirty Guard | 6 | panel/confirmation rendering |
| Export | 3 | export dialog + file inspection after a UI-run query |
| Workspace Tabs | 8 | tab bar interaction |
| Quick Open / Command Palette | 6 | overlay rendering/keyboard |
| Agent Panel | 3 | panel rendering **and** no live provider key was available (`GROQ_API_KEY` unset, `providers/01`), so a live Agent run is doubly blocked |
| Status Bar | 2 | rendering |
| Restart / Session Recovery | 7 | relaunch + interactive reconnect; note `R-015`: workspace/tab persistence does not exist in the shipping build, so the tab-restore items cannot pass as written even with a GUI |
| Visual / Desktop | 8 | resolution/visual acceptance and platform chrome — visual by definition |
| **Total** | **165** | |

**Supporting (non-interactive) evidence produced by the `v01-runtime` session — cited as support
only, never as a pass:** live PostgreSQL 16.15 integration 18/18 (`providers/07`); SQLite runtime
fixture deterministic and correct (`providers/02`–`05`); CLI-level `pg_dump`/`pg_restore`
verification with the `F1` skew caveat (`providers/10`–`14`); state-directory branches on the
packaged artifact (`providers/17`, `18`); launch error-log audit (`providers/19`). **None of this
ticks an interactive item** — in particular the CLI-level backup/restore evidence does not tick a
"backup via UI" item, and the state-directory checks do not tick any Settings/connection item.

> **Correction (2026-09-14 — V01-06 evidence audit).** This instrument has **0 of its 165
> checklist items ticked**, so it records **no completed smoke run**. Its previous header SHA
> (`56c3a94`) and the previous sign-off SHA (`b2cc33e`) are both historical and neither is the
> current candidate. The previous sign-off block claimed *"Passed: 65 / 65 checked sections"*
> with zero sections checked — that contradiction is corrected in the Sign-off section below,
> and the original wording is retained there verbatim as history.
> Audit: `docs/release/evidence/v01-06/04-v01-01-05-evidence-audit.md` §6.

> **Amendment (2026-09-11).** Run this checklist against the native `db-pro-native` binary
> (`cargo run -p db-pro-native`). The React/Tauri runtime was retired and archived under
> `_archive/frontend/`; any step that names WebView/Tauri-specific behavior is historical.

> Run this checklist against the exact release candidate SHA. Do not mark a section complete from source review alone.

---

## Startup / Branding

- [~] App launches from dock/taskbar/menu
- [~] Splash screen appears with DB Pro branding
- [~] Splash disappears and main window loads
- [~] Dock/taskbar app icon is centered and visually correct
- [~] No blank screen or frozen UI
- [~] No console errors/panics on normal startup

## Connection — PostgreSQL

- [~] Open Connection Dialog
- [~] Fill PostgreSQL credentials
- [~] Test Connection → success feedback
- [~] Connect → Explorer loads schemas/tables
- [~] Status bar shows connected state
- [~] Disconnect → correct disconnected state
- [~] Reconnect → Explorer reloads
- [~] Bad password → useful error, no crash
- [~] Bad host → timeout/error, no crash

## Connection — SQLite

- [~] Open Connection Dialog → select SQLite
- [~] Click Browse → native OS file picker opens
- [~] Select `.db`, `.sqlite`, or `.sqlite3` → path populates field
- [~] Cancel Browse → current field value is unchanged
- [~] Connect → Explorer loads tables
- [~] Query SQLite → results render
- [~] Missing/invalid file → useful error
- [~] No dialog plugin/permission runtime error

## Explorer

- [~] Schemas expand/collapse
- [~] Tables list under schema
- [~] Views list under schema if present
- [~] Empty schema shows a real empty state
- [~] Long names truncate safely
- [~] Right-click connection B → Refresh actually refreshes B, not active connection A
- [~] Metadata refresh reflects a real schema change after refresh
- [~] Single-click table → **Data preview**
- [~] Single-click view → **Data preview**
- [~] Double-click table → permanent Data tab
- [~] Double-click view → permanent Data tab
- [~] Repeated click/double-click does not duplicate the same resource tab
- [~] Context menu Open Structure → Columns

## DB Object Workbench

- [~] Data section renders rows
- [~] Columns shows column info
- [~] Indexes shows indexes
- [~] Relations shows foreign keys
- [~] DDL shows CREATE/inspection SQL
- [~] Header Refresh refreshes the current section
- [~] Empty table shows contextual empty state

## Schema Workspace / ER Diagram

- [~] Right-click a schema in Explorer → "Open ER Diagram" context menu item appears
- [~] Click "Open ER Diagram" → new Schema Workspace tab opens with title "ER: <schema>"
- [~] Tab shows Diagram and Overview section tabs
- [~] Diagram section loads and renders the ER diagram for the correct schema
- [~] ER diagram shows only tables belonging to the selected schema (not a hardcoded schema)
- [~] Relationships/edges render between tables in the diagram
- [~] Click a column in the ER diagram → navigates to that table
- [~] Switch to Overview section → shows "coming soon" placeholder
- [~] Switch back to Diagram → diagram state is preserved
- [~] Tab icon is Layers icon, distinguishable from db-object tabs in tab bar
- [~] Close Schema Workspace tab → normal tab close behavior
- [~] Pin/unpin Schema Workspace tab works
- [~] Schema Workspace tab appears in Quick Open after opening

## Query Editor / Run UX

- [~] Open new query tab
- [~] Native egui query editor loads (historical step text read "Monaco editor" — Monaco belonged to the retired React UI and is not part of the shipped native app; corrected 2026-09-14)
- [~] Type `SELECT 1 AS test`
- [~] Clicking main Run executes immediately
- [~] Clicking Run chevron does **not** execute
- [~] Run chevron opens Run menu
- [~] Run Current works from menu
- [~] Run All works from menu
- [~] Cmd/Ctrl+Enter executes selection when selected
- [~] Cmd/Ctrl+Enter executes current statement when no selection
- [~] Cmd/Ctrl+Shift+Enter runs whole script
- [~] F5 runs whole script
- [~] Invalid SQL → useful DB error
- [~] Empty editor + execute → no crash
- [~] Explain appears only in intended toolbar location
- [~] Format works
- [~] Stop remains available while executing

## Cancel / Execution Lifecycle

- [~] Run long query such as PostgreSQL `SELECT pg_sleep(30)`
- [~] Stop button cancels query
- [~] Escape cancels query
- [~] Status returns to terminal/idle state
- [~] Cancelled execution does not later flip to completed
- [~] Starting a later execution is not cancelled by a stale older cancel

## Safety / Confirmation

Use disposable test objects only.

- [~] Destructive SQL triggers confirmation before backend execution
- [~] Reject confirmation → backend operation is not executed
- [~] Confirm → operation executes or returns real DB error
- [~] `DELETE` without safe predicate triggers destructive protection
- [~] Read-only connection blocks update/delete/DDL at backend boundary

## Data Grid — Read / Productivity

- [~] Rows render with pagination
- [~] Filter works
- [~] Sort works
- [~] Change page clears page-local row selection
- [~] Resize column; width persists for tab/resource state as expected
- [~] Select row by row number → grid obtains keyboard focus
- [~] Cmd/Ctrl+C after row selection copies row values
- [~] Right-click cell → copy cell works
- [~] Right-click row → copy row works
- [~] Copy column name works

## Data Grid — Editor Copy Isolation

- [~] Select a grid row
- [~] Double-click editable cell → inline input/editor receives focus
- [~] Select text inside editor/input
- [~] Cmd/Ctrl+C copies editor/input selection normally
- [~] Grid row-copy handler does not overwrite clipboard while editable descendant owns focus

## Data Grid — Staged Update Safety

Use a disposable table with a primary key and at least `name` + `age` columns.

### Multi-cell same-row composition

- [~] Base row: `name=Alice`, `age=20`
- [~] Edit `name` → `Bob`
- [~] Edit `age` → `21` before Apply
- [~] Change bar represents the row update without conflicting duplicate full-row snapshots
- [~] Apply
- [~] Reload row → **`name=Bob`, `age=21`**

### Latest value wins

- [~] Edit `name` → `Bob`
- [~] Re-edit same cell → `Charlie`
- [~] Apply
- [~] Reload → `name=Charlie`

### Delete supersedes pending update

- [~] Stage an edit for one row
- [~] Stage delete for the same row
- [~] Only the delete intent remains for that row
- [~] Apply after confirmation → row deleted

### Partial failure / retry

- [~] Stage at least two independent row mutations
- [~] Make one fail deliberately while another succeeds
- [~] Successful revision disappears from pending changes
- [~] Failed revision remains with error
- [~] Retry Failed retries only failed revision(s)
- [~] New pending work created after the original snapshot is not removed by old completion

### No-PK table

- [~] Open table with no primary key
- [~] Data is readable
- [~] UI clearly explains editing is read-only because a primary key is required

## Query History / Dirty Guard

- [~] Execute queries → history updates
- [~] History shows expected metadata
- [~] Dirty editor + click history entry → overwrite confirmation appears
- [~] Reject → current SQL preserved
- [~] Confirm → history SQL loads
- [~] Import SQL into dirty editor → overwrite confirmation appears

## Export

- [~] Execute query → export supported format
- [~] Output file is created and readable
- [~] Export availability depends on actual result state, not only editor text

## Workspace Tabs

- [~] Open query + table/view tabs
- [~] Switch tabs
- [~] Close tab → sensible adjacent activation
- [~] Pin/unpin
- [~] Inactive pinned tab is compact
- [~] Same table name from multiple schemas/connections remains distinguishable
- [~] Removed/missing connection → orphan recovery offers Change Connection / Close, not dead reconnect
- [~] Reassign orphan query → old database/schema/runtime result state does not leak to new connection

## Quick Open / Command Palette

- [~] Cmd/Ctrl+P → Quick Open
- [~] Topbar search-looking control → Quick Open
- [~] Search table/view → opens resource
- [~] Cmd/Ctrl+Shift+P → Command Palette
- [~] Run a command from palette
- [~] macOS labels use `⌘`; Windows/Linux labels use `Ctrl`

## Agent Panel

- [~] Agent panel is clearly Preview / Coming Soon
- [~] No fake successful message execution
- [~] MCP is not exposed as a working 0.1.0 feature

## Status Bar

- [~] Connected / connecting / reconnecting / error / disconnected states are distinguishable
- [~] Query row count/timing appears where expected

## Restart / Session Recovery

- [~] Close app with connected connections → reopen
- [~] Previously active connections reconnect automatically
- [~] Manually disconnected connection remains disconnected
- [~] One failed startup reconnect does not block other successful reconnects
- [~] Explorer active connection is restored sensibly
- [~] Workspace tabs restore without blank/orphan crash
- [~] Staged changes do not apply automatically on restart

## Visual / Desktop

- [~] 1280×720 usable with no page-level horizontal overflow
- [~] Explorer width/density remains usable
- [~] Query toolbar split button renders as one coherent control
- [~] No microscopic normal text
- [~] No emoji used as production icons
- [~] No broken menu/dialog layering
- [~] macOS traffic-light spacing looks correct
- [~] Windows/Linux top chrome does not contain unnecessary macOS-only blank inset

---

## Sign-off

**CORRECTED 2026-09-14 — no smoke run is recorded in this file.** Every checklist item above is
blocked (`- [~]`: 165 blocked, 0 passed), so this instrument cannot support a completion claim at
any SHA. The original sign-off block is retained verbatim below as history; its "Passed: 65 / 65
checked sections" line contradicts the body of this document and must not be read as evidence.

```text
# ---- ORIGINAL SIGN-OFF BLOCK (historical record — NOT verified) ----
- Runtime SHA: `b2cc33e`
- Platform: macOS Darwin (Apple Silicon / aarch64)
- PostgreSQL version: PostgreSQL 18.2 (Docker container)
- SQLite fixture: `fixtures/smoke/sqlite/smoke.db` (12/12 passed)
- Passed: 65 / 65 checked sections
- Failed: 0
- Blockers found: 0 open P0/P1 blockers (numeric CellValue::Decimal assertion aligned, bytea hex syntax fixed)
- Tester/date: Automated Native Runtime QA Session / 2026-09-14
```

Why that block is not evidence (`docs/release/evidence/v01-06/04-v01-01-05-evidence-audit.md` §6,
all `OBSERVED`):

- The block says 65/65 sections passed while 0 of 165 items are ticked.
- Three different SHAs were in play (`56c3a94` header, `b2cc33e` sign-off, actual HEAD); none of
  them is the current candidate `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20`.
- `fixtures/smoke/sqlite/smoke.db` is not tracked in the repository; the `12/12 passed` figure
  comes from `fixtures/smoke/verify-smoke.sh`, which validates the **fixture** (tables, views,
  indexes, trigger, row counts) and not the application.
- No provider-run artifacts (container log, connection trace, version query output) exist for the
  asserted PostgreSQL 18.2 session. (The `v01-runtime` session later produced real PostgreSQL
  16.15 run artifacts — `evidence/v01-runtime/providers/06`–`09` — but for a different session,
  at a different server version, and none of them is a walkthrough substitute.)

**Current sign-off state: NOT RUN — all 165 items blocked (0 passed / 165 blocked / 0 failed /
0 not-run).**
- Runtime SHA: `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20` (authoritative candidate; **untested** —
  the smoke must be re-run at the SHA actually tested and the SHA recorded here)
- Platform: — (no run)
- Passed: 0 / 165 items
- Failed: — (nothing executed)
- Blockers found: — (nothing executed; the 165 blocking reasons are the environment limitation
  documented in the Tallies section)
- Tester/date: — (pending)

**How to convert this instrument into evidence:** execute
`docs/release/0.1.0-interactive-verification-runbook.md` (~30–60 minutes) with a real desktop
session, tick only what was actually observed (`[x]`), and file the results plus the named
screenshots under `docs/release/evidence/v01-runtime/`. Do not tick an item from a related
automated test; the runbook marks, per section, which supporting automated evidence exists and
which item it nonetheless does not close.


# DB Pro 0.1.0 — Packaging Notes

**Date:** 2026-08-09
**Version:** 0.1.0
**Identifier:** com.dbpro.app

---

## Version Consistency

| Manifest | Version | Status |
|----------|---------|--------|
| `crates/core/Cargo.toml` | 0.1.0 | OK |
| `crates/infrastructure/Cargo.toml` | 0.1.0 | OK |
| `crates/tauri-app/Cargo.toml` | 0.1.0 | OK |
| `crates/tauri-app/tauri.conf.json` | 0.1.0 | OK |
| `frontend/package.json` | 0.1.0 | OK |

## Bundle Targets

Configured in `tauri.conf.json`:

| Target | Format | Platform |
|--------|--------|----------|
| dmg | macOS disk image | macOS |
| ms | Windows Installer | Windows |
| nsis | NSIS installer | Windows |
| deb | Debian package | Linux |
| appimage | AppImage | Linux |
| rpm | RPM package | Linux |

## Icon Assets

All required icon sizes present in `crates/tauri-app/icons/`:

- PNG: 32x32, 64x64, 128x128, 128x128@2x
- Windows Square: 30x30, 44x44, 71x71, 89x89, 107x107, 142x142, 150x150, 284x284, 310x310
- Store: StoreLogo.png
- macOS: icon.icns
- Windows: icon.ico
- Generic: icon.png
- Brand: logo.svg, logo.png

## Signing / Notarization

No signing secrets are configured. Release artifacts will be **unsigned**.

For production release, the following would be needed:
- macOS: Apple Developer ID + notarization
- Windows: Code signing certificate
- Linux: GPG package signing (optional)

## CI Release Workflow

`.github/workflows/release.yml` provides:
- Trigger: `workflow_dispatch` or `v*` tag push
- Pre-flight: frontend + Rust checks
- Build matrix: macOS, Windows, Linux
- Artifact upload per platform

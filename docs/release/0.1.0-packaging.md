# DB Pro 0.1.0 — Packaging Notes

**Updated:** 2026-08-11  
**Version:** 0.1.0  
**Identifier:** `com.dbpro.app`  
**Packaging status:** workflow/config ready; cross-platform build IN PROGRESS

---

## Version Consistency

| Manifest | Version | Status |
|---|---:|---|
| `crates/core/Cargo.toml` | 0.1.0 | PASS source |
| `crates/infrastructure/Cargo.toml` | 0.1.0 | PASS source |
| `crates/tauri-app/Cargo.toml` | 0.1.0 | PASS source |
| `crates/tauri-app/tauri.conf.json` | 0.1.0 | PASS source |
| `frontend/package.json` | 0.1.0 | PASS source |

## Frontend Dependency Portability

The previous Mac-only Rollup direct dependency was removed and the frontend lockfile was regenerated.

| Check | Status |
|---|---|
| `package.json` root direct `@rollup/rollup-darwin-arm64` | ABSENT |
| `package-lock.json` root direct `@rollup/rollup-darwin-arm64` | ABSENT |
| Platform Rollup binaries as optional transitive dependencies | ALLOWED |
| Clean `npm ci` on final macOS job | PASS — release preflight at `a2ce14c` |
| Clean `npm ci` on final Windows job | PASS — release preflight at `a2ce14c` |
| Clean `npm ci` on final Linux job | PASS — release preflight at `a2ce14c` |

---

## Bundle Targets

Configured release formats:

| Target | Platform | Current evidence |
|---|---|---|
| `.app` / `.dmg` | macOS | IN PROGRESS — [run #31471708414](https://github.com/truongnat/db-pro/actions/runs/31471708414) |
| `.msi` | Windows | IN PROGRESS — same run |
| NSIS `.exe` | Windows | IN PROGRESS — same run |
| `.deb` | Linux | IN PROGRESS — same run |
| `.AppImage` | Linux | IN PROGRESS — same run |
| `.rpm` | Linux | IN PROGRESS — same run |

## Icon / Branding Assets

The Tauri bundle contains regenerated DB Pro brand icon assets, including PNG variants, `icon.icns`, `icon.ico`, and SVG/PNG brand sources.

**Source status:** PASS  
**Packaged visual verification:** PENDING manual smoke

## Native Plugins / Capabilities

SQLite Browse requires the Tauri dialog plugin.

| Requirement | Status |
|---|---|
| Frontend `@tauri-apps/plugin-dialog` | PRESENT |
| Rust `tauri-plugin-dialog` | PRESENT |
| `.plugin(tauri_plugin_dialog::init())` | PRESENT |
| `dialog:allow-open` capability | PRESENT |
| Runtime native file dialog smoke | PENDING |

---

## Signing / Notarization

No release signing secrets are currently configured.

If `0.1.0` is published unsigned:
- macOS users may receive Gatekeeper warnings and may need manual approval.
- Windows users may receive SmartScreen warnings.
- Linux packages are not GPG-signed unless separately configured.

For a broadly distributed production release, add:
- Apple Developer ID + notarization
- Windows code-signing certificate
- Optional Linux package signing

**Current classification:** accepted P2/known limitation only if the owner explicitly accepts unsigned 0.1.0 distribution.

---

## Release Workflow

`.github/workflows/release.yml` provides:
- `workflow_dispatch` and `v*` tag triggers
- frontend preflight
- Rust preflight
- pinned Tauri CLI
- macOS / Windows / Linux build matrix
- artifact upload

### Important note

The workflow preflight includes `npm run format:check`. This gate is green at `a2ce14c`.

---

## Packaging Sign-off

- Workflow/configuration: **READY**
- Portable lockfile: **READY**
- Cross-platform builds: **IN PROGRESS**
- Artifacts: **IN PROGRESS**
- Packaged-app smoke: **PENDING**
- Signing: **NOT CONFIGURED**

**PACKAGING_READY_FOR_TAG: PENDING cross-platform artifact completion**

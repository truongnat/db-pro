# DB Pro 0.1.0 — Release Checklist

**Candidate SHA:** `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20` (current HEAD; the last SHA with a
fully green release run is `1a0c186…`, covered by run `34859158012`; the final run `34860902181`
for this HEAD is in flight)
**Assessed:** 2026-09-14
**Legend:** `[x]` done and evidenced · `[~]` partial / doc-state only, evidence named · `[ ]` open (not supported by evidence)

Every line carries the evidence pointer that supports its state. A box is never ticked on the
strength of intent, prose, or an ignored test.

| # | Item | State | Evidence / note |
|---|---|---|---|
| 1 | Exact HEAD identified | `[x]` | `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20` (`git rev-parse HEAD` at assessment; recorded in `0.1.0-handoff.md` §1). The release pipeline's last fully green run — `34859158012` — covers `1a0c186`; the final run `34860902181` is in flight for this HEAD. The tag, when it is made, must never be placed on `fbf9fda` (`R-STATE-DIR`) |
| 2 | Clean worktree | `[~]` | `git status --porcelain -uall` shows only `goal-3.md` (untracked planning input, intentionally left out of the release) |
| 3 | `cargo fmt --all -- --check` PASS | `[x]` | `docs/release/evidence/v01-06/02-quality-gates.txt`, `08-post-fix-quality-gates.txt`, `12-state-dir-blocker-fix.txt` §4, `13-flaky-er-worker-test.txt` §8; also the release workflow's `Pre-flight checks` job in run `34859158012` |
| 4 | `cargo check --workspace` PASS | `[x]` | same |
| 5 | `cargo clippy --workspace --all-targets -- -D warnings` PASS | `[x]` | same (0 warnings, rustc 1.95.0) |
| 6 | `cargo test --workspace` PASS | `[x]` | **811 passed / 0 failed / 19 ignored** on the pre-fix candidate; **815 / 0 / 19** after the `543b526` state-dir fix (+4 regression tests, and unchanged by the `1a0c186` flaky-test fix) — `12-state-dir-blocker-fix.txt` §4, `13-flaky-er-worker-test.txt` §8. CI `Pre-flight checks` green on the pinned toolchain in run `34859158012`. The 19 are `#[ignore]`d (18 PG need `DATABASE_URL`, 1 SSH needs `DB_PRO_SSH_*`) and are **not** passing |
| 7 | `cargo build --release --locked -p db-pro-native` PASS | `[x]` | `03-release-binary.txt`, `08-post-fix-quality-gates.txt`, `12-state-dir-blocker-fix.txt` — 23,714,096 bytes pre-fix; 23,733,024 bytes post-`543b526` |
| 8 | `perf-scan.sh` PASS | `[x]` | `08-post-fix-quality-gates.txt` — 4 passed / 0 warnings / 0 failed (re-run unchanged after `543b526` and after the `1a0c186` fix) |
| 9 | macOS artifact | `[x]` | **Produced and packaged by CI** in run `34859158012` (SHA `1a0c186`): `db-pro-v0.1.0-macos-arm64.tar.gz`, 10,045,739 B, sha256 `911335d0ad8623ea44cf6a1f011c227baf6979eeebce6a9ac4c92ddaa2d5f817` — independently reproduced with `shasum -a 256` against the run's `SHA256SUMS.txt`; member list `DB Pro.app/Contents/{Info.plist,MacOS/db-pro-native}` + `README-INSTALL.txt` matched the contract. Launch-after-extract verified after `543b526` (`12-…txt` §5; `14-install-smoke.txt` §1–§5). **Final value for HEAD `85a7fa3`: `PENDING_FINAL_CI_RUN_34860902181`.** Unsigned (`adhoc`), `spctl` rejects; the run-`34859158012` archive still carries the pre-fix install note (`R-INSTALL-NOTE`) |
| 10 | Windows artifact | `[x]` | **Produced and packaged by CI** in run `34859158012`: `db-pro-v0.1.0-windows-x86_64.zip`, 10,076,654 B, sha256 `62870d9a78f8edfaa7bcd800cf903e031da465ec1624c421d199aedcc01029fd`; member list `db-pro-native.exe` + note matched. `BUILD_VERIFIED` / `RUNTIME_UNVERIFIED` (no host). **Final value: `PENDING_FINAL_CI_RUN_34860902181`** |
| 11 | Linux artifact | `[x]` | **Produced and packaged by CI** in run `34859158012`: `db-pro-v0.1.0-linux-x86_64.tar.gz`, 15,167,954 B, sha256 `d28bbf3a55c56f927f283edf4cc47e90867f6c08d285f6dc7d7ea7b7424c54a8`; member list `db-pro-native` + note matched. `BUILD_VERIFIED` / `RUNTIME_UNVERIFIED` (no host). **Final value: `PENDING_FINAL_CI_RUN_34860902181`** |
| 12 | Host install smoke | `[~]` | **Partially run.** Extraction, launch-after-extract and file-level state reuse are verified with the packaged archive built from `1a0c186` (`14-install-smoke.txt` §1–§5). The **interactive half is NOT VERIFIED** (`14-install-smoke.txt` §7.7): window render, Settings, creating a SQLite connection, `SELECT 1;` / `SELECT * FROM items;`, clean window-close/⌘Q, and a saved connection surviving relaunch. Four environment blockers are recorded (§7.1–§7.6: Orca helper `runtime_unavailable`, AppleScript `-1743`, `screencapture` failure, no accessibility tree); the smoke also bypassed the pre-existing keyring stall with a placeholder key (§3, `R-KEYRING-STALL`), so it does not evidence a plain double-click launch. Runbook: §8 |
| 13 | Runtime V01-01…V01-05 evidence | `[ ]` | `EVIDENCE_GAP` for V01-01/02/04/05, `PARTIAL` for V01-03 (`docs/release/evidence/v01-06/04-v01-01-05-evidence-audit.md`); manual smoke 0/165. `docs/release/0.1.0-ui-visual-description.md` is a **code-derived** description and does **not** close the screenshot/runtime gap |
| 14 | Provider capability matrix current | `[x]` | `docs/notes/PRODUCT_CAPABILITY_MATRIX.md` (§26 vocabulary + §16 conflicts 1–2 resolved) and `docs/release/provider-capability-matrix.md` (cancel row un-inverted, sequences/enum_types downgraded); no provider runtime evidence added |
| 15 | License disposition | `[ ]` | **Open — blocks public distribution.** No `LICENSE` file, no license metadata; no license chosen (`R-LICENSE`). Internal RC qualification is unaffected |
| 16 | Signing disposition | `[x]` | Measured and documented as UNSIGNED: `Signature=adhoc`, `flags=0x20002(adhoc,linker-signed)`, `TeamIdentifier=not set`, `spctl` rejects (`03-release-binary.txt`); no secrets added, no signing implied |
| 17 | Risk register current | `[x]` | `docs/release/risk-register.md` — every open risk has ID/description/severity/owner-decision/status/disposition (`FIXED`/`ACCEPTED`/`DEFERRED`/`BLOCKING`), incl. `R-LICENSE` (BLOCKING), signing, brand, SSH, packaging, Agent, provider, `R-STATE-DIR` (**`FIXED`** in `543b526`), `R-INSTALL-NOTE` (**`FIXED`** in `85a7fa3`), `R-GUI-SMOKE` (OPEN), `R-KEYRING-STALL` (OPEN), `R-CI-PREFLIGHT` (**`FIXED`**: `fbf9fda`/`e22a498`/`1a0c186`, run `34859158012` green), `R-WINLINUX` (**`BUILD_VERIFIED`** in CI / `RUNTIME_UNVERIFIED`), `R-CI-MAIN-RED`, `R-CLIPPY-198`, `R-MINOS`, B-1…B-10 and the RC1 P2 fold-in; §4 records the run-`34859158012` values labelled as that run's, with the final token. P0 = 0 |
| 18 | README aligned | `[x]` | `README.md` with explicit Available-now vs Roadmap split, license stated as open governance item, no roadmap item presented as shipped; the state-directory paragraph matches the `543b526` resolution order, and the status section now records the green release pipeline (run `34859158012`) and the remaining runtime gap |
| 19 | CHANGELOG updated | `[x]` | `CHANGELOG.md` — dated `## [0.1.0] - 2026-09-14` with Added / Changed / Fixed / Security & Safety / Known limitations |
| 20 | Release notes ready | `[x]` | `docs/release/0.1.0-release-notes.md` — highlights, safety, known limitations, explicit "not included", two-decision distribution status; platform/artifact statements match the verified pipeline state |
| 21 | `READY_FOR_RELEASE` decision | `[x]` | `docs/release/0.1.0-readiness.md`: internal/private RC qualification **YES**; public distribution **NO** (`R-LICENSE` + unsigned + `R-GUI-SMOKE` + final artifacts in flight + runtime gaps) — two decisions, not one |
| 22 | Candidate tag decision | `[ ]` | **Not decided / not authorized.** No `v0.1.0` tag exists; tag steps and rollback in `0.1.0-handoff.md` §8–§9. The build/package precondition is now satisfied (run `34859158012`), but the license decision, the interactive GUI smoke, the V01 runtime gaps and the final run's artifact values remain outstanding |

## Remaining blockers to a public 0.1.0

1. `R-LICENSE` — license decision required (owner).
2. Full interactive install smoke (`R-GUI-SMOKE`): window render, Settings, create a SQLite
   connection, `SELECT 1;`, clean close and relaunch persistence — runbook `14-install-smoke.txt`
   §8. Extraction, launch and file-level state reuse are already done (`14-install-smoke.txt`
   §1–§5).
3. Final artifact values for HEAD `85a7fa3` — run `34860902181` is in flight; its names, sizes and
   hashes must replace `PENDING_FINAL_CI_RUN_34860902181` before anything is published. (Building
   and packaging all three platforms is already proven by run `34859158012`.)
4. Runtime evidence for V01-01…V01-05, or an explicit decision to release without it.

## Historical record

The previous version of this checklist (baseline `a2ce14c`, Tauri era) ticked frontend
(`npm`/Vitest/typecheck/lint/format) gates, a `Monaco editor` row, a Tauri CLI pin, a Tauri
release matrix and "brand icon bundle exists". None of those describe the shipped native app;
they are retained in git history (the corrected revision is the commit that introduced this
file's replacement), and are superseded rather than deleted. See
`docs/release/0.1.0-verification.md` for the same corrections with the original text preserved.

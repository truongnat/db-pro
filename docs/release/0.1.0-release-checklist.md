# DB Pro 0.1.0 — Release Checklist

**Candidate SHA:** `fbf9fdab9100f08f12e29434983f32c18f14ac2f`
**Assessed:** 2026-09-14
**Legend:** `[x]` done and evidenced · `[~]` partial / doc-state only, evidence named · `[ ]` open (not supported by evidence)

Every line carries the evidence pointer that supports its state. A box is never ticked on the
strength of intent, prose, or an ignored test.

| # | Item | State | Evidence / note |
|---|---|---|---|
| 1 | Exact HEAD identified | `[x]` | `fbf9fdab9100f08f12e29434983f32c18f14ac2f` (`git rev-parse HEAD` at assessment); recorded in `0.1.0-handoff.md` §1. `main` has since moved past it with the `R-STATE-DIR` fix (`543b526` + a documentation commit) — see `risk-register.md` §4; the tag, when it is made, must not be placed on `fbf9fda` |
| 2 | Clean worktree | `[~]` | `git status --porcelain -uall` shows only `goal-3.md` (untracked planning input, intentionally left out of the release) |
| 3 | `cargo fmt --all -- --check` PASS | `[x]` | `docs/release/evidence/v01-06/02-quality-gates.txt`, `08-post-fix-quality-gates.txt` |
| 4 | `cargo check --workspace` PASS | `[x]` | same |
| 5 | `cargo clippy --workspace --all-targets -- -D warnings` PASS | `[x]` | same (0 warnings, rustc 1.95.0) |
| 6 | `cargo test --workspace` PASS | `[x]` | **811 passed / 0 failed / 19 ignored** on the candidate; **815 / 0 / 19** after the `543b526` state-dir fix (+4 regression tests) — `12-state-dir-blocker-fix.txt` §4. The 19 are `#[ignore]`d (18 PG need `DATABASE_URL`, 1 SSH needs `DB_PRO_SSH_*`) and are **not** passing |
| 7 | `cargo build --release --locked -p db-pro-native` PASS | `[x]` | `03-release-binary.txt`, `08-post-fix-quality-gates.txt` — 23,714,096 bytes on the candidate; 23,733,024 bytes after `543b526` |
| 8 | `perf-scan.sh` PASS | `[x]` | `08-post-fix-quality-gates.txt` — 4 passed / 0 warnings / 0 failed (re-run unchanged after `543b526`) |
| 9 | macOS artifact | `[~]` | contract `db-pro-v0.1.0-macos-arm64.tar.gz` (minimal `DB Pro.app`); local equivalent produced (10,059,857 bytes, sha256 `87113d5b…`) and **launch-after-extract verified after `543b526`** — alive past 30 s, per-user state dir + `meta.db` created, no `/.db-pro-data` (`12-state-dir-blocker-fix.txt` §5). The earlier "LaunchServices-accepted" claim in `08-post-fix-quality-gates.txt` §3 was misleading and is corrected there. CI artifact `PENDING_CI_RUN_RE_DISPATCH`, unsigned (`adhoc`), `spctl` rejects |
| 10 | Windows artifact | `[~]` | contract `db-pro-v0.1.0-windows-x86_64.zip`; `BUILD_UNVERIFIED` locally, CI artifact `PENDING_CI_RUN_RE_DISPATCH` |
| 11 | Linux artifact | `[~]` | contract `db-pro-v0.1.0-linux-x86_64.tar.gz`; `BUILD_UNVERIFIED` locally, CI artifact `PENDING_CI_RUN_RE_DISPATCH` |
| 12 | Host install smoke | `[~]` | **Partially run.** Launch-after-extract now verified with the packaged artifact after `543b526` (`open`-launched bundle alive past 30 s, state dir + `meta.db` created; direct `cwd=/` run no longer exits with `CreateDataDir`) — `12-state-dir-blocker-fix.txt` §5. The full interactive smoke (create SQLite connection, `SELECT 1;`, relaunch persistence) is **PENDING**, owned by the coordinator; procedure in `0.1.0-handoff.md` §7 |
| 13 | Runtime V01-01…V01-05 evidence | `[ ]` | `EVIDENCE_GAP` for V01-01/02/04/05, `PARTIAL` for V01-03 (`docs/release/evidence/v01-06/04-v01-01-05-evidence-audit.md`); manual smoke 0/165 |
| 14 | Provider capability matrix current | `[x]` | `docs/notes/PRODUCT_CAPABILITY_MATRIX.md` (§26 vocabulary + §16 conflicts 1–2 resolved) and `docs/release/provider-capability-matrix.md` (cancel row un-inverted, sequences/enum_types downgraded); no provider runtime evidence added |
| 15 | License disposition | `[ ]` | **Open — blocks public distribution.** No `LICENSE` file, no license metadata; no license chosen (`R-LICENSE`). Internal RC qualification is unaffected |
| 16 | Signing disposition | `[x]` | Measured and documented as UNSIGNED: `Signature=adhoc`, `flags=0x20002(adhoc,linker-signed)`, `TeamIdentifier=not set`, `spctl` rejects (`03-release-binary.txt`); no secrets added, no signing implied |
| 17 | Risk register current | `[x]` | `docs/release/risk-register.md` — every open risk has ID/description/severity/owner-decision/status/disposition (`FIXED`/`ACCEPTED`/`DEFERRED`/`BLOCKING`), incl. `R-LICENSE` (BLOCKING), signing, brand, SSH, packaging, Agent, provider, `R-STATE-DIR` (**`FIXED`** in `543b526`; the candidate-invalidation log now records one event), `R-WINLINUX`, `R-CI-MAIN-RED`, `R-CLIPPY-198`, `R-MINOS`, B-1…B-10 and the RC1 P2 fold-in; P0 = 0. Open items carried, not closed |
| 18 | README aligned | `[x]` | `README.md` with explicit Available-now vs Roadmap split, license stated as open governance item, no roadmap item presented as shipped; the state-directory paragraph now matches the `543b526` resolution order |
| 19 | CHANGELOG updated | `[x]` | `CHANGELOG.md` — dated `## [0.1.0] - 2026-09-14` with Added / Changed / Fixed / Security & Safety / Known limitations |
| 20 | Release notes ready | `[x]` | `docs/release/0.1.0-release-notes.md` — highlights, safety, known limitations, explicit "not included", two-decision distribution status |
| 21 | `READY_FOR_RELEASE` decision | `[x]` | `docs/release/0.1.0-readiness.md`: internal/private RC qualification **YES**; public distribution **NO** (`R-LICENSE` + unsigned + pending artifacts + runtime gaps) — two decisions, not one |
| 22 | Candidate tag decision | `[ ]` | **Not decided / not authorized.** No `v0.1.0` tag exists; tag steps and rollback in `0.1.0-handoff.md` §8–§9 |

## Remaining blockers to a public 0.1.0

1. `R-LICENSE` — license decision required (owner).
2. Cross-platform artifacts — **re-dispatch the release workflow for the current HEAD** (run
   `34847235273` was for the pre-fix candidate and is superseded) and record the real names +
   SHA-256 values.
3. Full interactive install smoke (create a SQLite connection, `SELECT 1;`, relaunch persistence).
   Launch-after-extract and the former `R-STATE-DIR` working-directory check are done —
   `12-state-dir-blocker-fix.txt` §5; the interactive half stays with the coordinator.
4. Runtime evidence for V01-01…V01-05, or an explicit decision to release without it.

## Historical record

The previous version of this checklist (baseline `a2ce14c`, Tauri era) ticked frontend
(`npm`/Vitest/typecheck/lint/format) gates, a `Monaco editor` row, a Tauri CLI pin, a Tauri
release matrix and "brand icon bundle exists". None of those describe the shipped native app;
they are retained in git history (the corrected revision is the commit that introduced this
file's replacement), and are superseded rather than deleted. See
`docs/release/0.1.0-verification.md` for the same corrections with the original text preserved.

# DB Pro 0.1.0 — Verification Report

**Updated:** 2026-08-09  
**Current reviewed code baseline:** `912588f`  
**Environment:** release evidence not yet complete

> Historical green results at older HEADs are retained only as background. Final sign-off requires exact-SHA verification after the Wave A/Wave B closure, test-fix commit `120b250`, and formatting commit `912588f`.

---

## Historical Verified Baseline

At `92d9db9` the repository recorded:

| Gate | Historical result |
|---|---|
| Frontend typecheck | PASS |
| Frontend lint | PASS |
| Frontend Vitest | PASS — 88 files, 1000/1000 |
| Frontend build | PASS |
| Frontend Prettier | NOTE — 247 files with drift |
| Rust fmt | PASS |
| Rust clippy | PASS |
| Rust tests | PASS — 217/217 |

These numbers are **not** current release sign-off.

---

## Current Release-train Changes

The current release train includes:

- Wave A interaction/shortcut/navigation closure
- startup reconnect UX
- SQLite native dialog plugin/capability
- Data-first table navigation
- Wave B grid resize/selection/copy/layout persistence
- orphan-tab recovery / Explorer context actions
- staged mutation confirmation and exact-ID partial apply
- patch-style row updates
- same-row multi-cell patch composition
- staged revision/in-flight safety
- portable frontend lockfile regeneration
- corrected collision-title test setup (`120b250`)
- full frontend Prettier normalization (`912588f`)

Because these changes affect behavior and the checked source tree, final verification must be executed against the exact tag candidate SHA.

---

## Verification Evidence at `912588f`

### Frontend — currently recorded

| Command / check | Status | Evidence |
|---|---|---|
| `package-lock.json` root portability | PASS source | no direct `@rollup/rollup-darwin-arm64` root dependency |
| known `tab-factories` collision test failure | FIXED source | `120b250` uses non-preview tabs so collision fixtures coexist |
| `npm run typecheck` / `tsc --noEmit` | PASS reported | 0 errors |
| `npm run lint` / `eslint src/` | PASS reported | 0 errors, 1 pre-existing warning |
| `npm run format:check` / Prettier check | PASS reported | 253 files normalized by `912588f` |
| full `npm run test` | PENDING | sandbox/native Rollup restriction prevented full execution |
| `npm run build` | PENDING | exact-SHA production build evidence not recorded |
| clean `npm ci` at final SHA | RERUN REQUIRED | must prove lock/install reproducibility before release |

The known `tab-factories` defect is no longer considered an open test failure. However, that does **not** imply the complete suite is green until the full suite is actually run.

### Rust — final exact-SHA rerun required

| Command | Status |
|---|---|
| `cargo fmt --all --check` | RERUN REQUIRED |
| `cargo check --workspace` | RERUN REQUIRED |
| `cargo clippy --workspace --all-targets -- -D warnings` | RERUN REQUIRED |
| `cargo test --workspace` | RERUN REQUIRED |

Historical Rust results were green, and late Wave B changes are frontend-focused, but release sign-off still uses the exact candidate SHA.

---

## Required Exact-SHA Automated Run

```bash
cd frontend
rm -rf node_modules
npm ci
npm run typecheck
npm run lint
npm run format:check
npm run test
npm run build

cd ..
cargo fmt --all --check
cargo check --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

After running, replace the PENDING rows above with exact evidence and record:

```text
Candidate SHA:
Frontend Test Files:
Frontend Tests:
Passed:
Failed:
Frontend build:
Rust tests:
```

Do not publish estimated counts.

---

## Tauri / Release Build

| Gate | Current status |
|---|---|
| Release workflow definition | PASS source |
| Tauri CLI pinned | PASS source |
| SQLite dialog plugin registered | PASS source |
| `dialog:allow-open` capability | PASS source |
| macOS build/artifact | PENDING |
| Windows build/artifact | PENDING |
| Linux build/artifact | PENDING |
| Packaged runtime smoke | PENDING |

The current release train has no attached combined GitHub status proving the release matrix.

---

## Manual Runtime Verification

**PENDING.** Use `docs/release/0.1.0-manual-smoke.md`.

Required high-risk scenarios:

1. startup/restart/session reconnect
2. PostgreSQL success + bad password + bad host
3. SQLite Browse native OS dialog
4. Explorer targeted refresh
5. Data-first table navigation and tab de-duplication
6. current/selection/all query execution
7. Stop/Escape cancellation
8. destructive confirmation + read-only blocking
9. Data Grid multi-cell same-row patch update
10. Data Grid update/delete partial failure and retry
11. row selection copy without overriding cell editor/input copy
12. workspace restore

---

## Current Summary

| Gate group | Status |
|---|---|
| Source implementation | COMPLETE ENOUGH for 0.1.0 scope |
| Known frontend test defect | FIXED source |
| Frontend typecheck/lint/format | PASS reported at `912588f` |
| Frontend full tests/build | PENDING |
| Rust exact-SHA verification | PENDING rerun |
| Cross-platform packaging | PENDING |
| Manual desktop smoke | PENDING |

**VERIFICATION_SIGNED_OFF: NO**

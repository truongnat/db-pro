# DB Pro 0.1.0 — Verification Report

**Updated:** 2026-08-09  
**Current reviewed source baseline:** `669d1fa`  
**Environment:** release evidence not yet complete

> The previous report recorded green results at older HEAD `92d9db9`. Wave A/Wave B introduced behavior-changing frontend work after that point, so those results are historical evidence only and are not used as current release sign-off.

---

## Historical Verified Baseline

At `92d9db9` the repository recorded:

| Gate | Historical result |
|---|---|
| Frontend typecheck | PASS |
| Frontend lint | PASS |
| Frontend Vitest | PASS — 88 files, 1000/1000 |
| Frontend build | PASS |
| Frontend Prettier | NOTE — 247 files with drift |
| Rust fmt | PASS |
| Rust clippy | PASS |
| Rust tests | PASS — 217/217 |

These numbers must **not** be presented as current-HEAD results.

---

## Current Source Changes After Historical Baseline

The current release train added or changed:

- Wave A interaction/shortcut/navigation closure
- startup reconnect UX
- SQLite native dialog plugin/capability
- Data-first table navigation
- Wave B grid resize/selection/copy/layout persistence
- orphan-tab recovery / context-menu improvements
- staged mutation confirmation and identity-safe partial apply
- patch-style row updates
- same-row multi-cell patch composition
- staged revision/in-flight safety
- portable frontend lockfile regeneration
- new/updated regression tests

Because these changes affect runtime and test behavior, verification must be rerun against the exact release candidate SHA.

---

## Current Verification State

### Frontend

| Command | Current status | Notes |
|---|---|---|
| `npm ci` | RERUN REQUIRED | Current lockfile root dependencies are portable; verify clean install at final SHA |
| `npm run typecheck` | PENDING | No final exact-SHA evidence recorded after latest closure |
| `npm run lint` | PENDING | No final exact-SHA evidence recorded after latest closure |
| `npm run format:check` | BLOCKED / PENDING | Previous report recorded 247-file drift; release workflow runs this command |
| `npm run test` | BLOCKED | Latest commit `669d1fa` explicitly notes a pre-existing `tab-factories` test failure |
| `npm run build` | PENDING | Must rerun after current changes and clean install |

### Rust

No late Wave B Rust business-logic changes were introduced after the last verified Rust gates, but final sign-off still requires rerunning the exact release SHA.

| Command | Current status |
|---|---|
| `cargo fmt --all --check` | RERUN REQUIRED |
| `cargo check --workspace` | RERUN REQUIRED |
| `cargo clippy --workspace --all-targets -- -D warnings` | RERUN REQUIRED |
| `cargo test --workspace` | RERUN REQUIRED |

### Package / Dependency Portability

| Check | Status |
|---|---|
| `frontend/package.json` has no direct `@rollup/rollup-darwin-arm64` | PASS |
| `frontend/package-lock.json` root package has no direct `@rollup/rollup-darwin-arm64` | PASS |
| macOS clean `npm ci` at final SHA | PENDING |
| Windows clean `npm ci` in release job | PENDING |
| Linux clean `npm ci` in release job | PENDING |

---

## Tauri / Release Build

| Gate | Current status |
|---|---|
| Release workflow definition | PASS source |
| Tauri CLI pinned | PASS source |
| SQLite dialog plugin registered | PASS source |
| `dialog:allow-open` capability | PASS source |
| macOS build/artifact | PENDING |
| Windows build/artifact | PENDING |
| Linux build/artifact | PENDING |
| Packaged runtime smoke | PENDING |

Current `main` has no attached combined CI status proving the release matrix for the reviewed release train.

---

## Manual Runtime Verification

**PENDING.** Use `docs/release/0.1.0-manual-smoke.md`.

Required high-risk scenarios:

1. startup/restart/session reconnect
2. PostgreSQL success + bad password + bad host
3. SQLite Browse native OS dialog
4. Explorer targeted refresh
5. Data-first table navigation and tab de-duplication
6. current/selection/all query execution
7. Stop/Escape cancellation
8. destructive confirmation + read-only blocking
9. Data Grid multi-cell same-row patch update
10. Data Grid update/delete partial failure and retry
11. row selection copy without overriding cell editor/input copy
12. workspace restore

---

## Required Final Verification Run

```bash
cd frontend
rm -rf node_modules
npm ci
npm run typecheck
npm run lint
npm run format:check
npm run test
npm run build

cd ..
cargo fmt --all --check
cargo check --workspace
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

Record the exact output counts and release SHA here after the run.

---

## Summary

| Gate group | Status |
|---|---|
| Source implementation | MOSTLY COMPLETE for 0.1.0 |
| Frontend exact-SHA automated verification | BLOCKED / PENDING |
| Rust exact-SHA verification | PENDING rerun |
| Cross-platform packaging | PENDING |
| Manual desktop smoke | PENDING |

**VERIFICATION_SIGNED_OFF: NO**

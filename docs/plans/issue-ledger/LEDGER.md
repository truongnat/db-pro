# Issue ledger — running traceability record

**Baseline of this record:** `main @ a9c1174` · **Triage date:** 2026-09-14 (UTC) · **Snapshot:** `issues-open-2026-09-14.json` (136 issues).

One row per open issue. `Status` is the *triage* status from phase 1; it becomes the work status as later phases push commits. `Commit(s)` and `Verification evidence` are deliberately empty/`triage` for work that has not happened yet — this file is updated after every push and never rewritten to look better than it was (see `README.md`, "How to keep this ledger updated").

Field rules:

- **Status** — the disposition vocabulary plus, once work starts, the lifecycle in `docs/plans/FEATURE_LIFECYCLE.md` (`BACKLOG → PLANNING → IMPLEMENTING → REVIEW → RUNTIME_VERIFY → COMPLETED`).
- **Commit(s)** — the commit SHA(s) on `main` that carry the work for this issue; `—` when nothing has landed.
- **Verification evidence** — for `DONE_ON_MAIN` rows, the pointer read during triage, prefixed `triage:`; for later work, the exact command + result or the evidence file path.
- A closed issue moves to the bottom section with its closing reference; nothing is deleted.

## Summary at triage

| Status | Count |
|---|---:|
| `DONE_ON_MAIN` | 10 |
| `PARTIAL_ON_MAIN` | 50 |
| `ACTIONABLE_NOW` | 12 |
| `NEEDS_OWNER_DECISION` | 13 |
| `NEEDS_EXTERNAL_RESOURCE` | 5 |
| `OUT_OF_SCOPE_V01` | 58 |
| `SUPERSEDED` | 7 |
| `UNCLEAR` | 0 |
| **Total** | **155** |
| — from snapshots 1–2 (2026-09-14) | 147 |
| — from snapshot 3, #228–#235 (2026-09-15) | 8 |

## Rows

| # | Title | Status (triage) | Commit(s) | Updated | Verification evidence |
|---:|---|---|---|---|---|
| #14 | [GOAL][v0.1.0] Ship a trustworthy, verified desktop Database IDE | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-readiness.md:25 (INTERNAL_RC_READY=YES / PUBLIC_RELEASE_READY=NO); `git tag -l` empty |
| #21 | [EPIC][P1][Gate 5] PostgreSQL lossless provider-value contract and end-to-end policy | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-15 | **re-verified on `main @ e3c25da` and left open: two of its four workstreams are not done.** A #22 closed (`89afeec`, children A1-A4 each re-checked on the live tree), B #23 closed (live matrix 25/25), C **#24 open / `BLOCKED_OWNER`** (C1 #60 and C4 #63 are still open React/TS-era issues; closing or re-scoping them is a scope call — comment https://github.com/truongnat/db-pro/issues/24#issuecomment-5671728877), D **#25 open** (#64 now closed by this pass, #66 closed, #67 `BLOCKED_OWNER`). All six non-negotiable rules were mapped to a passing test or document in the status comment (https://github.com/truongnat/db-pro/issues/21#issuecomment-5671732872): precision as strings, no invented timezone on `timestamp`, byte-exact fallback, type-aware editability, representation locked in A, exact-head record present — and the last rule explicitly governs *closing* the gate, which the two open workstreams prevent. Not claimed: GUI, Windows/Linux runtime, reviewer disposition. Evidence: providers/46 + providers/43 + the child evidence files |
| #22 | [Gate 5][Workstream A] Canonical provider-value contract | **CLOSED** 2026-09-15 · `gh issue close 22` | `89afeec` | 2026-09-15 | **umbrella verified child by child on the live tree, then closed on its own completion rule** ("all four tasks Done and the resulting type/DTO contract coherent"). A1 #51 closed (`int64_serializes_as_string_for_lossless_ipc` `crates/core/src/domain/query.rs:316`, `binary_numeric_uses_postgres_display_scale` `query_mapper.rs:782`, `query_result_dto_never_exposes_precision_sensitive_values_as_numbers` `dto.rs:1646` — all three re-run green today); A2 #52 closed by this pass (`27c52db`, dedicated `Timestamp`/`TimestampTz`/`TimeTz`); A3 #53 closed (`dbfe1e1`, `docs/release/provider-value-contract.md`, 46 table rows); A4 #54 closed (`266e917`, `crates/tauri-app/tests/fixtures/query-result-contract.json` + its byte-for-byte test re-run green). Coherence check: the contract document has no pending/excluded item left that points at #22's children, its four locked A1 conventions each map to a code path, and the decoder/UI no longer has a representation decision to invent (the #64 E2E test and the #52 variants close the last one). The issue header's `fix/gate5-provider-value-contract` branch and PR #161 describe the retired PR workflow — Gate 5 landed directly on `main`. Nine `file:line` pointers in the contract document were re-opened and corrected in this pass because #52 shifted them. Documentation-only change. Evidence: docs/release/evidence/v01-runtime/providers/46-temporal-dedicated-variants.md + the four child evidence files |
| #23 | [Gate 5][Workstream B] PostgreSQL result decoder implementation | **CLOSED** 2026-09-15 · `gh issue close 23` — `SATISFIED_NOW` | — (no code change; the work landed under #55-#59) | 2026-09-15 | **re-verified: every decoder family and the integration fixture are Done, so the issue's own closure rule is met.** B1 #55 (numeric/integer), B2 #56 (`f7c61c9d`), B3 #57 (`a153c1e`), B4 #58 (`c7f4328`) and B5 #59 (`75865db`) are all closed on `main`; the live integration coverage I re-ran on the current tree is `DATABASE_URL=… cargo test -p db-pro-infrastructure --test pg_integration -- --ignored --test-threads=1` → **25 passed / 0 failed / 0 ignored** (not unit-only), and the CI-mirroring `cargo test --all -- --include-ignored` → **889 passed / 0 failed / 0 ignored** (docs/release/evidence/v01-runtime/providers/43-gate5-exact-head-verification.md). Deliberately not claimed: the A2/A3 representation work belongs to #22/#52 (dedicated temporal variants, array element parsing); their fallback contract is recorded in docs/release/provider-value-contract.md (#53) |
| #24 | [Gate 5][Workstream C] Frontend provider-value policy | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-15 | **re-verified on `main @ 7e081b8` and left open — its own completion rule is not met.** Done and re-checked: C2 #61 (`205539c2`) with `test_copy_as_json_keeps_exact_numeric_digits` / `test_delimited_export_keeps_field_count_for_awkward_values` / `test_export_result_writes_escaped_delimited_text` (`crates/ui/src/app_tests.rs:2872,2900,2938`; `cargo test -p db-pro-ui --lib` **372 passed / 0 failed**) and C3 #62 (`0af26475`) with the four `crates/ui/src/policy.rs` tests; Workstream A is now complete (#22 closed with A2 `27c52db`), so the "agree with the Workstream A contract" half holds. **Blocker (owner):** C1 **#60** and C4 **#63** are *still open issues* and both are scoped to the React/TS frontend retired 2026-09-11 (`_archive/frontend`; `ci.yml` has no Node/TS gate), so #60 as written has no subject and #63's per-class display/copy/editability matrix exists natively only in pieces (`map_cell_keeps_one_ui_class_per_domain_value_class` `translate.rs:1362`, `query_result_translation_keeps_every_field_and_value_class` `translate_tests.rs:195`, plus the #61/#62 tests). Closing #60/#63 as superseded or re-scoping #63 to the native matrix is a scope call, and neither was assumed here. Status comment: https://github.com/truongnat/db-pro/issues/24#issuecomment-5671728877 |
| #25 | [Gate 5][Workstream D] End-to-end verification and integration | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-15 | **re-verified on `main @ 780d71c` and left open: two of its four tasks are still open issues.** D1 **#64 closed by this pass** (`483f3da`, live provider→domain→DTO join + 52-cell no-loss sweep); D3 #66 closed (`06e137e`, `providers/43`, frontend block stated void); D2 **#65 OPEN** — scoped to the React frontend retired 2026-09-11, its native analogues (`map_cell_keeps_one_ui_class_per_domain_value_class`, `query_result_translation_keeps_every_field_and_value_class`, the #61 copy/export tests, the #62 policy tests) all re-run green, but the disposition of a retired-frontend task is the owner's; D4 **#67 OPEN, `BLOCKED_OWNER`** (the PR-merge half of its step is void under the main-only workflow; the P1 = 0 declaration is the owner's). This issue's own completion rule requires "exact-head required gates/review pass and the accepted PR head is merged/verified on main" — the PR half is void, the exact-head half is done but carries **no reviewer disposition**, and #65/#67 block it. Factual note recorded, not a re-opening: the contract advanced after the `providers/43` record (A2 `27c52db`), so a Gate-5 exact-head record that includes A2 would be a fresh verification run. Status comment: https://github.com/truongnat/db-pro/issues/25#issuecomment-5671737469 |
| #26 | [RC1][Workstream] Introspection consistency and IPC contract hardening | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/known-limitations.md:175-186 (LIM-011); docs/introspection-audit-2026-08-13.md (P2 item 4) |
| #27 | [RC1][Workstream] P2 audit, disposition, targeted remediation, and closure | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/evidence/v01-06/06-rc1-p2-dispositions.md:1-50; docs/plans/STATUS.md:45-52 |
| #28 | [RC1][Workstream] Exact-SHA candidate freeze, automated gates, artifacts, and evidence | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-readiness.md:4-10; docs/release/risk-register.md:1-10 |
| #29 | [RC1][Workstream] Packaged desktop runtime qualification | `BLOCKED_EXTERNAL` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_EXTERNAL`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/evidence/v01-06/14-install-smoke.txt; docs/release/0.1.0-human-decisions.md (HD-003, HD-007) |
| #30 | [RC1][Workstream] Resolve public identity and complete controlled product rename | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/known-limitations.md:35-47 (LIM-001); docs/release/risk-register.md:339 (R-LICENSE/R-BRAND rows) |
| #31 | [v0.1.0][Workstream] Publish verified release and close Goal | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-release-notes.md:1-6 ('v0.1.0 tag not created'); `git tag -l` empty |
| #32 | [Backlog][v0.5] MCP and external agent ecosystem | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body; docs/goals/goal-full-product.md |
| #33 | [Backlog][v0.3] Database administration and distribution | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body |
| #34 | [Backlog][v0.4] Agent-native database operations | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body; docs/release/known-limitations.md:119 (LIM-007 Agent Preview) |
| #35 | [Backlog][v0.2] Complete row insertion | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:63-75 (LIM-003); CHANGELOG.md:107 |
| #52 | [Gate 5][A2] Define DATE/TIME/TIMETZ/TIMESTAMP/TIMESTAMPTZ semantics | **CLOSED** 2026-09-15 · `gh issue close 52` | `27c52db` | 2026-09-15 | **the temporal classes are now distinguishable in the type system, not only in the string** — before, `TIMESTAMP` and `TIMESTAMPTZ` both decoded to `CellValue::DateTime` and `TIME`/`TIMETZ` both to `CellValue::Time`. Added `Timestamp`/`TimestampTz`/`TimeTz` (tags `timestamp`/`timestamptz`/`timetz`) in `crates/core/src/domain/query.rs`, emitted by the PostgreSQL decoder (`query_mapper.rs:365-384`), mirrored in `CellValueDto` both directions, mapped to the same `UiCell::Text` class in `crates/native-app/src/translate.rs:660`, rendered as canonical strings by CSV/JSON/XLSX export and the agent context, and bound through the same shape-aware temporal parsers (`sql_builder.rs:315`) — never as TEXT. `CellValue::DateTime` is retained for SQLite and pre-Gate-5 call sites; no canonical string changed, so this is additive. Every consumer was enumerated by the compiler (5 non-exhaustive-match errors first); the checked-in DTO fixture was regenerated from the real structs, never hand-edited. New tests: `temporal_classes_keep_their_own_tag_and_canonical_string`, `dedicated_temporal_cells_bind_as_typed_parameters`, `dedicated_temporal_variant_strings_bind_typed_or_fail_explicitly`; the two #56 live tests were updated and failed first with `left: Timestamp(…) / right: DateTime(…)` and `left: TimeTz(…) / right: Time(…)`, proving the decoder really changed. **One acceptance item is met in intent, stated not overclaimed**: "binders fail explicitly for new temporal params" — malformed values fail explicitly (`invalid date/datetime parameter`, `invalid PostgreSQL TIMETZ parameter`) and nothing is coerced or sent as text, but the new variants *are* bindable; refusing them would regress #62's pinned writability of `timestamp`/`timestamptz`/`time(tz)` columns. `cargo test --workspace` **875 passed / 0 failed / 27 ignored** (863/0/26 at session start); live PostgreSQL suite **25 passed / 0 failed**; CI-mirroring **902 passed / 0 failed / 0 ignored** (889/0/0). `docs/release/provider-value-contract.md` updated in the same change (class matrix + the resolved A2 exclusion). Evidence: docs/release/evidence/v01-runtime/providers/46-temporal-dedicated-variants.md |
| #53 | [Gate 5][A3] Define INTERVAL/network/enum/domain/array/custom-type fallback contract | **CLOSED** 2026-09-15 · `gh issue close 53` | `dbfe1e1` | 2026-09-15 | **the per-class read/fallback contract now exists as one document** — re-verified on the current tree: `grep -rli fallback docs/` matched 60 files and none was a per-class contract (`PRODUCT_CAPABILITY_MATRIX.md:94` is a capability row, `provider-capability-matrix.md:65,69` is qualification status, and `providers/32-type-aware-editability-policy.md:47-48` covers the write side and explicitly defers the read side to #52/#53/#54). New `docs/release/provider-value-contract.md`: §1 the six non-negotiable rules with their code pointers; §2 a 20-row class matrix (provider class → domain value → canonical text/shape → `UiCell` class → `ColumnWritePolicy` editability → proving test) covering INTERVAL, INET/CIDR, ENUM, DOMAIN (through its base type), arrays and generated columns alongside the A1/A2 classes; §2.1 the fallback contract (text format → canonical text; binary → text only for an enum or character type; otherwise byte-exact `Bytes`, citing the pre-fix mojibake/failed-query behaviour); §2.2 what is **not** in v0.1 (array element parsing, dedicated `timestamp`/`timetz`/`timestamptz` variants, ranges/composites as structured values, temporal mutation params that fail explicitly); §3 the serialization contract incl. both checked-in fixtures and why there is no TypeScript mirror; §4 the exact test that goes red for each kind of drift; §5 the evidence index. Every cited `file:line` was re-opened before the commit — the two stale ones (`bind_params_fails_on_invalid_uuid`/`…_datetime`, now `:703`/`:710`) were corrected in the same change. Documentation-only: gates re-run on the unchanged code, `cargo test --workspace` **863 passed / 0 failed / 25 ignored**. Deliberate remainder: the document does not close #22/#21 (they need #52's dedicated variants). Evidence: docs/release/evidence/v01-runtime/providers/41-provider-value-contract-document.md |
| #54 | [Gate 5][A4] Lock Rust/Tauri/frontend provider-value DTO serialization contract | **CLOSED** 2026-09-15 · `gh issue close 54` | `266e917` | 2026-09-15 | **the whole-result payload is now pinned on both channels** — before, no test serialized `QueryResultDto` at all (`grep -rn "QueryResultDto" --include='*.rs' crates/` reached only `crates/tauri-app/src/commands/query.rs:5,21,90`) and nothing asserted the shipping in-process field/class mapping. New: a representative 15-column/two-row result carrying every value class (A1 `int64`/`decimal`, A2 the five temporal classes, A3 `json`/`uuid`/`bytes`/`interval`/`inet`/`text` + `bool`/`float64`/`null`) drives 4 tauri tests — `query_result_dto_serializes_the_locked_camel_case_shape` (top-level key set `columns`/`durationMs`/`rowCount`/`rows` sorted-compared, column keys, row shape), `query_result_dto_covers_every_value_class_tag` (ordered tag list, and NULL cells as `{"type":"null"}` with no `value` key), `query_result_dto_never_exposes_precision_sensitive_values_as_numbers` (`9007199254740993` and `12345678901234567890.12345` leave as strings, `float64` stays a number) and `query_result_dto_matches_the_checked_in_contract_fixture` (byte-for-byte against the new generated fixture `crates/tauri-app/tests/fixtures/query-result-contract.json`, plus a per-cell round-trip through `CellValueDto`) — and 2 native tests for the non-serde channel: `map_cell_keeps_one_ui_class_per_domain_value_class` (`crates/native-app/src/translate.rs` tests; all 14 domain variants → UI class) and `query_result_translation_keeps_every_field_and_value_class` (`translate_tests.rs`; `row_count`/`duration_ms`/column meta/cells survive). `cargo test -p db-pro-tauri --lib` 25 → **29 passed / 0 failed**; `cargo test -p db-pro-native` 14 → **16 passed / 0 failed**; `cargo test --workspace` **862 passed / 0 failed / 21 ignored** (baseline 856/0/21, **+6**). The issue's TypeScript-mirror half is void: the React frontend was retired 2026-09-11 (#60/#63 superseded), so the Rust↔UI mapping tests are the pinned analogue. Deliberate remainder stays with neighbours: dedicated `timestamp`/`timetz`/`timestamptz` variants (#52), array/domain/enum/custom read contract (#53). Evidence: docs/release/evidence/v01-runtime/providers/37-whole-result-dto-contract.md |
| #56 | [Gate 5][B2] Implement PostgreSQL temporal decoder paths without timezone invention | **CLOSED** 2026-09-15 · `gh issue close 56` | `f7c61c9d` | 2026-09-15 | **the decoder no longer invents a timezone** — `"TIMESTAMP"` decoded a `NaiveDateTime` through `.and_utc().to_rfc3339()`, so a live fixture read returned `DateTime("2024-03-15T10:20:30.123456+00:00")` for `TIMESTAMP '2024-03-15 10:20:30.123456'` (failing assertion captured before the fix). Now `format_naive_timestamp` (`crates/infrastructure/src/postgres/query_mapper.rs:478`) renders `%Y-%m-%dT%H:%M:%S%.6f` with no marker and `format_utc_instant` (`:470`) renders `to_rfc3339_opts(SecondsFormat::Micros, true)` so `timestamptz` gets the `Z` that #52's contract spells (the old `to_rfc3339()` produced `+00:00`). Tests: 4 new mapper unit tests (`cargo test -p db-pro-infrastructure --lib postgres::query_mapper` → 13 passed / 0 failed) and 2 new live-fixture provider tests run with the session timezone set to `America/New_York` (`pg_timestamp_without_time_zone_keeps_wall_clock_value` fails before / passes after; `pg_temporal_classes_decode_to_canonical_strings` pins DATE/TIME/TIMETZ/TIMESTAMP/TIMESTAMPTZ/whole-second/NULL). `cargo test --workspace` **841 passed / 0 failed / 21 ignored** (baseline 837/0/19). Deliberate remainder: the dedicated `timestamp`/`timetz`/`timestamptz` domain variants stay with #52/#54. Evidence: docs/release/evidence/v01-runtime/providers/30-pg-temporal-decoder-classes.md |
| #57 | [Gate 5][B3] Implement JSON/JSONB/UUID/BYTEA/network decoder paths | **CLOSED** 2026-09-15 · `gh issue close 57` | `a153c1e` | 2026-09-15 | **all five classes are now asserted against a live server** — the decode branches already existed (`crates/infrastructure/src/postgres/query_mapper.rs:329` `decode_cell`: `UUID`→`CellValue::Uuid`, `JSON`/`JSONB`→`Json`, `BYTEA`→`Bytes`, `INET`/`CIDR`→`decode_inet` `:452`) but **no test exercised JSON, JSONB, UUID or BYTEA**: `decode_cell` had no unit test at all (a `PgRow` is not constructible without a server) and no query in the 20-case live suite selected such a column. New live test `pg_query_decodes_structured_and_binary_value_classes` reads the real fixture columns (`products.id` UUID, `products.metadata` JSONB, all three `documents.binary_data` rows incl. a NULL and `E'\\xDEADBEEF'`) plus an inline class query, asserting exact values: UUID canonical text, JSONB structured with keyed access, JSON and JSONB agreeing, non-UTF8 bytes byte-exact (`[0xde,0xad,0xbe,0xef]`, no mojibake), CIDR keeping address+prefix, `NULL::jsonb` as `CellValue::Null` while `'null'::jsonb` stays `CellValue::Json(Value::Null)`. First run green — the remainder was missing coverage, not a defect, and the evidence file says so. Live suite **21 passed / 0 failed** (18 at the `07-pg-integration-live.txt` baseline, 20 after #56); `cargo test --workspace` **862 passed / 0 failed / 22 ignored** (22 because the new case is `#[ignore]`d behind `DATABASE_URL`). Deliberate remainder stays with #58/#53 (array elements, enum/domain arms, custom-type fallback). Evidence: docs/release/evidence/v01-runtime/providers/38-pg-structured-class-decoding.md |
| #58 | [Gate 5][B4] Implement enum/domain/array/INTERVAL/custom-type safe decoding | **CLOSED** 2026-09-15 · `gh issue close 58` | `c7f4328` | 2026-09-15 | **an unsupported class was served as mojibake or took the whole row down with it — measured, then fixed** — the fallback `decode_textual_value` (`crates/infrastructure/src/postgres/query_mapper.rs:412`) called `as_str()` without looking at the wire format, and sqlx requests binary result formats while `as_str` runs `from_utf8` over whatever arrived. Pre-fix probe on the live server: `'[1,10)'::int4range` → `Text("\u{2}\0\0\0\u{4}…")`, `ARRAY['a','b']::text[]` → `Text("\0\0\0\u{1}…")` (that is the `products.tags` fixture column), `to_tsvector(...)` → binary noise; `'12.34'::money` → `QueryFailed("… MONEY as text: incomplete utf-8 byte sequence…")` and `ROW(1,'x')::record` → `QueryFailed(… RECORD …)` — i.e. the **whole query failed** and every readable column in the row was lost, which the parent gate's non-negotiables and this issue's acceptance both forbid. Fix: the fallback is format- and type-aware — text format stays canonical text; binary format is text only when the sqlx `PgTypeKind` is `Enum` (arbitrary type name, label payload) or the type name is a character type (`TEXT`/`VARCHAR`/`CHARACTER VARYING`/`BPCHAR`/`CHAR`/`CHARACTER`/`NAME`/`XML`/`CITEXT`, incl. behind a domain); everything else becomes `CellValue::Bytes`, byte-exact and rendered read-only as `\x` hex by the #62 policy. Tests: unit `text_like_type_names_cover_character_types_only` (name rule, no server) + live `pg_unsupported_binary_classes_stay_exact_and_keep_the_row_readable` (range bytes `[0x02,0,0,0,4,0,0,0,1,0,0,0,4,0,0,0,0x0A]`, money = BE cents 1234, composite and `text[]` as bytes, `'ok'::text` and `42` of the same row intact) + live `pg_enum_and_domain_values_decode_to_canonical_values` (enum label, domain over text, domain over integer through its base type, NULL domain). **Fails before / passes after**: same two-test command with the source file stashed → `1 passed; 1 failed` (`QueryFailed("cannot decode PostgreSQL MONEY as text…")`), restored → `2 passed; 0 failed`. Live suite **23 passed / 0 failed**; `cargo test --workspace` **863 passed / 0 failed / 24 ignored** (baseline 862/0/22). Scope boundary recorded, not hidden: array **element parsing** is deliberately not implemented — the fallback is byte-exact and the display-only representation is the A3 contract (#53); this issue's own acceptance explicitly allows "fallback text follows contract". Evidence: docs/release/evidence/v01-runtime/providers/39-pg-unsupported-class-fallback.md |
| #59 | [Gate 5][B5] Add PostgreSQL integration fixture covering full decoder matrix | **CLOSED** 2026-09-15 · `gh issue close 59` | `75865db` | 2026-09-15 | **the fixture now carries one column per value class and one test asserts the whole matrix** — before, `fixtures/postgres/001_schema.sql` had no domain, interval, time/timetz, int2, float4/float8, inet/cidr, range or composite column, the classes it did have were asserted only through inline literals inside tests, the `tags TEXT[]` column was never selected, and `grep -rn 'CREATE DOMAIN' fixtures/` returned nothing. Added on the CI load path (`ci.yml:69-71`): `CREATE DOMAIN positive_quantity` / `postal_code`, `CREATE TYPE decoder_pair`, the `decoder_matrix` table (26 columns: int2/int4/int8, float4/float8, numeric(24,4), date/time/timetz/timestamp/timestamptz, interval, uuid, json/jsonb, bytea, inet/cidr, enum, 2 domains, `TEXT[]`, `INT4RANGE`, composite, nullable), a fully populated seed row (int maxima, 24 significant digits, microseconds, `+07:00` timetz offset) plus a row NULL in every nullable column, and two `003_verify.sql` assertions (exactly 2 rows; populated row intact). New live test `pg_decoder_matrix_covers_every_value_class` asserts the expected-vs-actual matrix column by column — including `Decimal("12345678901234567890.1234")`, `DateTime("2024-03-15T10:20:30.123456")` with no invented offset vs `…Z` for the instant, `Interval("1 mons 2 days 03:04:05.000006")`, domains through their base type (`Int64(7)`, `Text("SW1A 1AA")`), the byte-exact fallback for `TEXT[]`/`INT4RANGE`/composite, and `Null` for all 24 nullable cells of the second row — plus the declared provider type reaching the caller for `INT2`/`NUMERIC`/`TIMETZ`/`TEXT[]`/`INT4RANGE`/`decoder_pair`. Fixture reload re-ran the exact CI steps (`ON_ERROR_STOP`, `001`→`002`→`003`) green; live suite **24 passed / 0 failed** (23 before); `cargo test --workspace` **863 passed / 0 failed / 25 ignored` (baseline 863/0/24). Two measurement-driven adjustments while writing the test (the composite column's declared name is `decoder_pair`, the NULL row's key is `2`) are recorded in the evidence file rather than silently fixed. Array element parsing stays out of v0.1 (A3 contract, #53). Evidence: docs/release/evidence/v01-runtime/providers/40-pg-decoder-matrix-fixture.md |
| #60 | [Gate 5][C1] Align frontend provider-value DTO types with canonical Tauri contract | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — _archive/frontend/src/modules/query/types/query.types.ts:1-14; CHANGELOG.md:46; docs/plans/STATUS.md:44 |
| #61 | [Gate 5][C2] Render/copy/export canonical provider values without precision or timezone loss | **CLOSED** 2026-09-15 · `gh issue close 61` | `205539c2` | 2026-09-15 | **three frontend serialization paths were lossy and are now one shared renderer** — (D1) the query-view export wrote every cell raw (`crates/ui/src/query_view.rs:1363-1383` before), so a value containing the delimiter, a quote or a newline broke the record layout; (D2) copy-as-JSON parsed `Number` through `i64` then `f64` (`crates/ui/src/result_grid_view.rs:1821-1840`), copying `9007199254740993` as `9007199254740992.0`; (D3) the tab-separated clipboard paths joined `cell_text()` with a literal `\t`/`\n`, so an embedded tab or newline added fields/rows and `NULL` copied as the text `NULL`. Now `escape_delimited_field` + `format_cell_delimited` + `format_result_delimited` (`crates/ui/src/result_grid_view.rs`) serve export, copy-row(s), copy-with-headers and copy-as-CSV, `export_result` calls the shared serializer, and `cell_to_json_value` emits a JSON number only when the exact text survives (±2^53 integer, or the shortest `f64` form) — otherwise the exact digits stay a JSON string, matching `export_service.rs:216`. Display (`cell_text`, `result_grid.rs:128`) is untouched and still pinned by `cell_text_keeps_null_and_json_visible`. 3 new UI tests incl. a real file-write test (`cargo test -p db-pro-ui --lib` → 364 passed / 0 failed); `cargo test --workspace` **844 passed / 0 failed / 21 ignored** (baseline 841/0/21 after the #56 fix). Evidence: docs/release/evidence/v01-runtime/providers/31-canonical-value-copy-export.md |
| #62 | [Gate 5][C3] Enforce type-aware editability and safe mutation policy | **CLOSED** 2026-09-15 · `gh issue close 62` | `0af26475` | 2026-09-15 | **one value-class policy now gates every edit entry point** — before, only the binary check existed and it lived in two places; a `GENERATED` column could be staged and was only rejected by the database at apply time, and Duplicate Row prefilled generated values into the insert dialog. New `crates/ui/src/policy.rs` (`ColumnWritePolicy`/`ColumnWriteBlock`): bytea/blob/binary and `is_generated` columns are read-only with user-facing reasons; identity stays writable; every class the parsers can round-trip (serial/integer/numeric(p,s)/float/bool/text/varchar/citext/uuid/timestamp±tz/date/time(tz)/interval/inet/enum/domain/`T[]`/json/unknown-as-text) stays writable and validated. Enforced in `begin_data_cell_edit`, `submit_data_cell_edit`, `submit_insert_row` (empty blocked column skipped; filled one refused deterministically), `open_duplicate_row` (blocked columns left empty), the insert dialog (read-only label instead of input/NULL/Gen), the context menu (muted "Read-only Column" + hover reason, no "Set to NULL") and the paste path. no-PK/readonly gating untouched (`can_edit_table_rows` still requires both; `no_primary_key_table_blocks_safe_row_mutations` green). Tests: 4 policy unit tests + 4 interaction tests; `cargo test -p db-pro-ui --lib` → 372 passed / 0 failed; `cargo test --workspace` **852 passed / 0 failed / 21 ignored** (baseline 844/0/21). Evidence: docs/release/evidence/v01-runtime/providers/32-type-aware-editability-policy.md |
| #63 | [Gate 5][C4] Add frontend regression matrix for provider-value rendering/edit policy | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/plans/STATUS.md:44; crates/ui/src/app_tests.rs:219-263 |
| #64 | [Gate 5][D1] Verify PostgreSQL fixture -> domain decoder -> Tauri DTO end-to-end | **CLOSED** 2026-09-15 · `gh issue close 64` | `483f3da` | 2026-09-15 | **the two halves are now joined in one live test** — before, `pg_decoder_matrix_covers_every_value_class` stopped at the decoder and `query_result_dto_matches_the_checked_in_contract_fixture` started from a hand-built `QueryResult`, so "the fixture row reaches the frontend contract intact" was an inference from two disjoint tests. New `live_fixture_query_survives_the_provider_to_dto_path` (`crates/tauri-app/src/dto.rs`, the only crate that has both the private `QueryResultDto` and the infrastructure connector) runs `SELECT * FROM decoder_matrix ORDER BY id` on the live fixture and asserts on the same result: the #54 payload shape (top-level keys, `rowCount`, numeric `durationMs`, 26 column objects with exactly `dataType/name/nullable`), the **26-cell class matrix** (int2/int4/int8/domain→`int64`, float4/8→`float64`, numeric→`decimal`, date→`date`, time+timetz→`time`, timestamp+timestamptz→`datetime`, interval, uuid, json/jsonb, bytea→`bytes`, inet/cidr, enum+domain→`text`, array/range/composite→`bytes`, NULL→`null`), the loss-sensitive values (`9223372036854775807` and `12345678901234567890.1234` as exact strings, `local_stamp` with no invented zone vs `instant` with `Z`, timetz `+07:00`, bytea `[222,173,190,239]`, NULL as `{"type":"null"}` with no `value`) and the declared provider types `TEXT[]`/`INT4RANGE`/`decoder_pair`/`NUMERIC`/`TIMETZ` still on the column meta. Plus a no-loss sweep: for all 52 cells the domain value's own JSON equals the DTO's JSON and the emitted cell round-trips back to identical JSON. **Green on the first run — the remainder was missing coverage, not a defect** (as with #57); no product code changed. Falsified three ways: wrong password → `AuthFailed` failure; `DATABASE_URL` unset → explicit failure (a live leg cannot silently skip into a pass); one class expectation mutated → `count int4: expected text, emitted int64`, restored. `cargo test --workspace` **872 passed / 0 failed / 27 ignored** (863/0/26 at session start); CI-mirroring `--include-ignored` **899 passed / 0 failed / 0 ignored** (889/0/0 at session start). Evidence: docs/release/evidence/v01-runtime/providers/45-provider-to-dto-end-to-end.md |
| #65 | [Gate 5][D2] Verify Tauri DTO -> frontend render/copy/editability end-to-end | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — crates/runtime/src/worker.rs:21-22; CHANGELOG.md:46 |
| #66 | [Gate 5][D3] Run exact-head Rust/frontend/provider verification for type matrix | **CLOSED** 2026-09-15 · `gh issue close 66` | `06e137e` | 2026-09-15 | **the exact-head verification record now exists** — before, no Gate-5-specific type-matrix verification record existed (the closest were the RC1 candidate gate file `08-post-fix-quality-gates.txt` and the older 18-case live run `07-pg-integration-live.txt`). Recorded on **`main @ d24eb6a`** in `docs/release/evidence/v01-runtime/providers/43-gate5-exact-head-verification.md`: all six gates green on one SHA (`cargo fmt --all -- --check` exit 0, `cargo check --workspace` exit 0, `cargo clippy --workspace --all-targets -- -D warnings` exit 0, `cargo test --workspace` **863 passed / 0 failed / 26 ignored**, `cargo build --release --locked -p db-pro-native` exit 0, `perf-scan.sh` `PASS` 4/0/0), the live PostgreSQL matrix **25 passed / 0 failed**, and the **CI-mirroring** invocation `DATABASE_URL=… cargo test --all -- --include-ignored` → **889 passed / 0 failed / 0 ignored**. The frontend gate block is recorded as **void, not skipped**: the React frontend was retired 2026-09-11 and `ci.yml` states there is no Node/TS gate left; the shipping consumer's mapping and DTO boundary are pinned by the native tests and the whole-result fixture. P0/P1: no Gate 5 source-review finding is open at that SHA (the register's P1 rows are `R-LICENSE`/`R-WINLINUX`/`R-GUI-SMOKE`/`F1` — distribution, host and GUI, not the type matrix); a **reviewer disposition is explicitly not claimed** (no reviewer in this environment). CI: the head's run 34902131743 was queued at record time; the preceding run 34900407192 (`46b2bb5`) is the introspection race fixed in `0d3aa84` (#237). Evidence: docs/release/evidence/v01-runtime/providers/43-gate5-exact-head-verification.md |
| #67 | [Gate 5][D4] Merge verified Gate 5 PR and prove main contains the exact accepted type matrix | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: crates/infrastructure/src/postgres/query_mapper.rs (on main); docs/plans/STATUS.md:42 (P1 matrix status is a requirement list, not evidence) |
| #68 | [RC1][Introspection A1] Decide CHECK constraint v0.1 disposition from current source and consumer value | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:175-186 (LIM-011 'Decision pending (#68)'); docs/release/provider-capability-matrix.md:65 ('Disposition pending (#68)') |
| #69 | [RC1][Introspection A2-KEEP] Harden SQLite CHECK parser semantics and focused tests | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: crates/infrastructure/src/sqlite/introspect.rs:355-437 (parser), :455-473 (skip_quoted escapes), :607-637 (tests) |
| #70 | [RC1][Introspection A3-KEEP] Complete CHECK provider -> domain -> Tauri -> frontend exposure | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: crates/infrastructure/src/postgres/introspect.rs:635; crates/core/src/domain/schema.rs:145; crates/native-app/src/translate.rs:804; crates/ui/src/table_metadata_view.rs:641,736 |
| #71 | [RC1][Introspection A2-DEFER] Remove/deactivate partial CHECK exposure cleanly for v0.1 | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — crates/ui/src/table_metadata_view.rs:641,736; docs/release/known-limitations.md:175-186 |
| #72 | [RC1][Introspection B1] Add explicit introspection IPC serialization contract tests | ~~`ACTIONABLE_NOW`~~ **CLOSED** 2026-09-15 · `gh issue close 72` | `a059506d` | 2026-09-15 | **row repaired in pass 6** — this record had been two rows concatenated into one line (the triage row with status `ACTIONABLE_NOW` and updated `2026-09-14`, and its closing row), so the status was ambiguous and the open-row count could not be reconciled against the live repository. The closing content is kept verbatim; nothing was deleted. **the introspection IPC shape is now pinned by four contract tests and a checked-in fixture** — before, no test serialized `IntrospectResultDto` at all, so the old singular `fromColumn`/`toColumn` drift, a rename or a dropped field could have shipped silently. `crates/tauri-app/src/dto.rs` tests: exact sorted 8-key top-level set + camelCase leaves + `null` optionals; single/composite FK column order in both directions with plural names asserted absent-or-present; a drift payload using singular names is rejected; and `serde_json::to_string_pretty` of the DTO is byte-identical to `crates/tauri-app/tests/fixtures/introspect-contract.json` (generated from the real structs, not hand-written). CHECK payloads stay out until #68 decides KEEP. `cargo test -p db-pro-tauri --lib` → 25 passed / 0 failed (22 before); `cargo test --workspace` **856 passed / 0 failed / 21 ignored** (baseline 852/0/21). Evidence: docs/release/evidence/v01-runtime/providers/33-introspection-ipc-contract-tests.md |
| #73 | [RC1][Introspection C1] Reconcile audit/docs and run exact-head introspection regression verification | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/introspection-audit-2026-08-13.md:1-45; docs/plans/STATUS.md:18-27 |
| #75 | [RC1][P2-A] Audit Workspace/Shell/navigation findings on post-P1 baseline | **CLOSED** 2026-09-15 · `gh issue close 75` | `84f21c0` | 2026-09-15 | **all eight live Workspace/Shell rows now carry exactly one release disposition**, decided from the current tree rather than from the React-era report: `QA-P2-01` pinned-tab order **`Defer post-v0.1`** (no pinned-tab concept exists natively — `grep -rn pinned crates/ui/src/workspace_view.rs` is empty; workspace persistence is LIM-016), `QA-P2-02` modifier labels **`Accept RC1`** (`primary_modifier_label()` `crates/ui/src/app.rs:637`, 12 call sites), `QA-P2-03` traffic-light topbar **`Accept RC1`** (no topbar/titlebar code exists — grep returns nothing), `QA-P2-04` agent Preview badge **`Accept RC1`** (`agent_view.rs:224`, with its `"Offline draft"` condition recorded rather than glossed), `QA-P2-05` agent shortcut hint **`Accept RC1`** (no hint exists; only the API-key placeholder at `:163`), `QA-P2-13` sidebar search scale **`Accept RC1`** (filter over loaded nodes `explorer_view.rs:246`, palette over a static list `palette_view.rs:179`), `QA-P2-14` explorer row mounting **`Accept RC1` with the residual risk stated** (plain `ScrollArea` at `explorer_view.rs:209`, 250-table fixture as the v0.1 scale target, **no measurement exists**), `QA-P2-23` `window.confirm` **`Accept RC1`** (`NOT_APPLICABLE_IN_NATIVE`). Zero `Fix RC1`, so no child issue was spawned; the issue's other scope bullets have no live P2 row, recorded as such. Code untouched. Evidence: docs/release/rc1-p2-release-dispositions.md §A |
| #76 | [RC1][P2-B] Audit Data Grid/read-update-delete findings on post-P1 baseline | **CLOSED** 2026-09-15 · `gh issue close 76` | `744d91f` | 2026-09-15 | **all six live Data Grid rows carry exactly one disposition** (docs/release/rc1-p2-release-dispositions.md §B): `QA-P2-07` columns picker **`Accept RC1`** (set-based hide + show-all at `result_grid_view.rs:212,216`; the toggle semantics are *not* pinned by a test — stated), `QA-P2-08` read-only affordances **`Accept RC1`, positively verified** (editability = writable connection **and** PK, `table_editor_view.rs:1494`, `result_grid_view.rs:73`, plus #62's policy tests and `no_primary_key_table_blocks_safe_row_mutations` — the safety-adjacent carry-over #74 flagged), `QA-P2-09` off-screen menus **`Accept RC1`** (egui popup layer `components/overlay.rs:787,825`), `QA-P2-10` mouse-only resize **`Defer post-v0.1`**, `QA-P2-11` drag cleanup **`Accept RC1`** (no unmount lifecycle in immediate mode), `QA-P2-12` synchronous sorting **`Fix RC1` → child issue #238** — **measured**: 200k rows × 4 cols → 5.35 ms unsorted vs **3,414 ms** sorted on a text column, recomputed every frame (`result_grid.rs:97` called from `result_grid_view.rs:76`), reachable to `MAX_MAX_ROWS = 100_000` (`connection.rs:111,116`). Evidence: docs/release/rc1-p2-release-dispositions.md §B |
| #77 | [RC1][P2-C] Audit connection/session/SSH-related findings on post-P1 baseline | **CLOSED** 2026-09-15 · `gh issue close 77` | `e764f9b` | 2026-09-15 | **all seven live connection/session rows carry one disposition, and the SSH scope item is dispositioned too** (docs/release/rc1-p2-release-dispositions.md §C): `QA-P2-15` stale test result **`Accept RC1`, verified** (tested draft remembered, `events.rs:481-485`, cleared on every edit), `QA-P2-16` hidden backend detail **`Accept RC1`, verified** (`events.rs:956` stores the runtime message, rendered verbatim at `connection_view.rs:497-501`), `QA-P2-17` SQLite Browse error path **`Accept RC1`, verified for the cancelled branch** (`events.rs:425-441`), `QA-P2-18` `driverChanged` **`Accept RC1`** (no change-tracked flag exists), `QA-P2-19` duplicate omits credentials **`Accept RC1`** (secrets stay in the keyring; the empty field is visible — with the missing “password not copied” hint recorded as polish, not asserted ideal), `QA-P2-20` favourites **`Defer post-v0.1`** (no favourites feature exists), `QA-P2-21` SQLite recent subtitle **`Defer post-v0.1`** (no recent-connections surface). Scope item **SSH control with no in-UI caveat → `Fix RC1`, child issue #239** (the issue's own acceptance forbids implying qualified SSH support, and LIM-006 is invisible in the app). Evidence: docs/release/rc1-p2-release-dispositions.md §C |
| #78 | [RC1][P2-D] Audit Query/ER residual findings after Gate 4/5 | **CLOSED** 2026-09-15 · `gh issue close 78` | `522f173` | 2026-09-15 | **all four Query/ER rows have one disposition and none duplicates Gate 4/5 closure work** (docs/release/rc1-p2-release-dispositions.md §D): `QA-P2-22` export enablement **`Accept RC1`, verified** (the Export affordance only exists inside `if let Some(value) = result`, `query_view.rs:412-421`), `QA-P2-23` `window.confirm` **`Accept RC1`** (cross-referenced to §A, not double-counted; `NOT_APPLICABLE_IN_NATIVE`), `QA-P2-24` ER search auto-pick **`Accept RC1`** (search is a view mode over *all* matches, `diagram_view.rs:168,172,296`, with the explicit show-all escape pinned; the untested match *ordering* is recorded, not promoted), `QA-P2-25` ER derived state **`Accept RC1`** (graph structures built once at `diagram/model.rs:85-86`, search reuses the graph per `diagram/tests.rs:1629`; the React-Flow sub-items are `NOT_APPLICABLE_IN_NATIVE`). No `Fix RC1`; the scope bullets without a live P2 row (statement selection, cancellation, Explain metadata, rename atomicity, history, error states, minimap, persisted positions, perf HUD) are recorded as such. Evidence: docs/release/rc1-p2-release-dispositions.md §D |
| #79 | [RC1][P2-E] Audit performance tooling, bundle evidence, and release-measurement semantics | **CLOSED** 2026-09-15 · `gh issue close 79` | `9e8c0f6` | 2026-09-15 | **the audit is complete and three of its findings were defects in the evidence tool itself** — `docs/release/rc1-p2-release-dispositions.md` §E carries one disposition per scope bullet (no `QA-P2-*` row exists for perf/tooling, stated rather than papered over): stale `dist` heuristics **`Accept RC1`**, positively verified (the scan builds the release binary in the same run and reports *no size at all* when that build fails, so a stale binary was never measurable; the PR #13 wording described the React-era script); bundle/dependency drift **`Accept RC1`** (the only such numbers live in two `HISTORICAL (2026-09-11)` reports and `grep` over `docs/release/*` + `README.md` finds no bundle-size claim); loading fallback/i18n tied to perf paths **`Defer post-v0.1`** (the React Suspense fallbacks belong to the retired frontend) cross-referenced to §F (#80) so `QA-P2-06` is not double-counted; PR #13 follow-ups **`Accept RC1`** (its bundle metrics are void, its two lasting items are the two below). Three `Fix RC1` rows → child **#240**, fixed in place: `all` printed `Status: PASS — All checks passed` while the two benchmark sections and the ER/DB runtime sections were **not run** (measured raw before/after in the evidence file); `er` printed `WARN` and **exited 0** (measured: old exit 0 vs new exit 2), so a status-only gate certified a warned run as green; and no run named its source revision or artifact (SKILL.md §2/§4 + `references/perf-budgets.md` also budgeted five UI operations that **neither bench file registers** and a `strip` packaging step no workflow performs). Fixed: skipped sections counted and named (`PASS (partial) — … not run: …`), exit contract `PASS` 0 / `WARN` 2 / `FAIL` 1, provenance `Source revision: <sha>[+dirty(N)]` + `Measured artifact: … sha256 <digest> (<size>MB)`, budget tables corrected to the registered criterion ids. `bash .skills/perf-audit/scripts/perf-scan.sh --self-test` → **14 assertions, SELF-TEST PASS**, falsified with three probes (1, 2 then 1 failures). Gates: fmt/check/clippy exit 0, `cargo test --workspace` **875 passed / 0 failed / 27 ignored** (delta 0), release build exit 0, perf-scan `PASS (partial)` 4/0/0 + 4 sections not executed. No Rust source touched. Evidence: docs/release/evidence/v01-runtime/providers/47-perf-tooling-and-measurement-semantics.md |
| #80 | [RC1][P2-F] Audit cross-cutting i18n/accessibility/error-state release polish | **CLOSED** 2026-09-15 · `gh issue close 80` | `ebf085b` | 2026-09-15 | **the audit is complete and six of its findings were defects, fixed in place** — `docs/release/rc1-p2-release-dispositions.md` §F carries one disposition per scope bullet; the single live cross-cutting P2 row `QA-P2-06` is settled on the mechanism (there is **no** localization layer to bypass: `grep -rniE 'i18n|fluent|gettext|translat|locale|language'` returns only `small_translate`, the `translate.rs` IPC marshalling and `CodeBlock::language("sql")`; no locale files; Settings has no language control — `navigation_view.rs:940-987`), and the §A/§B/§C rows that touch these bullets are cross-referenced, not re-counted. Six `Fix RC1` rows → child **#241**, fixed: (1) the status bar showed `runtime_message` only when the text contained failed/error (`navigation_view.rs:233` + `app.rs:723-728`) so ~165 assignment sites of refusals and gates were invisible — now any non-empty message is shown with danger reserved for errors; (2) **`Apply DDL` did nothing** while its caption claimed "execution is confirmation-gated" (`ddl_execute_confirmation` never set true, `submit_ddl`'s only non-test caller unreachable) — now disabled with the reason and the documented path; (3) `Button::accessible_name()` fell back to the literal `"Button"` for icon-only controls (`components/button.rs:155`) — now falls back to the tooltip; (4) `is_context_menu_triggered` required hover, so `Shift+F10` was dead for a focused widget (`overlay.rs:791`); (5) five `Cmd/Ctrl+…` tooltips + a hardcoded `⌘I` claim for an unbound key; (6) eight hardcoded RGB literals duplicating the **opposite** theme's tokens. Deferred with sites named: modal focus lifecycle (`dialog.rs` has zero `focus` matches), ~30 glyph-only controls, three hand-painted widget families unreachable by Tab, and the light-theme caption/warning contrast (measured 3.45:1 / 3.19:1 against 4.5:1 AA). Gates: fmt/check/clippy exit 0, `cargo test --workspace` **878 passed / 0 failed / 27 ignored** (+3), release build exit 0, perf scan `PASS (partial)` 4/0/0. Evidence: docs/release/evidence/v01-runtime/providers/48-cross-cutting-polish-audit.md |
| #81 | [RC1][P2-G] Synthesize Fix/Accept/Defer table and spawn only required RC1 fix tickets | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/evidence/v01-06/06-rc1-p2-dispositions.md:30-50 |
| #82 | [RC1][P2-H] Verify completed P2 dispositions on exact post-remediation main SHA | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/plans/STATUS.md:45-52 ('the P2 gate is formally unsatisfied') |
| #84 | [RC1][Freeze B1] Run complete frontend release gates on candidate SHA | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — .github/workflows/ci.yml:13-15; .github/workflows/release.yml:163-175; docs/plans/STATUS.md:44 |
| #88 | [RC1][Freeze C2] Generate candidate evidence manifest and artifact checksums | **CLOSED** 2026-09-15 · `gh issue close 88` | `326c313` | 2026-09-15 | **the single in-repo manifest now exists and its invariants are a gate** — `grep -rn 'evidence-manifest\|evidence_manifest'` over the tree returned no matches before this change. `docs/release/evidence-manifest.json` collects: the candidate `85a7fa3` + pinned toolchain, collection provenance, the final run `34860902181` with all **nine job ids re-read from the GitHub API** (not from prose), every gate with the SHA it was observed at, the three archives + `SHA256SUMS.txt` + the three build artifacts + the provenance record with GitHub artifact ids and checksums, signing truth status (`not_configured`), the P2 disposition counts (25 confirmed / 24 OBSOLETE / 20 carried-over / 1 satisfied / 3 not-applicable), the 18-entry limitation registry and the #29/#31 handoff, with superseded run `34859158012` kept as history. Values were cross-checked across five documents and re-read from the API. New `crates/native-app/tests/evidence_manifest_contract.rs` — **9 contract tests** enforcing: closed status vocabulary where `pass` requires evidence and every other status requires a reason (a skipped gate can never read as a pass), all artifacts/provenance at the candidate SHA with gate observations limited to the candidate or a recorded descendant, checksums as 64-hex-or-unavailable-with-reason, no superseded digest posing as a candidate one, `public_release_ready` ⇒ zero blockers + every gate `pass` + signing configured, and every cited evidence path resolving to a non-empty file. **Falsification run first**: three probes (deleted reason; a `not_applicable` gate relabelled `pass` with a dangling pointer; `public_release_ready=true` with a foreign artifact SHA and a `deadbeef` gate observation) each turned the suite red (1, then 2, then 3 failures), restored manifest green. `cargo test --workspace` **872 passed / 0 failed / 26 ignored** (baseline 863/0/26, **+9**); fmt/check/clippy/release-build/perf-scan (`PASS`, 4/0/0) all exit 0; **CI-mirroring** `DATABASE_URL=… cargo test --all -- --include-ignored` → **898 passed / 0 failed / 0 ignored** (baseline 889/0/0). Deliberate limits stated, not hidden: the manifest does not declare `FINAL_RC_SHA` (that is #89, still open), `checksum_manifest.sha256` is `null` with a reason because the run never hashes `SHA256SUMS.txt` itself, and the two intermediate binaries without a recorded digest carry an unavailability reason instead of a guessed value. Evidence: docs/release/evidence/v01-runtime/providers/44-candidate-evidence-manifest.md |
| #89 | [RC1][Freeze D1] Final latest-head review and declare FINAL_RC_SHA | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: grep -rn 'FINAL_RC_SHA' docs/ README.md CHANGELOG.md -> no matches |
| #91 | [RC1][Smoke B1] Run packaged macOS runtime smoke on FINAL_RC_SHA artifact | `BLOCKED_EXTERNAL` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_EXTERNAL`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/evidence/v01-06/14-install-smoke.txt §7.7; docs/release/0.1.0-manual-smoke.md (0 passed / 165 blocked) |
| #92 | [RC1][Smoke B2] Run packaged Windows runtime smoke on FINAL_RC_SHA artifact | `NEEDS_EXTERNAL_RESOURCE` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/0.1.0-human-decisions.md (HD-003); docs/release/risk-register.md (R-WINLINUX) |
| #93 | [RC1][Smoke B3] Run packaged Linux runtime smoke on FINAL_RC_SHA artifact | `NEEDS_EXTERNAL_RESOURCE` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:273-287 (LIM-018); docs/release/0.1.0-packaging.md:86-101 |
| #94 | [RC1][Smoke C1] Triage packaged-runtime failures and enforce re-freeze loop | `BLOCKED_EXTERNAL` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_EXTERNAL`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/evidence/v01-runtime/providers/23-findings-and-dispositions.md; docs/release/evidence/v01-06/12-state-dir-blocker-fix.txt |
| #95 | [RC1][Smoke D1] Reconcile platform results and sign off packaged runtime | `NEEDS_EXTERNAL_RESOURCE` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/0.1.0-final-report.md; docs/release/0.1.0-readiness.md:241-252 |
| #96 | [RC1][Brand A1] Competitive landscape and parity evidence workstream | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/plans/STATUS.md; docs/notes/PRODUCT_CAPABILITY_MATRIX.md:1-6 |
| #97 | [RC1][Brand A2] Define product wedge and v0.1 vs long-term positioning statement | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:35-47 (LIM-001); docs/release/0.1.0-human-decisions.md (HD-001..) |
| #98 | [RC1][Brand B1] Define naming criteria and rejection rules from product positioning | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:42 ('Name decision pending (#101, #104)') |
| #99 | [RC1][Brand B2] Generate and score replacement-name shortlist | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/0.1.0-human-decisions.md; docs/release/risk-register.md (R-BRAND) |
| #100 | [RC1][Brand B3] Run live collision/domain/product diligence on naming shortlist | `NEEDS_EXTERNAL_RESOURCE` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body 'use current live sources at execution time' |
| #101 | [RC1][Brand B4] Select and lock replacement public identity | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:35-47 (LIM-001); docs/release/0.1.0-human-decisions.md |
| #102 | [RC1][Brand C1] Inventory every public-facing rename surface before code changes | **CLOSED** 2026-09-15 · `gh issue close 102` | `f5d94ff1` | 2026-09-15 | **inventory produced as `docs/release/brand-rename-inventory.md`** (baseline `main @ 6c698bc`) — §1 records the five repository-wide search patterns with raw counts (`DB Pro` 399, `db-pro` 992, `db_pro` 638, `dbpro` 431, `DbPro` 647); §2 classifies every public surface with file:line (Tauri product/window names + bundle id `com.dbpro.app`, native window title `crates/native-app/src/main.rs:315,322`, in-app labels `navigation_view.rs:478` / `component_gallery_view.rs:227` / `palette_view.rs:143`, README, GitHub repo name/description, release archive names, crate/type names, archived npm name, absent surfaces: icons/telemetry/domain/updater verified by grep); §5 hands #103 the implementation checklist (no blind replace, order of work). The selected identity itself is the owner decision (#101/#30) and is deliberately left PENDING here. Evidence: docs/release/brand-rename-inventory.md |
| #103 | [RC1][Brand C2] Apply selected public identity in one controlled rename PR | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:35-47 |
| #104 | [RC1][Brand C3] Verify rename completeness, compatibility, and release-facing metadata | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body dependencies |
| #105 | [v0.1.0][Release A1] Draft release notes from verified shipped scope and limitations | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-release-notes.md:1-16,75-145; docs/release/known-limitations.md:189-201 |
| #106 | [v0.1.0][Release A2] Verify version, identity, package metadata, and artifact naming before tag | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-readiness.md:11-13; CHANGELOG.md:5-13 |
| #107 | [v0.1.0][Release B1] Final pre-tag consistency check against FINAL_RC_SHA | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/0.1.0-readiness.md:25-39,241-252 |
| #108 | [v0.1.0][Release B2] Create v0.1.0 tag and GitHub Release on accepted SHA | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/0.1.0-human-decisions.md (HD-001, HD-008); docs/release/0.1.0-readiness.md:253-270 |
| #109 | [v0.1.0][Release B3] Attach and validate release assets against evidence manifest | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body; `git tag -l` empty |
| #110 | [v0.1.0][Release C1] Update README/release docs to match published v0.1.0 reality | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: README.md:10-12,37-85,206-251 |
| #111 | [v0.1.0][Release C2] Post-publish verification and close release Goal | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/0.1.0-readiness.md:25-39 |
| #112 | [META][Project] Configure GitHub Projects control plane for v0.1 execution | `NEEDS_EXTERNAL_RESOURCE` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body 'Connector limitation' |
| #113 | [META][Agents] Agent dispatcher, claim queue, and concurrency rules for v0.1 | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/plans/STATUS.md:1-20; docs/plans/FEATURE_LIFECYCLE.md; AGENTS.md:28-42,223-241 |
| #114 | [META][Agents] Standardize evidence, progress, and review log contract | **CLOSED** 2026-09-15 · `gh issue close 114` | `81f5bc90` | 2026-09-15 | **the canonical contract now exists and is wired into the agent guide** — `docs/plans/_template/AGENT_EVIDENCE.md` (beside PLAN/CHECKLIST/FINDINGS/VERIFICATION templates) holds §1 Claim / §2 Progress checkpoint / §3 Implementation handoff / §4 Review outcome / §5 Research-audit handoff / §6 Tổng kết, with the non-negotiable rules (exact SHA mandatory, skipped/failed gates reported as such, review separates inherited failures from regressions, normalized Todo/Ready/In Progress/Review/Blocked/Done/Deferred and Draft/Ready/Merged) and two filled examples (the real #56 coding fix, the real #121 audit). `AGENTS.md` gained an "Agent evidence contract" section after "PR workflow" pointing at the template; #113 got a location comment. Evidence: docs/release/evidence/v01-runtime/providers/34-agent-evidence-contract.md |
| #115 | [Gate 4][Support] Build reusable 201/500/1000/dense-hub ER fixture factory | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — crates/ui/src/diagram/tests.rs:71-113 (isolated/star/chain/dense_1000_tables), :682-704 (timings) |
| #116 | [Gate 4][Support] Build renderer/layout instrumentation harness for exact invariant tests | `SUPERSEDED` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — crates/ui/src/diagram/tests.rs:166-260 (worker tests), :333-360 (graph/spatial invariants) |
| #119 | [RC1][Prep] Decide license, redistribution, and public release policy | `NEEDS_OWNER_DECISION` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/risk-register.md:56-66 (R-LICENSE BLOCKING), :339; docs/release/0.1.0-human-decisions.md (HD-001) |
| #120 | [RC1][Brand Prep] Inventory source-backed product differentiators and parity evidence | **CLOSED** 2026-09-15 · `gh issue close 120` | `391091b` | 2026-09-15 | **audit complete** — deliverable `docs/release/brand-differentiators-inventory.md` (baseline `11854b3`): §1 classifies every capability the issue lists (plus the ones it implies) into exactly its five status classes with source/test/doc evidence, a value line and a parity-vs-wedge call; §2 is the `can say / cannot say yet` table for #97 plus the explicit prohibited-overclaim list; §3 the acceptance check. Four wedge rows are source-backed: **local-first with zero telemetry** (`audit-security-boundaries.md` §1: telemetry/update/licence/remote-asset greps → 0, only two user-key-gated AI endpoints), **type-aware lossless values** (`provider-value-contract.md` + `providers/37`–`46` + the live fixture join), **staged and gated writes** (staged apply as one transaction, no-PK/read-only enforced in UI and core, the new destructive hold), and **large-schema ER boundedness** (500/1000-table timing tests in `diagram/tests.rs`). Disclosed rather than marketed: agent = `PREVIEW/EXPERIMENTAL` (LIM-007), MCP/import/JSON-XLSX export/schema-and-role administration/Windows-Linux = `BACKLOG` (LIM-008/LIM-012 + #92/#93), SSH = unqualified (LIM-006), workspace persistence absent (LIM-016). Commodity-parity rows are labelled so positioning cannot mistake table stakes for advantage. Documentation-only change |
| #121 | [RC1][Brand Prep] Inventory persisted storage/config identifiers for rename compatibility | **CLOSED** 2026-09-15 · `gh issue close 121` | `f5d94ff1` | 2026-09-15 | **inventory produced in the same `docs/release/brand-rename-inventory.md` §3** — every persisted identifier classified with file:line: keyring service `com.dbpro.app` (`crates/runtime/src/lib.rs:62`, `crates/native-app/src/main.rs:198`) → PERSISTED COMPATIBILITY; app-data dirs `DB Pro` / `db-pro` (`main.rs:130,187-194`) and legacy `.db-pro-data` (`:128`) → MIGRATE WITH FALLBACK / preserve legacy lookup; env vars `DB_PRO_DATA_DIR`, `DB_PRO_ALLOW_FILE_SECRET_FALLBACK`, `DB_PRO_CODEX_*`/`DB_PRO_GROQ_*` → preserve legacy; 11 `dbpro.native.*` eframe storage keys (`crates/ui/src/app.rs:445-471`) → preserve legacy key (renaming the eframe app id orphans layouts/history); `meta.db`, `secrets/` → brand-neutral; `.db-pro-*.tmp`, RUST_LOG filter, test dirs → keep. Migration rules recorded: read-old/write-new, one-time marker, legacy dir untouched for one release, rollback, cleanup grace, tests needed. Evidence: docs/release/brand-rename-inventory.md |
| #122 | [RC1][Trust] Audit local-first security boundaries, credential handling, and unsafe execution paths | **CLOSED** 2026-09-15 · `gh issue close 122` | `6446d94` | 2026-09-15 | **audit complete; no P0 and no P1 found.** Deliverable `docs/release/audit-security-boundaries.md` (audited baseline `b49d667`): §1 the complete network/data-flow inventory — the app's **only** egress is two AI endpoints (`crates/runtime/src/agent.rs:13,15`, HTTPS-only guard at `:153`), verified negatives for telemetry/update-check/licence/remote-assets and no socket on the startup path; §2 the credential matrix (secrets never in `meta.db`, summaries, logs, errors or clipboard — `ConnectionConfig` has no password field, SSH password `skip_serializing` **and** nulled before save, summaries drop `secret_ref`, no connection-URL constructor exists so no error can embed one; the encrypted fallback is gated and DEV-ONLY keyed by service name); §3 execution boundaries (classification, the enforcement points that are independent of the UI, staged grid mutations with an explicit Apply, six gated UI entry points plus backend re-checks, the agent approval gate bound to run id + document version, and DDL unreachable from the table editor since `DDL_APPLY_ENABLED = false`); §4 host boundary (four external programs, argv-only, no `sh -c`, secrets via child env; no path canonicalisation/symlink handling; the legacy `tauri-app` is not in the shipping graph; **no `unsafe` blocks anywhere**). Findings: **T-1 P2** → child **#242** (the AI features are the app's only egress and nothing in the product says so — `PredictionMode::Eager` by default, key silently seeded from the OS keyring at startup, agent tool results carry up to 20×12 sample rows, and a `grep` for any privacy/egress wording returns 0 hits); **T-2/T-3/T-4 P3** `Accept RC1` (dead redaction helpers with zero call sites; SSH/pg_dump argv-injection residual + path handling; the legacy crate's capability surface). T-5 cross-referenced to #129, T-6 to #126 — not re-counted. Documentation-only change (the task's own rule is "source/config audit only"), so no product code was touched |
| #126 | [RC1][Trust] Audit keyring and encrypted-fallback secret lifecycle | `BLOCKED_EXTERNAL` (2026-09-15) | — | 2026-09-15 | **audit complete, issue left open on its own closure gate.** Deliverable `docs/release/audit-keyring-secret-lifecycle.md` (baseline `6446d94`): §1 what is on in which build (`build_secret_store` `runtime/src/lib.rs:99-110`; keyring always attempted, session map always on, encrypted file only in debug or with `DB_PRO_ALLOW_FILE_SECRET_FALLBACK`) and the per-OS backends the shipped features link (macOS Keychain, Windows Credential Manager, Linux `linux-native-sync-persistent` = keyutils + Secret Service, i.e. session-scoped without a running Secret Service); §2 the **13-row lifecycle matrix** `event → metadata → keyring → fallback → retrieval → failure` covering create (Postgres and SQLite), edit (blank, supplied, driver change), delete, connect/reconnect, restart, keyring-unavailable, corrupt fallback, legacy `secret_ref`-less rows and the agent key; §3 findings; §4 explicit negatives (SQLite has no password concept, no rename path, no startup orphan sweep, no rotation); §5 the test inventory. Findings: **K-1 P2** store/delete failures are swallowed (`keyring_vault.rs:179-181,230-244`) so a save that never reached the OS store reads as success and only fails after a restart; **K-2 P2** orphans are possible and invisible (no enumeration on the port, row-missing delete deletes nothing, `connection_service.rs:277-286`); **K-3 P2** the agent API key uses a raw `keyring::Entry` outside the vault and **cannot be deleted** from the app (`native-app/src/main.rs:205-234`); **K-4 P2** Linux persistence semantics are weaker than the storage name suggests → `Accept RC1` (Linux is not release-qualified, #93); **K-5/K-6/K-7 P3** test-coverage gaps (incl. an accept-either assertion that cannot fail) and the dead legacy key format. K-1/K-2/K-3/K-5 → child **#243**. Positively verified: per-UUID key isolation cannot collide, the SSH password is skipped in serde **and** nulled before save, create is ordered secret-then-row with rollback both ways, the file fallback is unreachable in a release build that did not opt in, `Debug` impls redact. Also delivered: the packaged-runtime credential scenarios the issue's acceptance asks for — **Step 19** in `docs/release/0.1.0-interactive-verification-runbook.md` (+ its coverage-map row). **Blocker (external):** the issue's review gate requires the credential store proved on each platform in a packaged build — no Windows/Linux host (#92/#93, `R-WINLINUX`) and no interactive macOS session (#91, `R-GUI-SMOKE`, with `R-KEYRING-STALL` still OPEN/ACCEPTED) exist here; owner decision named: whether a persistent Linux backend is required |
| #127 | [RC1][Release Prep] Audit app versioning, updater configuration, and persisted-state compatibility across upgrades | **CLOSED** 2026-09-15 · `gh issue close 127` | `39fe37e2` | 2026-09-15 | **decision record produced as `docs/release/0.1.0-versioning-updater-persistence.md`** — version matrix: `crates/native-app/Cargo.toml` is the release source of truth (parsed by `release.yml:136`, tag cross-check `:144-145`); the other five crate manifests + two archived manifests are manually duplicated `0.1.0`. Updater: no config/plugin/endpoint/key exists anywhere in the live tree — the no-auto-update decision is already in `risk-register.md:110`, LIM-017, `release-notes.md:143`; nothing dormant is presented as working. Persistence: `meta.db` schema_version LATEST=2 with idempotent append-only migrations and explicit downgrade refusal (`migration.rs:61-64`); eframe `dbpro.native.*` keys are NOT persisted in v0.1 (LIM-016 — `NativeOptions` has no persistence); data-dir precedence reuses legacy `.db-pro-data` and is tested (`main.rs:350-392`). Decisions recorded: D1 manual updates + D4 version source-of-truth from existing tree decisions; D2 persisted-state policy proposed with code rows; D3 legacy state and D5 bundle-id stability recorded with options and left PENDING owner decision. `docs/release/brand-rename-inventory.md` K12 corrected in the same change (eframe persistence off in v0.1). Evidence: docs/release/evidence/v01-runtime/providers/35-versioning-updater-persistence-audit.md |
| #128 | [RC1][Data Safety] Audit export/import/backup integrity, cancellation, and partial-failure semantics | **CLOSED** 2026-09-15 · `gh issue close 128` | `458ec72` | 2026-09-15 | **audit complete; every operation classified and three real gaps filed.** Deliverable `docs/release/audit-data-integrity.md` (baseline `a0cdebb`) with the per-operation classification the issue asks for: **release-qualified** — SQLite backup (`VACUUM INTO` + fsync + no-overwrite `hard_link` publish + sidecar removal, 8 tests from #145) and SQLite restore (staged copy → `quick_check` → atomic rename + introspection-cache drop), plus the clipboard copy paths (#61); **implemented but not qualified** — CSV/TSV export from the query view (`query_view.rs:1297,1356-1369`) and PostgreSQL backup/restore (`pg_dump.rs:85-170`, tools not bundled, LIM-015); **deferred/not supported** — JSON/XLSX export (the core `ExportService` owns the `csv`/`rust_xlsxwriter` crates but has **no shipped surface**: `grep export_api()` over native/ui/runtime → 0 hits, no `xlsx` reference in `crates/ui`) and import (LIM-012, no code path). Findings: **E-1 P2** export is not atomic — one `std::fs::write` to the destination, so a mid-write failure leaves a **truncated file that looks complete**, with no overwrite check, progress or cancellation; **E-2 P2** PostgreSQL restore runs `psql -f`/`pg_restore` with no transaction boundary, so a mid-script failure leaves the database **partially restored** and the app says nothing about the resulting state; **E-3 P2** `RuntimeCommand::CancelOperation` is declared (`worker.rs:205`) and handled (`:1563`) but **constructed nowhere**, so a running backup cannot be stopped; **E-4 P2 fixed in this audit** — the release-facing `provider-capability-matrix.md:109-117` claimed XLSX export and a `csv`-crate CSV export for both providers, neither reachable in the shipped app, and now carries the corrected rows plus the partial-failure note; E-5/E-6/E-7 P3 accepted (no progress reporting; external tools unbundled; import vacuous). Partial-failure table (§3) and the test inventory (§4) included; positively verified: staged grid mutations are one transaction. Rule from the issue body was inspection-only, so the three code gaps are child **#244**. Documentation-only change |
| #129 | [RC1][Data Safety] Audit destructive SQL, multi-statement execution, cancellation, and confirmation boundaries | **CLOSED** 2026-09-15 · `gh issue close 129` | `49e87fe`, `458ec72` | 2026-09-15 | **the audit is complete and the confirmation gap it found is fixed** — deliverable `docs/release/audit-execution-safety.md`: the required 12-row matrix (`statement/input → classifier → execution API → transaction semantics → cancellable → confirmation → partial failure → UI result`) plus the parser/classifier test inventory. **The query editor had no destructive-statement confirmation**: `Run`/`Run All` sent `DROP`/`TRUNCATE`/`DELETE`-without-`WHERE` straight to the backend, while the human runbook's Step 13 expects a prompt before anything reaches the backend and the design system's `DestructiveOperationDialog` sat unused. Fixed in `crates/ui/src/events.rs`: `hold_destructive_run` classifies with `classify_script_safety` and holds only `Destructive` (the held text **and** buffer version are dispatched by `confirm_pending_destructive_run`, so a confirm can never run something the user did not see), `cancel_pending_destructive_run` drops it, both dispatchers now share one `send_query_run` tail, and the dialog lives at `query_view.rs` `draw_destructive_run_dialog`. Documentation drift corrected in the same pass: `docs/08-technology-decisions.md:147` and `docs/09-architecture-decisions.md` §5 both still described multi-statement execution as rejected and the confirmation as future work; §5 now records the implemented contract (worst-statement classification, atomic batch via `execute_transaction`, per-statement envelopes, destructive hold) with code pointers. Positively verified in the matrix: cancellation identity is request-id-scoped at the runtime layer (`worker.rs:1524-1532`; an unknown or finished id is a no-op, so a cancel cannot target another execution), results are routed by originating document id rather than by active tab, a mutation batch is all-or-nothing, and the classifier's unknown-keyword default is the fail-safe `Write`. New tests: 4 UI gate tests (falsified by disabling the gate → 3 failures) + `transaction_control_batches_classify_as_write_and_never_hide_a_mutation` in core. `cargo test --workspace` **883 passed / 0 failed / 27 ignored** (875 at session start; +3 from #80). Gaps recorded, not claimed: no live destructive run, no interactive dialog observation (`R-GUI-SMOKE`), and no test for the cancellation no-op branch |
| #131 | [RC1][Traceability] Map every release-critical capability to tests, CI jobs, runtime smoke, and evidence owner | **CLOSED** 2026-09-15 · `gh issue close 131` | `4eae4ad` | 2026-09-15 | **audit complete** — deliverable `docs/release/audit-release-traceability.md` (baseline `de77057`): the 25 capability rows the issue lists, each with `Capability / Source owner / Unit tests / Integration tests / CI job / Smoke row / Blocking issue / Evidence task / Status / Gap`, using exactly the issue's six coverage words. Every test cell names the test **and** the file:line it was read from (e.g. `pg_introspection_survives_concurrent_index_churn` `pg_integration.rs:1107`, `no_primary_key_table_blocks_safe_row_mutations` `app_tests.rs:475`, the seven `diagram/tests.rs` large-schema cases); CI cells name the real jobs (`Rust checks` with its fixture steps; the release workflow's `Pre-flight checks`, `Build`, `Package`, `Assemble SHA256SUMS`) and record that **no performance job exists**; smoke cells name runbook steps 1–19 (Step 19 added by #126). Statuses: 7× `PROVEN` (grid staging, no-PK/read-only, dirty/close guard, palette, ER large-schema invariants, provider-value type matrix, lint/test/build gates), 3× `PARTIAL` (startup reconnect source-verified only; cancellation lacks a no-op-branch test; export is not atomic), 2× `DEFERRED/NOT SHIPPED` with registry entries (LIM-016, LIM-012) plus the LIM-014 cancellation asymmetry, the rest `TESTED BUT NOT RELEASE-QUALIFIED` because the packaged runtime leg is host-blocked. All gaps assigned: **#238**, **#243**, **#244** and #91–#95 (`R-GUI-SMOKE`/`R-WINLINUX`). Documentation-only change; #111 can use the matrix as its final evidence index |
| #134 | [RC1][Platform] Inventory packaged-runtime native prerequisites and OS-specific dependency risks | **CLOSED** 2026-09-15 · `gh issue close 134` | `dd984bdd` | 2026-09-15 | **the prerequisites document was reconciled with the native runtime** — it still described the Tauri/WebView era (dmg/deb/rpm/MSI, WebKitGTK, WebView2, a disputed `libsecret` deb/rpm dependency, macOS 10.15) while v0.1 ships three portable `db-pro-native` archives. Now: artifact/Source table from `package-*.sh` + `release.yml:14-16`; per-platform matrix separating *build* from *runtime* prerequisites (macOS arm64, **macOS 11.0 measured from `LC_BUILD_VERSION minos`** in `evidence/v01-06/08-post-fix-quality-gates.txt:163`; Windows x86_64 with **no WebView2 requirement**; Linux X11/Wayland + OpenGL + `libdbus-1.so.3` (resolved via `cargo tree --target all`: keyring → dbus-secret-service → dbus → libdbus-sys) + a D-Bus Secret Service provider); the workflow's actual apt list (`release.yml:191-193`, `:397-399`) is the build truth and agrees with the runtime list; external tools (`pg_dump`/`pg_restore`/`ssh` not bundled, `pg_restore` version skew) match `README-INSTALL.txt:22-43` and `release-notes.md:108-111`; §5 is the explicit smoke-setup checklist for #91-#93. No focused child issue spawned: no release-critical dependency declaration is missing. Evidence: docs/release/evidence/v01-runtime/providers/36-native-prerequisites-reconciliation.md |
| #136 | [META][RC1] Central release risk, decision, and blocker register | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/release/risk-register.md:1-40,336-370; docs/plans/STATUS.md:45-52 |
| #141 | [RC1][Brand A1.5] Synthesize competitor research into parity, gap, and positioning evidence map | `BLOCKED_OWNER` (2026-09-15) | — | 2026-09-14 | **re-verified 2026-09-15 and left open as `BLOCKED_OWNER`** (blocker comment posted: exact blocker, what is already done, what would unblock it) — triage pointer: docs/notes/PRODUCT_CAPABILITY_MATRIX.md (partial substitute); no synthesis doc found |
| #144 | [P1][RC1][Security] Make PostgreSQL remote connections secure by default | **CLOSED** 2026-09-15 · `gh issue close 144` | `df9c7edd` | 2026-09-15 | **implemented against the owner IMPLEMENTATION CONTRACT on the issue**: new PostgreSQL connection drafts default to `UiSslMode::Require` (`crates/ui/src/runtime.rs`); SQLite→PostgreSQL driver selection re-initializes TLS to `Require` (`select_connection_driver`); per-mode guidance warns that `Disable` is plaintext; `Disable` remains an explicit opt-out. **Rust `SslMode::default()` / serde fallback left as `Disable`** so persisted records that omit the field do not silently change meaning. Edit/duplicate preservation already landed under #236. Tests: `new_postgresql_connection_defaults_to_tls_require`, `switching_sqlite_to_postgresql_initializes_tls_require`, `editing_a_disable_connection_keeps_disable_until_the_user_changes_it`, `explicit_disable_selection_is_preserved_on_submit`, `ssl_mode_guidance_names_the_plaintext_risk_for_disable` + the #236 regressions. Evidence: `docs/release/evidence/v01-runtime/providers/62-pg-secure-default-tls-require.md`. Deliberate non-claims: no CA/client-cert UI; no packaged GUI observation of the warning text (`R-GUI-SMOKE`). |
| #145 | [P1][RC1][Data Safety] Make SQLite backup and restore snapshot-safe | `BLOCKED_EXTERNAL` (2026-09-15) | `e39290b` | 2026-09-15 | **Measured first, then fixed; issue stays open on its own merge gate.** Measured: the backup was already snapshot-safe (`VACUUM INTO` into `.db-pro-<uuid>.tmp` at `crates/infrastructure/src/backup/sqlite_backup.rs:131`, published by a no-overwrite `hard_link` at `:57-76`) — a WAL source with committed-but-uncheckpointed writes is captured, and a concurrent `BEGIN IMMEDIATE` writer neither tears the artifact nor leaks its uncommitted row. **Two real defects found and fixed:** (1) restore over a target that has a `-wal`/`-shm` pair — live *or* left by a crashed session — returned `Ok(())` while a reader still saw the replaced database (`["from-old-db"]`), because SQLite does not bind a WAL to the database file next to it and replays old frames over the freshly published file; (2) a backup published at a path with a valid stale `-wal` read back as that other database (`["foreign-wal-content"]`). Fix: `sidecar_paths`/`remove_database_sidecars` (`:30`, `:38`) remove `-wal`/`-shm`/`-journal` at the publish target — restore at `:216` (after the staged copy validates, immediately before the atomic rename), backup at `:155` — plus `flush_file` (`:80`, called at `:143`) fsyncing the artifact before it is published by name. Service side: a successful restore now drops the persisted introspection cache for that connection (`crates/core/src/application/backup_service.rs:89`; `BackupService::new` gained `introspection_cache: Box<dyn IntrospectionCache>` at `:15-33`, wired at `crates/runtime/src/lib.rs:170`), so a reconnect cannot serve a schema describing the replaced database. **8 regression tests** (6 in `sqlite_backup`, 2 in `backup_service`); the two defect tests fail on the unfixed code (`FAILED … 5 passed; 1 failed` and `6 passed; 1 failed`, both exit 101) and pass after it. `cargo test --workspace` **837 passed / 0 failed / 19 ignored** (session baseline 827/0/19, **+10**); fmt/check/clippy/release-build/perf-scan (`PASS`, 4/0/0) all green, exit 0. **Remainder (why it is not closed):** the issue's Tests/evidence line requires *packaged-runtime smoke before completion* and its Merge gate requires acceptance on one exact SHA plus reflection in #128/#136 — this environment has no GUI session or packaged artifact (`#91`, `HD-007`), and acceptance is the owner's. Backup cancellation is also untouched: `RuntimeCommand::CancelOperation` (`crates/runtime/src/worker.rs:205`, handled at `:1563`) is never constructed anywhere in the workspace, so a running backup cannot be cancelled (#128's cancellation semantics). Status comment: https://github.com/truongnat/db-pro/issues/145#issuecomment-5668512477. Evidence: docs/release/evidence/v01-runtime/providers/29-sqlite-backup-snapshot-safety.md |
| #146 | [P1][RC1][Governance] Protect main and require release CI checks | **CLOSED** 2026-09-15 · `gh issue close 146` | ruleset `20631004` + `23461211` | 2026-09-15 | **repository settings applied via GitHub rulesets API.** Before: classic branch protection 404; ruleset `main` was active but `ref_name.include=[]` so it matched no branch and only blocked deletion/force-push. After: `main` targets `refs/heads/main`, requires a pull request (`dismiss_stale_reviews_on_push`), requires status check **`Rust checks`** (the live CI job; `Frontend checks` is void after the React archive), blocks deletion and non-fast-forward, retains admin `RepositoryRole` bypass. New `release-tags` ruleset restricts `refs/tags/v*` create/update/delete to the admin bypass. Evidence: `docs/release/evidence/v01-runtime/providers/63-main-branch-protection-rulesets.md`. |
| #182 | [GOAL][Post-v0.1] Expand DB Pro into a DBeaver-class, AI-native database IDE | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-full-product.md; docs/goals/goal-phase-*.md; docs/notes/PRODUCT_ROADMAP.md |
| #183 | [Phase A][A01] Typed Object Mutation Framework | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/notes/V0_1_CLOSURE_PLAN.md:7; docs/goals/goal-phase-a-object-crud.md |
| #184 | [Phase A][A02] Views and Materialized Views CRUD Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #185 | [Phase A][A03] Index CRUD Wizard | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #186 | [Phase A][A04] Constraint CRUD — PK, FK, Unique, Check | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #187 | [Phase A][A05] Trigger Editor and Lifecycle Management | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #188 | [Phase A][A06] PostgreSQL Sequence Management | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #189 | [Phase A][A07] PostgreSQL Types, Enums, and Domains Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #190 | [Phase A][A08] Schema and Database Management | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #191 | [Phase B][B01] Routine Domain Model and Explorer | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-b-routines.md |
| #192 | [Phase B][B02] Routine Source Editor, CRUD, and Execute/Call Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-b-routines.md |
| #193 | [Phase C][C01] Streaming Data Transfer Foundation | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-c-transfer.md |
| #194 | [Phase C][C02] CSV Import and Export Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-c-transfer.md |
| #195 | [Phase C][C03] JSON and Excel Transfer Formats | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-c-transfer.md |
| #196 | [Phase D][D01] Monitoring Foundation — Sessions, Active Queries, Cancel/Terminate | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-d-monitoring.md |
| #197 | [Phase D][D02] Locks, Transactions, Size/Stats, and Maintenance | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-d-monitoring.md |
| #198 | [Phase E][E01] Native Users, Roles, Memberships, and Privileges Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:91-103; docs/goals/goal-phase-e-security.md |
| #199 | [Phase F][F01] Schema Snapshot and Compare Engine | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-f-compare-migration.md |
| #200 | [Phase F][F02] Migration Preview, SQL Generation, and Safe Apply | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-f-compare-migration.md |
| #201 | [Phase G][G01] Global Search Activity and Unified Command Palette | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-g-productivity.md |
| #202 | [Phase G][G02] Saved Queries, Snippets, Favorites, Recent, and Pinned Objects | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-g-productivity.md |
| #203 | [Phase H][H01] Advanced AI Tools on Canonical Application Actions | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-h-ai.md; docs/release/known-limitations.md:119-131 |
| #204 | [Productivity][I01] Connection Folders, Tags, Favorites, Import/Export Profiles | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/notes/PRODUCT_CAPABILITY_MATRIX.md:78 |
| #205 | [Settings][K01] Complete Native Settings and Keybindings Surface | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/notes/PRODUCT_ROADMAP.md |
| #206 | [Tasks][J01] Saved Database Tasks and Run History | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/notes/PRODUCT_ROADMAP.md |
| #207 | [Phase A][A09] Table and Column CRUD Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:77-89 |
| #208 | [Phase C][C04] Backup and Restore Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-c-transfer.md; issue #145 |
| #209 | [Phase C][C05] Database-to-Database Data Transfer | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-c-transfer.md |
| #210 | [Phase F][F03] Data Compare and Row-Diff Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-f-compare-migration.md |
| #211 | [Phase G][G03] Queries Activity — Open, Saved, History, Snippets, Scratch | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-g-productivity.md |
| #212 | [Phase G][G04] Data Activity — Recent, Pinned, and Reusable Dataset Navigation | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-g-productivity.md |
| #213 | [Tasks][J02] Scheduled Database Tasks and Execution Policies | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue body Safety section |
| #214 | [Diagnostics][L01] Native Diagnostics, Logs, and Support Bundle | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/evidence/v01-runtime/providers/19-error-log-audit.txt |
| #215 | [Phase H][H02] Query Plan Visualizer and Slow Query Advisor | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-h-ai.md |
| #216 | [Phase A][A10] Object Dependencies and References Navigator | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md |
| #217 | [Phase A][A11] PostgreSQL Partitioned Tables and Partition Management | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md; issue body 'Parent Goal: #182' |
| #218 | [Phase A][A12] PostgreSQL Extensions Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:77-89 (LIM-004); docs/goals/goal-phase-a-object-crud.md |
| #219 | [Phase E][E02] PostgreSQL Row-Level Security Policies Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:91-103 (LIM-005); docs/goals/goal-phase-e-security.md |
| #220 | [Phase C][C06] SQL INSERT, PostgreSQL COPY, and Script Export Formats | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:189-201 (LIM-012); docs/goals/goal-phase-c-transfer.md |
| #221 | [Phase D][D03] Transaction and Session State Inspector | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-d-monitoring.md; docs/release/known-limitations.md:217-229 |
| #222 | [Phase G][G05] Workspace Sessions, Layout Presets, and Window Restore | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:245-257 (LIM-016); docs/goals/goal-phase-g-productivity.md |
| #223 | [Connections][I02] Advanced SSL/TLS, SSH Profiles, and Connection Diagnostics | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/release/known-limitations.md:105-117 (LIM-006); issue #144; docs/notes/PRODUCT_CAPABILITY_MATRIX.md:356-357 |
| #224 | [Query][Q01] Explicit Auto-Commit, Manual Transaction, Commit, and Rollback Controls | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — issue #147; crates/core/src/application/query_service.rs:213-239 |
| #225 | [Query][Q02] SQL Parameters, Variables, and Reusable Execution Bindings | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — crates/infrastructure/src/postgres/query_mapper.rs:9-64; docs/goals/goal-phase-g-productivity.md |
| #226 | [ER][ER01] Schema Design Mode with Draft Table/Relation Editing | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/goals/goal-phase-a-object-crud.md; docs/plans/STATUS.md (ER large-schema architecture) |
| #227 | [Data][DA01] Column Profiling, Distribution, Null, Distinct, and Quality Insights | `OUT_OF_SCOPE_V01` | — | 2026-09-14 | triage classification only; not yet re-verified against a post-triage commit — docs/notes/PRODUCT_ROADMAP.md; docs/goals/goal-full-product.md |
| #228 | [Data][DA02] Advanced Cell/Record Viewer for JSON, Arrays, Binary, and Large Text | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 data surface (`Parent Goal: #182`), no dependency declared — crates/ui/src/runtime.rs:544-552 (`UiCell` has no record/detail variant); crates/native-app/src/translate.rs:660-679 (`map_cell`); v0.1 items in the same family: #61, #62; docs/notes/V0_1_CLOSURE_PLAN.md:7 |
| #229 | [PostgreSQL][PG01] Foreign Data Wrappers, Foreign Servers, and User Mappings | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 admin scope, depends on #183 (+#198/#218); no partial surface — grep 'postgres_fdw'/'FOREIGN TABLE' over crates/ and fixtures/ returns nothing; docs/notes/V0_1_CLOSURE_PLAN.md:7 |
| #230 | [PostgreSQL][PG02] Logical Replication — Publications, Subscriptions, and Replication Slots | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 admin scope, last in the proposed queue — docs/goals/goal-full-product.md:648; docs/goals/goal-phase-d-monitoring.md:85; docs/notes/PRODUCT_ROADMAP.md:227; grep 'pg_publication'/'pg_replication_slots' over crates/ and fixtures/ returns nothing |
| #231 | [PostgreSQL][PG03] Event Triggers and Database-Level Trigger Administration | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 admin scope, depends on #183 (+#187); v0.1 ships table-trigger introspection only — crates/infrastructure/src/postgres/introspect.rs; grep 'EVENT TRIGGER' over crates/ and fixtures/ returns nothing |
| #232 | [PostgreSQL][PG04] Server Configuration, Runtime Parameters, and Session Settings | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 admin scope, depends on #196 — docs/goals/goal-full-product.md:1375 ('Settings view (`pg_settings`) — GATED (Later)'); docs/goals/goal-phase-d-monitoring.md:88; grep 'pg_settings' over crates/ returns nothing |
| #233 | [Data][DA03] Synthetic Data Generator and Table Seed Workbench | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | snapshot 3, triaged against `main @ f15b1a9`; post-v0.1 data tooling, depends on #207 — fixtures/smoke/large-er/generate-large-er.js is a *test-fixture* generator, not a product surface; docs/goals/goal-phase-g-productivity.md:589; docs/notes/V0_1_CLOSURE_PLAN.md:7 |
| #234 | [Provider][P01] Provider SDK and Capability Contract for Additional Database Engines | `OUT_OF_SCOPE_V01` → **`PARTIAL_ON_MAIN`** — implement landed, acceptance not met, **re-opened 2026-09-15**; pass 8 landed capability truthfulness, the UI capability path, the contract docs and the value/decoder conformance case — criteria 1 and 6 are now MET, 2/3/4/5 remain partial, **issue stays open** | `0729d699`, `111d248f`, `c17afb49`, `93861065`, `46cfed05` | 2026-09-15 | **pass-6 re-verification (2026-09-15, `main @ d4df6373`) — the merge landed a real seam but does not meet this issue's acceptance, so the row moves `OUT_OF_SCOPE_V01` → `PARTIAL_ON_MAIN` and the issue is RE-OPENED (`gh issue reopen 234`; remainder comment https://github.com/truongnat/db-pro/issues/234#issuecomment-5679555835).** Landed (`0729d699`): the `ProviderFactory` trait (`crates/core/src/ports/provider_factory.rs:13`) implemented by Postgres/SQLite/MySQL factories (`crates/infrastructure/src/connector.rs:19,39,59`), `register_factory` for runtime registration (`:149`), a 8-test conformance suite that runs un-gated in CI (`crates/infrastructure/tests/provider_contract_conformance.rs`), `DbError::Unsupported` reasons for some operations, and `DriverType::Mysql` threaded through every match arm. **Met:** criterion 5 structurally (no second UI stack). **NOT met:** criterion 2 — `crates/ui/src/app.rs:687-696` still resolves capabilities from a driver *string* and returns `None` for MySQL, so the new provider has no capability path, and `query_view.rs:147,1549-1553`, `connection_service.rs:49-51`, `user_service.rs:131-134` branch on provider name; criterion 6 — `grep -rl ProviderFactory docs/` returns **no files**, so no architecture doc records the contract, and `docs/release/known-limitations.md:49-57` (LIM-002) plus `docs/release/provider-capability-matrix.md` (no MySQL row) still describe the pre-merge product. **Partial:** criterion 1 (PostgreSQL still reached by bypassing the composite — `crates/runtime/src/api.rs:929,941,953,986,1012`; `pg_integration.rs:19` builds `PostgresConnector` directly, so factory dispatch for PG has no live test); criterion 3 (no capability-level reason channel — `DatabaseCapabilities` is booleans only, `crates/core/src/domain/capabilities.rs:11-112`; UI disabled states are hardcoded, `connection_view.rs:137,344`); criterion 4 (conformance executes SQLite-in-memory only, `provider_contract_conformance.rs:12`; PostgreSQL/MySQL are flag assertions, `:40-51,:71-77,:193-206`; **no value/decoder case**, which is why #235's mapper defect was not caught). Truth-gaps named in the comment: MySQL advertises `parameters: true` while `MySqlConnector::query`/`execute` drop params (`mysql/connector.rs:77,92`), and `server_sessions`/`partitions`/`backup` true while the services reject non-PostgreSQL (`user_service.rs:131-134`, `backup_service.rs:51`, `runtime/src/api.rs:929-953`). No provider code changed in this pass.. **pass-8 (2026-09-15, `main @ aabdda7f`) — capability truthfulness, step 1 of 3: the MySQL advertisement now matches the code.** The five flags pass 6 named as contradicted are corrected in `crates/core/src/domain/capabilities.rs`: `query.parameters` and `query.positional_parameters` are **false** (the provider's `query`/`execute` accept the parameter list and run `sqlx::query(sql)` with no binds — `crates/infrastructure/src/mysql/connector.rs`), and `features.server_sessions`, `features.partitions` and `features.backup` are **false** (`application/user_service.rs` `ensure_server_sessions_config` rejects every non-PostgreSQL driver, `runtime/src/api.rs` `PostgresApi::partitions` resolves a PostgreSQL handle, `application/backup_service.rs` returns a validation error for MySQL). One flag pass 6 did not name is corrected on the same rule and the same kind of evidence: `features.data_diff` was **true** while `DataDiffService::diff_table_data` asks for a dialect and `CompositeConnector::dialect` has no MySQL arm — the only data-diff path errors, so it is now **false**. The rule applied, stated in the doc comment on `DatabaseCapabilities::mysql()`: a flag is true only when a shipping code path serves that capability for this driver. Flags with such a path stay true (`explain` has a live test, `multi_statement`/DDL/INSERT/UPDATE/DELETE go through the driver-agnostic connector methods, `schema.schemas`/`schema.functions` come from live-verified introspection, `schema_diff` compares two `IntrospectResult`s). **Pinned by tests, not by comment:** three new `capabilities.rs` tests (`mysql_capabilities_drop_parameters_until_a_binder_exists`, `mysql_capabilities_do_not_advertise_postgres_only_features`, `mysql_capabilities_keep_the_flags_a_shipping_path_serves`) plus one dispatch-level test in the conformance suite (`mysql_advertisement_matches_the_shipping_code_paths`, `crates/infrastructure/tests/provider_contract_conformance.rs`) that goes through `CompositeConnector::capabilities`, so a factory overriding the values is caught too. `cargo test --workspace` **950 passed / 0 failed / 35 ignored** (baseline 942/0/35, **+8**); `fmt`/`check`/`clippy --all-targets -D warnings` exit 0. No public interface and no persisted data changed — only the advertised values, which are computed at runtime and stored nowhere. **pass-8, step 2 of 3 — the provider has a capability path in the UI.** `crates/ui/src/app.rs` resolved capabilities from a driver *string* through two separate `Option<DatabaseCapabilities>` lookups that returned `None` for anything but SQLite and PostgreSQL, so the MySQL factory registered in `0729d699` had no capability path at all and every gate read as "capability absent". The lookup is now a named state, `CapabilityLookup` (`crates/ui/src/app.rs`): `Supported(DatabaseCapabilities)` for a driver this build ships, `NoActiveConnection` when nothing is connected, and `UnsupportedDriver { driver }` for a label with no provider entry — so "cannot answer" is distinguishable from "the provider supports nothing", and `unavailable_reason()` gives the user-facing why. `for_driver_label` now maps `mysql` → `DriverType::Mysql` as well, so MySQL resolves to its own set (the corrected one from step 1). Consumers gate through `allows(..)` (`events.rs`, `explorer_view.rs`, `query_view.rs`, `navigation_view.rs`), which never satisfies a capability predicate for an unresolved lookup. One more silent-wrong-answer removed: `query_capabilities()` used to fall back to the display label `PostgreSQL` through `active_query_driver()` and answer with PostgreSQL's set while no connection existed; it now resolves from the bound connection, then the active connection, then `NoActiveConnection`. `query_view::explain_query` reports the named reason instead of a generic message. **Tests:** four new UI tests — `mysql_connection_resolves_to_its_own_capability_set`, `unknown_driver_resolves_to_a_named_state_not_none`, `no_active_connection_is_distinct_from_an_unsupported_driver`, `query_capabilities_follow_the_bound_connection_and_do_not_default_to_postgres` — plus the two existing capability tests rewritten onto the new state; `cargo test -p db-pro-ui --lib` **410 passed / 0 failed** (this suite was 406 before this step, **+4**; `git show HEAD~1:crates/ui/src/app_tests.rs | grep -c '^#\[test\]'` 134 → 138). **Criterion 2 is still not fully met:** the UI still branches on provider name where capability dispatch would do (`query_view.rs` sql_format/parse dialect choice, `navigation_view.rs` schema defaults, `connection_service.rs`, `user_service.rs`), and the lookup still starts from a driver label rather than from the provider's own registry — the label mapping is now complete and explicit, not capability-driven. **pass-8, step 3 of 3 — the conformance suite now has a value/decoder case, which is the gap pass 6 named as the reason #235's mapper defect went undetected.** `crates/infrastructure/tests/provider_contract_conformance.rs` gained a section that reads the fixture's `decoder_matrix` — the table both fixtures already ship for this purpose (`fixtures/postgres/001_schema.sql`, `fixtures/mysql/001_schema.sql`), not a new one — **through `CompositeConnector`**, so the same contract is asserted against whichever provider `DATABASE_URL` names (`postgres://` or `mysql://`), self-skipping with a reason when it names neither. Asserted per class: integers stay integers and are never booleans (`id` → `Int64(1)`, `big_count` → `Int64(i64::MAX)`) while the one genuinely boolean column still decodes as `Bool`; `DECIMAL` is exact (`amount`'s 20-digit integer part survives, with an in-test proof that an f64 round-trip would have destroyed it) rather than the `Text("unsupported")` MySQL used to serve; temporal classes keep their semantics (`DATE` has no time part, `Time` stays `Time`, the naive `DATETIME`/`TIMESTAMP` keeps its wall-clock reading and gains **no** offset marker, while the instant class keeps its explicit marker); `JSON` stays structured; the binary class stays byte-exact `Bytes` against the fixture's own bytes; and every nullable family stays `NULL` on the all-NULL row. **Two falsification probes were run against the live MySQL fixture and both turned the test red** (claim `id` is a boolean → failed with the probe message; claim the naive timestamp carries an offset → failed printing the real value `2024-03-15T10:20:30.123456`), then were reverted; the probes are not in the tree. **Criterion 1's "no live test drives PostgreSQL through the new factory dispatch" gap is also closed:** `factory_dispatch_drives_postgres_against_the_live_fixture` connects, queries and introspects PostgreSQL through `CompositeConnector` with a live `DATABASE_URL` (it self-skips on a MySQL URL). **Live results:** PostgreSQL — 11/11 in the conformance target, both new tests ran live; MySQL (`dbpro-v01-mysql-fixture`, recreated for this pass from `fixtures/mysql/`, removed again afterwards) — conformance 11 passed / 0 failed with only the PostgreSQL-specific test skipping, plus `mysql_fixture_matrix` 3 passed / 0 failed. `cargo test --workspace` **952 passed / 0 failed / 35 ignored** (baseline 942/0/35, **+10**; both new tests pass in their skip path with no `DATABASE_URL`); `cargo test --all -- --include-ignored` with the PostgreSQL fixture live **987 passed / 0 failed / 0 ignored** (baseline 977/0/0, **+10** — the same ten new tests, two of which ran live here instead of skipping). **pass-8, documentation: criterion 6's "no architecture doc records the contract" gap is closed, and the two documents pass 6 named stale are corrected.** New `docs/architecture/provider-contract.md` follows the existing architecture documents' shape and records: the `ProviderFactory`/`DbConnector` ports and the `CompositeConnector` registration+dispatch table (including that `DriverType` is a closed enum, so registration is compile-time and no plugin loading is required); the capability model and what `true` means (a shipping code path serves the capability — the rule the corrected MySQL set is written to) **plus an explicit statement that there is no per-flag reason channel yet**, which is criterion 3's remainder; the value-decoding contract, which it points at (`docs/release/provider-value-contract.md`) and the decoder evidence for each provider rather than restating, with the rule that a probe-chain decoder is invalid and the MySQL D1 defect as the worked example; an eight-item list of what a new provider must supply to be considered supported (factory, full `DbConnector` surface with named `Unsupported` refusals, class-aware mapper, introspector, dialect arm, justified capability set, fixture + live tests, UI capability entry); and five recorded contract gaps. `grep -rl ProviderFactory docs/` returned no file before this pass and now returns `docs/architecture/provider-contract.md` (plus this ledger row and the evidence file, which name the trait). `docs/release/known-limitations.md` LIM-002 no longer claims "Only PostgreSQL and SQLite drivers are implemented": it states that PostgreSQL and SQLite are the shipped drivers while MySQL 8 has a real provider on `main` with live evidence and is **not** a shipped v0.1 driver, adding what is still missing (no parameter binder, no dialect arm, disabled UI card, PostgreSQL-only services) so the entry cannot be read as "MySQL is supported"; the entry keeps the registry's `Accepted v0.1` status vocabulary and the Status | Count table is unchanged at 13. `docs/release/provider-capability-matrix.md` gained a `## MySQL 8 (post-v0.1 provider)` section — one row per capability area with its real, verified status (the shipped PG/SQLite tables are untouched), a rule statement, a key-differences entry and source references — with cells rested on live evidence or marked NOT YET QUALIFIED/NOT REACHABLE where nothing verifies them (for example the Data Grid read/filter/sort path is NOT REACHABLE for MySQL because it needs the dialect, and CHECK constraints stay PARTIAL). **pass-8 result (evidence: `docs/release/evidence/v01-runtime/providers/61-provider-capability-truthfulness.md`):** the MySQL capability set now matches the code (6 flags corrected: `parameters`, `positional_parameters`, `server_sessions`, `partitions`, `backup`, and `data_diff` which pass 6 had not named — each with the code path in the table and a pinning test), the UI resolves capabilities through the named `CapabilityLookup` state with MySQL entries and an explicit `UnsupportedDriver` instead of `None`, the provider contract is recorded in `docs/architecture/provider-contract.md`, LIM-002 and the capability matrix state MySQL's real level, and the conformance suite gained a value/decoder case proven live against both fixtures and falsified twice. **Criteria 1 and 6 are MET; 2, 3, 4 and 5 are PARTIAL and named in §6 of the evidence file** — the UI still branches on provider name outside the capability path, there is still no per-flag reason channel, the conformance file has no PostgreSQL/MySQL schema/lifecycle case, and MySQL stays unreachable from the UI while it has no binder or dialect. **The issue is left OPEN** — no partial credit taken. |
| #239 | [P2][RC1][UX] SSH tunnel control ships with no in-UI qualification caveat | **CLOSED** 2026-09-15 · `gh issue close 239` | `ccc0a24` | 2026-09-15 | **filed by the #77 audit** (docs/release/rc1-p2-release-dispositions.md §C) as that audit's single `Fix RC1` row: `crates/ui/src/connection_view.rs:713` renders `Connect via SSH Bastion Tunnel` for every PostgreSQL draft with no hint that SSH is unqualified in v0.1, while LIM-006, the readiness row and `R009` say so only in documents the packaged app never shows. The fix is one muted inline hint in the existing `Preview`-badge style (`agent_view.rs:224`). No behaviour change is asked for. Evidence: docs/release/rc1-p2-release-dispositions.md §C **Closing 2026-09-15:** the caveat is one muted caption in the SSH card (`connection_view.rs:732-740`), below the checkbox (`:724-729`) and outside the `if ssh_tunnel_enabled` block (`:742`) so the option cannot be read as a qualified peer of the other connection fields; it is built from the idiom this same dialog already uses (`:693-697`, `:834-838`) — no new component, no new state, no behaviour change. The string is `SSH_QUALIFICATION_HINT` (`:5-15`) and every claim in it is one LIM-006 already makes: "Unqualified in v0.1" (title + `Accepted v0.1`), "has not been end-to-end tested" (actual behaviour), "may not work reliably" (user-visible impact); the mapping is recorded in the doc comment rather than left to the reader. Three tests in a new `mod tests` pin it — `ssh_section_discloses_its_unqualified_v0_1_status` (painted before enabling), `ssh_caveat_is_non_blocking_and_keeps_the_control_usable` (still painted while enabled, SSH fields still render) and `ssh_caveat_wording_tracks_the_recorded_limitation` (LIM-006's three claims) — asserting on the painted `FullOutput` text shapes, so deleting the label fails even though the constant remains; both probes recorded (label removed → 2 failed / 1 passed; wording replaced with an invented claim → 1 failed / 2 passed) and reverted before the commit. `cargo test --workspace` **886 passed / 0 failed / 27 ignored** (baseline 883/0/27, **+3**); CI-mirroring `--include-ignored` **913 passed / 0 failed** (baseline 910, **+3**) against the `dbpro-v01-pg-fixture`, which was stopped after the run. Recorded, not hidden: `HD-005` (accept the unqualified SSH/backup status) is still `PENDING`, and this caveat is compatible with all three of its options — the in-app half of option (a), not false under (b) because the runtime suite is `BLOCKED` and unrun as of this commit, moot under (c). Evidence: docs/release/evidence/v01-runtime/providers/50-ssh-tunnel-ui-qualification-caveat.md |

| #238 | [P2][RC1][Perf] Result-grid sort rebuilds an allocating projection on every frame | ~~`ACTIONABLE` → `CLAIMED, LEFT OPEN`~~ **CLOSED** 2026-09-15 (auto-closed by the PR #245 merge; closure re-verified in pass 5) | — (verification only, no product commit) | 2026-09-15 | **filed by the #76 audit as its single `Fix RC1` row** — the native carry-over of `QA-P2-12`, measured rather than suspected: `filtered_sorted_indexes` (`crates/ui/src/result_grid.rs:97`) runs from the draw path (`result_grid_view.rs:76`) every frame with no cache and a comparator that allocates via `cell_text` (`:113-122`); 200,000 rows × 4 columns → **5.35 ms** unsorted vs **3,414 ms** sorted on a text column (debug, this host). Reachable at `max_rows` up to `MAX_MAX_ROWS = 100_000` (`crates/core/src/domain/connection.rs:111,116`). Not promoted to P1: responsiveness only, no crash/data loss/credential leak/SQL misclassification (goal-3 §24). The bounded fix options and acceptance list are in the issue. Evidence: docs/release/rc1-p2-release-dispositions.md §B **Re-verified 2026-09-15 on `main @ ba2f515` and deliberately left open — the work is already claimed by open PR #245 (`a0452a32`, branch `fix/rc1-p2-result-grid-sort-perf-7341115780816662696`), whose head is NOT an ancestor of `main` (`git merge-base --is-ancestor` → exit 1; `git log main --oneline --grep=238` → empty), so this pass verified and reported instead of duplicating another session's fix.** The defect was re-measured live before dispositioning (temporary probe, removed before the commit): 200k rows × 4 columns → 5.29 ms unsorted (audit 5.35 ms), **166.6 ms sorted on a plain text column, 6,649.6 ms sorted on a timestamp-shaped text column** (the audit's 3,414 ms is a lower bound, not the worst path — only a column whose values parse as temporal reaches it), 72.9 ms filter+sort. PR #245 implements the issue's option (b) (allocation-free `cell_text_as_str` comparator + a `looks_like_iso_temporal` parse gate) but not option (a): it does not touch `result_grid_view.rs`, so the projection is still rebuilt every frame — cheaper per frame, not once per data-or-sort change; it also adds no benchmark, so acceptance items 1 (no per-frame rebuild, call-count/work-count pinned) and 3 (sorted-path criterion case) stay unmet on that branch. Status comment: https://github.com/truongnat/db-pro/issues/238#issuecomment-5672452264. Evidence: docs/release/evidence/v01-runtime/providers/49-result-grid-sort-claim-verification.md **Closed 2026-09-15, but not by this pass and not on that PR's terms:** PR #245 merged as `00a3ce9` and GitHub auto-closed the issue at `2026-09-15T00:41:15Z`. The merged diff covered the issue's option (b) only (`result_grid.rs`/`lib.rs`; allocation-free comparator, `looks_like_iso_temporal` gate, ASCII filter fast path) and left acceptance items 1 and 3 unmet — the draw path still rebuilt the projection every frame and no sorted-path benchmark existed. Pass 5 therefore did not confirm the merge's closure: it re-measured on `main @ 9a8dc5b` (5.50 ms unsorted / 11.60 ms plain-text-sorted / **10,528.0 ms temporal-text-sorted** / 98.97 ms filter+sort, debug, 200k × 4) and implemented the missing bounded part in `7c356ff` — `GridProjectionCache` keyed on (epoch, filter, sort column, direction, row count, column count), epoch advanced at every site where displayed rows change, draw path take/restore, header auto-size reuse. Measured through the real draw path, cache off vs on, same build: **10,652 / 10,651 / 10,623 ms per frame → 10,731 ms on the first frame only, then 41.1 / 40.0 / 42.7 / 39.8 ms**; criterion group `result_grid_projection_sorted` added; 6 tests, two through the draw path, falsified twice. Status comment: https://github.com/truongnat/db-pro/issues/238#issuecomment-5673088045. Residual found and filed separately as **#246** (`GridSelectionLookup::new`: 36.5 ms of the ~40 ms steady-state frame). Evidence: docs/release/evidence/v01-runtime/providers/53-result-grid-projection-cache.md **Pass-6 ledger reconciliation:** the status cell still read `CLAIMED, LEFT OPEN` although PR #245 (`00a3ce9`) auto-closed the issue at `2026-09-15T00:41:15Z` and pass 5 re-verified the closure on the merged tree. Corrected so the open-row count matches the live repository; no new work, no change to the recorded evidence. |

| #235 | [Provider][P02] MySQL 8 Provider — Connection, Query, Schema, Data, and Core Tooling | `OUT_OF_SCOPE_V01` → **`PARTIAL_ON_MAIN`** — provider landed, acceptance not met, **re-opened 2026-09-15** | `5d075fe8` | 2026-09-15 | **pass-6 re-verification (2026-09-15, `main @ d4df6373`) — the adapter landed but the acceptance is not met and one criterion is a P1-class defect, so the row moves `OUT_OF_SCOPE_V01` → `PARTIAL_ON_MAIN` and the issue is RE-OPENED (`gh issue reopen 235`; remainder comment https://github.com/truongnat/db-pro/issues/235#issuecomment-5679558249).** Landed (`5d075fe8`): `MySqlConnector` (connect/disconnect/test/query/execute/execute_batch/execute_transaction/explain/dialect, cancel → `Unsupported`), `MySqlQueryMapper`, information_schema introspection (tables/columns/PK/FK/views/triggers/routines), `MySqlFactory` registered by default (`crates/infrastructure/src/connector.rs:134-137`), 5 live tests. **NOT met — criterion 3 (no silent precision loss):** `extract_cell` (`crates/infrastructure/src/mysql/query_mapper.rs:42-61`) probes `bool → i64 → f64 → String → Vec<u8>` and falls back to `CellValue::Text("unsupported")`; the vendored driver's compatibility rules (`sqlx-mysql 0.8.6`: `types/float.rs:9-13` excludes `DECIMAL`; `types/str.rs:19-32` accepts only VarChar/Blob-family/String/VarString/Enum; `types/int.rs:10-19` excludes `UNSIGNED`) mean **DECIMAL, JSON, DATE/DATETIME/TIMESTAMP/TIME and BIGINT UNSIGNED miss every probe and land on the fallback** — the value is destroyed and replaced by a literal string, on a column class the repository's own rules list under "never silently coerce precision-sensitive values". **NOT met — criterion 1:** no MySQL service in CI (`.github/workflows/ci.yml:18-34` declares PostgreSQL only), no MySQL fixture in `fixtures/` or `docker-compose.yml`, and the 5 live tests self-skip unless `DATABASE_URL` is `mysql://` (`mysql_integration.rs:18-20`), so the live half has never run in CI and no live run is recorded. **NOT met — criterion 5:** `CompositeConnector::dialect` returns an error for MySQL (`connector.rs:360-362`), so the table-data paths (`crates/core/src/application/table_data_service.rs:74,106,133,168,193,245`) cannot run. **Partial — criterion 4:** indexes are emitted with `table_name: String::new()` (`mysql/introspect.rs:134`) and grouped by `index_name` alone (`:108-119`), so `SchemaService`'s per-table filter (`crates/core/src/application/schema_service.rs:116-121`) drops **every** MySQL index; `check_constraints` is unconditionally empty (`:272`). **Partial — criteria 2 and 6:** the UI driver card is `is_disabled: true` ("Soon", `crates/ui/src/connection_view.rs:399-412`) so nothing is reachable, and there is no service/UI/runtime test for MySQL. Related main-health fact: this file was breaking the CI-mirroring gate on `main` at `455f2718` (`cargo test --all -- --include-ignored` → 5/5 MySQL tests failed in the PostgreSQL job) — fixed in pass 6 by `f2a36b4b` (skip with a reason when no MySQL fixture) and `d4df6373` (3 clippy `-D warnings` errors in the same file). The decoder defect was **not** measured live: no MySQL image on this host and no MySQL service in CI, so the evidence is the code path plus the driver's decode rules. No provider code changed in this pass. **Pass 7 (MySQL live-evidence pass, 2026-09-15 evening, `main @ e1b2eab2`, fix commit `640eaf4c` → see the evidence cell below) closed the two gaps this row named and measured a third defect the earlier code reading had missed.** A disposable `mysql:8.4` (8.4.11) fixture now exists in the repository (`fixtures/mysql/001_schema.sql`, `002_seed.sql`, `003_verify.sql`; container `dbpro-v01-mysql-fixture`, port 33306; new additive `mysql` service in `docker-compose.yml`). **Criterion 1 is now MET:** the five `#[ignore]`d live tests executed for the first time and came back **3 passed / 2 failed** (exit 101); after the fixes they are **5 passed / 0 failed**, with 3 further fixture-driven live tests (41-cell decoder matrix, integer/routine path, PK/FK/index introspection) that self-skip unless `DATABASE_URL` is `mysql://`. **Criterion 3's read half is fixed and proven by measurement:** the old `extract_cell` probed `bool → i64 → f64 → String → Vec<u8>` and, because the driver's `bool` decoder is `i8::decode(value) != 0` and its `bool` list accepts *every* integer class and `BIT`, **every integer whose value fits in an `i8` was served as `Bool`** — `SELECT count(*)` was a boolean, which is what failed `mysql_transaction_rolls_back_on_failure` live; `BIGINT UNSIGNED` above `i64::MAX` arrived as `Bool(true)` (not on the `"unsupported"` fallback as this row previously recorded — the `bool` list has no unsigned exclusion, unlike the `i64` list), and `DECIMAL`/`DATE`/`DATETIME`/`TIMESTAMP`/`TIME`/`YEAR`/`JSON`/`GEOMETRY`/`BIT(n>1)` were replaced by the literal `Text("unsupported")` (or, for `BIT`, by a boolean compiled from one byte). The mapper now dispatches on the reported class with arms covering every name `MySqlTypeInfo::name()` can produce (7 new unit tests + the live matrix), `DATETIME` keeps a marker-free reading while `TIMESTAMP` is a UTC-marked instant (verified with the server's global zone moved to `+07:00`; the driver pins the session to `+00:00`), and `DECIMAL(30,10)`/`BIGINT UNSIGNED` come back as exact `Decimal` digits. **Two more live defects were found and fixed in the same pass:** `information_schema` introspection could not run at all on MySQL 8 (`ColumnNotFound("table_name")` — MySQL 8 labels those columns in upper case, then a second failure from the same family, `VARBINARY`-flagged text columns failing the driver's `String` gate), and every introspected index was emitted with `table_name: String::new()`, so `SchemaService`'s per-table filter dropped **every** MySQL index (now grouped by table + name and carrying its table). **Criteria 2, 5 and the write half of 3 remain NOT met** (disabled UI card; `CompositeConnector::dialect` still refuses MySQL; `query`/`execute` still drop `_params` with no MySQL binder), so the issue **stays open** with a per-criterion table in the evidence file. Evidence: `docs/release/evidence/v01-runtime/providers/60-mysql-live-fixture-and-mapper.md`; the live per-criterion statement on the issue is the pass-7 comment https://github.com/truongnat/db-pro/issues/235#issuecomment-5680021447 |
| — | **snapshot 4 arrivals (2026-09-15 evening, #247–#284): the 38 issues filed by the post-v0.1 `[GOAL]` #182 workstream, triaged against `main @ 6171c45c`, one row per issue below** | — | — | — | — |
| #247 | [Query][Q03] Visual Query Builder with SQL Round-Trip | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182 (`[GOAL][Post-v0.1]`, open); `grep` for `query_builder` or `QueryBuilder` over `crates/ --include=*.rs` → **0** hits, and `crates/ui/src/query/` holds only `query_document.rs`, `schema_completion.rs`, `sql_format.rs`. The SQL parser (`sqlparser 0.52`, `crates/ui/Cargo.toml:12`) and the FK graph (`crates/ui/src/diagram/model.rs:109`) are foundations, not a builder. No v0.1 promise is broken by queueing it. |
| #248 | [Data][DA04] Result Charting and Lightweight Visualization | `PARTIAL_ON_MAIN` | — | 2026-09-15 | The merged `dd771155` added a chart *engine* that nothing mounts: `crates/ui/src/chart_view.rs` (682 lines, **0 `#[test]`**) is referenced outside itself only by the re-export at `crates/ui/src/lib.rs:23`; `OutputTab` (`crates/ui/src/app.rs:235-240`) has no chart tab; `grep -c -i chart crates/ui/src/query/query_document.rs` → **0**. Per-criterion remainder: https://github.com/truongnat/db-pro/issues/248#issuecomment-5679567754 |
| #249 | [Schema][S08] Object Comments and Metadata Documentation Editor | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #183 (open). `grep` for `pg_description`, `col_description` or `COMMENT ON` over `crates/ --include=*.rs` → **1** hit, a gallery fixture string (`crates/ui/src/component_gallery_view.rs:2102`); no `description`/`comment` field exists in `crates/core/src/domain/schema.rs`, so object comments are neither read nor written anywhere. |
| #250 | [Schema][S09] Schema Documentation Export — Markdown, HTML, and Portable Catalog | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on schema introspection and #249. The only export formats are CSV/JSON/XLSX (`crates/core/src/application/export_service.rs:65,95,123`); `grep` for `html`/`markdown` over `crates/` finds no documentation generator (Markdown appears only inside an agent prompt, `crates/runtime/src/agent.rs:20`). `diff_schemas` emits diffs, not catalogs. |
| #251 | [Monitoring][D04] pg_stat_statements Workload Dashboard and Top Query Analysis | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #196 (monitoring foundation, open). `grep` for `pg_stat_statements`/`pg_stat_activity` over `crates/ --include=*.rs` → **1** hit, an agent-prompt sample inside the component gallery (`crates/ui/src/component_gallery_view.rs:2565`); the Monitoring rail entry is a placeholder (`crates/ui/src/navigation_view.rs:615-620`). |
| #252 | [Routines][B04] PL/pgSQL Debugger and Routine Execution Trace | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #191/#192 (both open) and is explicitly deferred by the roadmap (`docs/notes/PRODUCT_ROADMAP.md:104`, "no debug-server ambitions (deferred)"). `grep` for `pldbgapi`/`breakpoint` over `crates/ --include=*.rs` → **0** hits; `pg_proc` is read only for routine introspection (`crates/infrastructure/src/postgres/introspect.rs`). |
| #253 | [Data][DA05] Data Masking, Anonymization, and Safe Sample Export | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on the transfer engine (#193/#233, unlanded). `grep -rni mask crates/ --include=*.rs` → **1** hit, an unrelated geometry variable (`crates/ui/src/components/legacy.rs:475`); `crates/core/src/application/export_service.rs` has no transform/redaction hook and there is no import pipeline. |
| #254 | [Connections][I03] Cloud Database Connection Helpers for RDS, Cloud SQL, and Azure Database | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #223 (open). `grep -rniE` for the words `rds`, `azure`, `cloud sql`, `aurora` over `crates/ --include=*.rs` → **0** hits; the shipped base is generic only — `SslMode` (`crates/core/src/domain/connection.rs:48`), the SSL dropdown (`crates/ui/src/connection_view.rs:626-645`) and the SSH-tunnel caveat (`crates/ui/src/connection_view.rs:5-15`). |
| #255 | [Productivity][G06] Git-Backed SQL, Migration, and Schema Artifact Workspace | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182. `grep -rnw git crates/*/src --include=*.rs` → **0** hits; saved queries persist through the meta-store repository (`crates/core/src/application/query_service.rs:76,473`), not files. The W-series names this issue as "Related", so it is not folded into #261. |
| #256 | [Platform][PL01] Native Plugin SDK for Provider and Tool Extensions | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #234, which pass 6 moved to `PARTIAL_ON_MAIN` and re-opened. No dynamic-loading dependency exists (`grep` for `libloading`/`plugin_manifest`/`PluginHost` over `crates/ --include=*.rs` → **0** hits); the only "plugin" in the tree is the Tauri dialog plugin (`crates/tauri-app/src/lib.rs:13`). |
| #257 | [Query][Q04] SQL Linting, Static Analysis, and Configurable Code Quality Rules | `PARTIAL_ON_MAIN` | — | 2026-09-15 | Rule-like checks **do** ship in the editor path — `crates/ui/src/query_view.rs:1620` (unclosed string literal), `:1624-1627` ("UPDATE without WHERE will affect every row"), `:1629-1633` (SQLite `ILIKE` rejected) — but they are hard-coded heuristics: the diagnostic model's sources are `Parser`/`Delimiter`/`Database` with **no `Lint` variant** (`crates/ui/src/editor/diagnostics.rs:4-9`), and there is no rule registry, per-rule severity/config or quick-fix surface. The missing part is an engine, not a rewrite. |
| #258 | [Monitoring][D05] Database Activity and Audit Event Viewer | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #196 (open). No audit/log reader exists (`grep` for `pgaudit`/`log_directory` over `crates/ --include=*.rs` → **0** hits); the Monitoring rail entry is a placeholder (`crates/ui/src/navigation_view.rs:615-620`) and `LogViewer` is gallery-only (`crates/ui/src/components/logs.rs`). |
| #259 | [Monitoring][D06] Database Health Advisor and Actionable Recommendations | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #196/#215/#251. Same placeholder rail entry (`crates/ui/src/navigation_view.rs:615-620`); no health/statistics rule engine exists — `crates/core/src/domain/safety.rs` classifies statements for safety, it does not advise on database health. |
| #260 | [Provider][P03] SQL Server Provider — Core Connection, Query, Schema, and Data Tooling | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #234 (re-opened, partial). `grep -ci` for `sql server`/`sqlserver` → **0** hits in both `crates/core/src/domain/connection.rs` and `crates/ui/src/connection_view.rs`: `DriverType` is Postgres/SQLite/Mysql only and the engine grid has no SQL Server card (`crates/ui/src/connection_view.rs:399-495`). `SqlServerDialect` exists only under `#[cfg(test)]` (`crates/core/src/application/sql_builder.rs:791`). |
| #261 | [IDE][W01] Local Workspace Folder and Project File Tree | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182, and the declared dependency of W02–W12 — the keystone of the whole W-series. `grep` for `WorkspaceService`/`WorkspaceRoot`/`workspace_index` over `crates/ --include=*.rs` → **0** hits (the only `WorkspaceTab` match, `crates/ui/src/app.rs:214-222`, is an unrelated tab enum), the ratified service list has no file/workspace service (`docs/goals/goal-full-product.md:345-357`) and the activity rail has no Files entry (`docs/goals/goal-full-product.md:226-238`). Adding a file layer is itself a product decision. |
| #262 | [IDE][W02] SQL File Editor, File-Backed Query Documents, and Workspace Run Context | `PARTIAL_ON_MAIN` | — | 2026-09-15 | The in-app document model ships — `QueryDocument` carries connection/schema binding, dirty flag and saved-query identity (`crates/ui/src/query/query_document.rs:86-99`), with run/run-selection in `crates/ui/src/query_view.rs:676-679` — but everything file-backed is missing (no `QueryDocumentSource::Scratch`/`Saved`/`File`, no external-change conflict check) and depends on #261 (open). |
| #263 | [IDE][W03] Unified Context Picker for Files, SQL Selections, Schema Objects, and Agent | `PARTIAL_ON_MAIN` | — | 2026-09-15 | A versioned, document-fingerprinted agent payload ships (`AgentContextBuilder`, `crates/ui/src/agent_state.rs:214-224`) rendered as read-only context chips (`crates/ui/src/agent_view.rs:254-268`; `ContextChipKind`, `crates/ui/src/components/agent_primitives.rs:8-30`). There is no add/remove picker, no folder/file context and no per-item sensitivity/size/staleness state — folder context needs #261/#264. |
| #264 | [IDE][W04] Workspace Index, Symbol/Search Layer, and SQL Project Intelligence | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261. `grep` for `workspace_index`/`SymbolIndex` over `crates/ --include=*.rs` → **0** hits (the one `full_text` hit is a local variable, `crates/ui/src/query/schema_completion.rs:617`); the only search index the ratified plan defines is DB-metadata backed (`SearchService`, `docs/goals/goal-full-product.md:355`). |
| #265 | [IDE][W05] Multi-Root Workspace and Workspace Configuration | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261. `grep` for `workspace.json`/`dbpro.workspace` over `crates/ --include=*.rs` → **0** hits and no versioned workspace-config format is defined anywhere; the only tree model is the database object tree (`crates/ui/src/components/tree.rs:35-46`). Distinct from the multi-root gap the plan records for the DB `ObjectTree` (`docs/goals/goal-full-product.md:371`). |
| #266 | [IDE][W06] File Operations, Tabs, Breadcrumbs, and IDE Navigation History | `PARTIAL_ON_MAIN` | — | 2026-09-15 | Document-level navigation and tab management ship — back/forward over documents (`crates/ui/src/navigation_view.rs:49-70`), close-others/close-right/run tab context menus (`crates/ui/src/workspace_view.rs:116-282`) and dirty-close protection (`crates/ui/src/app.rs:1168`). File operations, preview/pin tabs, editor breadcrumbs, reopen-closed and reveal-in-file-manager all need #261. |
| #267 | [IDE][W07] Find in Files, Replace in Files, and SQL-Aware Workspace Search | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261/#264. Only single-document find ships (`EditorSearchState`, `crates/ui/src/query/query_document.rs:36`; "Find in SQL…", `crates/ui/src/query_view.rs:219-227`); `grep` for `find_in_files`/`replace_all` over `crates/ --include=*.rs` → **0** hits. |
| #268 | [IDE][W08] SQL Navigation — Go to Definition, References, Symbols, and Database Objects | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261/#264. `grep` for `go_to_definition`/`find_references` over `crates/ --include=*.rs` → **0** hits; editor intelligence is completion only (`crates/ui/src/query/schema_completion.rs`), and the file-free parts (open database object, hover metadata) are thin slices of the separately-tracked #216. |
| #269 | [IDE][W09] Workspace Problems Panel for SQL Diagnostics, Migration Errors, and Lint Findings | `PARTIAL_ON_MAIN` | — | 2026-09-15 | A real diagnostics engine ships — `Diagnostic{range,severity,message,source,code}` with `Parser`/`Delimiter`/`Database` sources (`crates/ui/src/editor/diagnostics.rs:4-17`), severity-coloured inline rendering (`crates/ui/src/editor/renderer.rs:703-724`) and a per-document count (`crates/ui/src/query_view.rs:349-355`) — but there is no panel, no cross-file aggregation, no severity/source filters and no next/previous-problem command; those need #261/#264 and #257's lint findings. |
| #270 | [IDE][W10] Integrated Terminal and Database-Aware Task Runner | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182. `grep` for `portable_pty`/`TerminalPanel`/`terminal_view` over `crates/ --include=*.rs` → **0** hits; the only process spawns are SSH (`crates/infrastructure/src/ssh/tunnel.rs:51,71`) and pg_dump/pg_restore, and the ratified service list has no process/pty service (`docs/goals/goal-full-product.md:356`, where `TaskService` is "Later"). |
| #271 | [IDE][W11] Workspace Trust, Secret Detection, and Safe Project Context Boundaries | `NEEDS_OWNER_DECISION` | — | 2026-09-15 | The trust model is a product/security posture the tree does not choose: no path-identity or trust model exists (`docs/release/audit-security-boundaries.md:125-131`; `grep` for `canonicalize`/`symlink`/`read_link` over `crates/ --include=*.rs` → 0 hits), and the disclosed-egress subset is already accepted v0.1 scope (LIM-019, `docs/release/known-limitations.md:291`). Which secret patterns become a *binding* guarantee — and whether an untrusted-workspace mode belongs in a single-user desktop app at all — is the owner's call, and it only becomes concrete after #261 exists. Partially overlaps the open #126. |
| #272 | [IDE][W12] Migration Project Model, Migration Tree, Status, and Database Drift Detection | `NEEDS_OWNER_DECISION` | — | 2026-09-15 | Directly conflicts with a ratified non-goal: `docs/goals/goal-full-product.md:742` lists "migration history as a versioned changelog product" and "ORM migration frameworks" as non-goals, while the issue asks for a migration project model with framework adapters. The preview/apply half belongs to #199/#200, the file-artifact half to #255, and the drift half to #273; `crates/core/src/application/schema_diff.rs:11-19` is DB→DB only. Either the non-goal is amended or W12 is reduced to a subset of F — an owner call, not a triage. |
| #273 | [IDE][W13] Database Schema Snapshot Files and Workspace-to-Live Schema Sync | `PARTIAL_ON_MAIN` | — | 2026-09-15 | The compare half exists but is backend-only and shallow: `SchemaService::diff_schemas` (`crates/core/src/application/schema_diff.rs:11-19`) compares tables/columns/indexes and has **no UI consumer** (`grep -rn diff_schemas crates/ui/src crates/native-app/src` → 0 hits). Snapshot files, Git-friendly serialization, staleness fingerprints and workspace↔live sync do not exist. Flagged for the owner: F01's roadmap row puts snapshot persistence in the meta store (`docs/goals/goal-full-product.md:1168`) while this issue asks for workspace files. |
| #274 | [IDE][W14] AI Workspace Edit — Multi-File SQL Patches with Diff Review and Apply | `PARTIAL_ON_MAIN` | — | 2026-09-15 | Single-document review+apply ships with a stale guard (`AgentSqlPatch{document_id, expected_version, range}` and `apply_to` returning `StaleVersion`/`DocumentMismatch`, `crates/core/src/domain/agent.rs:159-180`; preview and confirmation, `crates/core/src/domain/agent_workflow.rs:436-463`; single patch diff view, `crates/ui/src/agent_view.rs:473-500`). Multi-file patch sets, per-file accept/reject, workspace roots and workspace undo do not exist and depend on #261. |
| #275 | [IDE][W15] SQL Refactoring Engine — Rename Object References and Safe Workspace Rewrites | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #264/#268. `grep` for `find_references`/`go_to_definition` over `crates/ --include=*.rs` → **0** hits; the only related artifact is an unused DDL string helper (`build_alter_table_rename`, `crates/core/src/application/ddl_builder.rs:98`, no caller). There is no reference-aware rename surface to extend. |
| #276 | [IDE][W16] Split Editor Groups, Side-by-Side SQL, and Persistent Layouts | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182. `grep` for `split_pane`/`editor_group`/`PaneGroup` over `crates/ --include=*.rs` → **0** hits; the workspace is a flat tab bar (`crates/ui/src/workspace_view.rs:17`). What does persist is pane widths and query documents (`crates/ui/src/app.rs:458-483`), which the open #222 already owns. |
| #277 | [IDE][W17] SQL Notebook Documents with Mixed Markdown, SQL Cells, and Results | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261/#262. `grep -rni notebook crates/ --include=*.rs` → **0** hits; the document model is in-memory tabs (`crates/ui/src/query/query_document.rs`), so both the versioned notebook format and the cell runner are new subsystems. |
| #278 | [IDE][W18] SQL Test Suites, Assertions, Fixtures, and Regression Runner | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #270 (task runner, itself post-v0.1) and #261. `grep` for `test_suite`/`TestSuite` over `crates/ --include=*.rs` → **0** hits: fixtures, isolation and a test explorer are greenfield, with nothing on main to extend. |
| #279 | [IDE][W19] Migration Sandbox and Disposable Preview Database | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #272/#208 (both open). `grep -rni sandbox crates/ --include=*.rs` → **0** hits, and `grep 'CREATE DATABASE'` matches only the live SSH backup test (`crates/infrastructure/tests/ssh_backup_runtime_verification.rs:117`), not product code. Provisioning also needs a live server with `CREATE DATABASE` rights — a host the gate does not have. |
| #280 | [IDE][W20] Workspace Environment Profiles and Context Switching | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; depends on #261/#265. The closest existing binding is `RunConfig{connection_id,…}` (`crates/core/src/domain/run_config.rs:6-12`), recorded as **unwired** (`docs/notes/PRODUCT_ROADMAP.md:239`); `grep -i environment crates/ui/src` matches only a gallery string. There is no workspace and no environment concept for profiles to attach to. |
| #281 | [IDE][W21] Unified Dependency Graph Across Workspace SQL and Live Database Objects | `PARTIAL_ON_MAIN` | — | 2026-09-15 | The live-database half ships and is user-reachable: `UiDependencyKind` and dependency rows (`crates/ui/src/runtime.rs:183,196`), a Dependencies tab drawing incoming/outgoing entries (`crates/ui/src/table_metadata_view.rs:846,975-992`) and ER edges (`crates/ui/src/diagram/model.rs`, `crates/ui/src/diagram_view.rs:95`). Workspace-file/migration nodes, impact analysis and file:range jumps do not exist, and the open #216 must stay the canonical dependency service rather than being forked. |
| #282 | [IDE][W22] SQL CodeLens and Inlay Hints for Connections, Objects, and Query Actions | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182. `grep` for `codelens`/`inlay` over `crates/ --include=*.rs` → **0** hits; the reuse surface is the decoration/diagnostic layer (`crates/ui/src/editor/decorations.rs:12-19`, incl. `GhostText`; `crates/ui/src/editor/diagnostics.rs:20-60`), which is a foundation, not a delivered slice of this issue. Related to #257 but distinct. |
| #283 | [IDE][W23] Schema Drift Watcher and Background Change Notifications | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182; overlaps #272 and #199. There is no background loop to hang a watcher on (`grep` for `poll_interval`/`set_interval` over `crates/runtime crates/ui --include=*.rs` → **0** hits): introspection caching is one row per connection with no fingerprint compare (`crates/infrastructure/src/meta/introspection_cache_repo.rs:11-40`) and invalidation is DDL-driven or manual (`crates/core/src/application/schema_service.rs:382`). |
| #284 | [IDE][W24] Query Benchmark Suites and Before/After Performance Comparison | `OUT_OF_SCOPE_V01` | — | 2026-09-15 | Post-v0.1 child of #182. The repo's benches are developer-only criterion targets (`crates/ui/benches/result_grid_benchmarks.rs`, `crates/infrastructure/benches/sqlite_benchmarks.rs`) — explicitly **not** the shippable user surface this issue asks for — while per-execution timing is already surfaced as "rows · ms" (`crates/ui/src/query_view.rs:472`) from the history model (`crates/core/src/domain/history.rs:11`). No in-app suite, warmup or comparison exists. |

## Closed by this workstream

A closed issue moves here from the triage table with its closing reference; the triage content is kept, nothing is deleted. Phase 1 (the triage pass) closed nothing: it is analysis and documentation only.

| # | Title | Closed | Commit(s) | Verification evidence (triage pointer → closing evidence) |
|---:|---|---|---|---|
| #142 | [P1][RC1][Security] Configure real OS keyring stores and remove DEV-only fallback from production | 2026-09-15 · `gh issue close 142` | `78fb39b8` | **triage:** crates/runtime/src/lib.rs:73-75 (`with_fallback()` wired unconditionally); crates/infrastructure/src/secret/keyring_vault.rs:159-181,244; Cargo.toml:29-33; regression history 81dcad19 -> cce09fd3 — **closing:** `DbProRuntime::new` now builds the store through `build_secret_store` (`crates/runtime/src/lib.rs:99`): OS keyring + in-memory session fallback in every build, encrypted-file fallback added only when `file_secret_fallback_enabled_for` (`:83`) allows it (debug build or `DB_PRO_ALLOW_FILE_SECRET_FALLBACK` = `1`/`true`), and a release build that did not opt in never opens `secrets.json` at all (`crates/infrastructure/src/secret/keyring_vault.rs:189`). No-keyring-item release degradation is the session store, not the file (`:194`, `:213-221`), pinned by `secret::keyring_vault::tests::disabled_file_fallback_never_touches_a_stale_fallback_file`. 5 tests in `db-pro-runtime` + `db-pro-infrastructure` (incl. the release-only `release_secret_store_never_selects_the_file_fallback_by_default`); `cargo test --workspace` **827 passed / 0 failed / 19 ignored** (baseline at `1ff33ae`: 823/0/19), fmt/check/clippy/release-build/perf-scan green. `docs/architecture/security-boundaries.md:35`, `docs/release/provider-capability-matrix.md:41`, `docs/release/risk-register.md` (`R011` corrected + new `R-KEYRING-FALLBACK`) updated. Evidence: docs/release/evidence/v01-runtime/providers/26-keyring-fallback-gating.md — **not** closed by this: the cross-platform runtime leg of the keyring backends (Windows/Linux hosts), owned by #92/#93 (`R-WINLINUX`), and `R-KEYRING-STALL`/`LIM-018`, which is untouched by this fix. |
| #55 | [Gate 5][B1] Implement exact numeric/integer PostgreSQL decoder paths | 2026-09-15 · `gh issue close 55` | — (pre-existing on `main`; no new commit) | triage: crates/infrastructure/src/postgres/query_mapper.rs:336-360,514-584; tests :672,:689; crates/core/src/domain/query.rs:305; docs/release/evidence/v01-runtime/providers/07-pg-integration-live.txt:24 — **closing (batch 3 item 3):** re-verified on `main @ e0bfc56` — `crates/infrastructure/src/postgres/query_mapper.rs:336-352` decodes `INT2`/`INT4`/`INT8`/`OID` to `CellValue::Int64` with no `f64` hop and `:514-584` holds `decode_binary_numeric`; `cargo test -p db-pro-infrastructure --lib postgres::query_mapper::tests` → **9 passed / 0 failed / 0 ignored / 55 filtered out** today (incl. `:672`, `:689`); `crates/core/src/domain/query.rs:305` `int64_serializes_as_string_for_lossless_ipc`; live `providers/07-pg-integration-live.txt:24` `pg_query_preserves_numeric_and_enum_values ... ok` |
| #74 | [RC1][P2-0] Freeze post-P1 audit baseline and consolidate latest findings inventory | 2026-09-15 · `gh issue close 74` | — (pre-existing on `main`; no new commit) | triage: docs/release/evidence/v01-06/06-rc1-p2-dispositions.md:3 (audited at HEAD 3e0b077), :13-50 (25 confirmed, disposition table) — **closing (batch 3 item 3):** `docs/release/evidence/v01-06/06-rc1-p2-dispositions.md:3` = `Audited at: HEAD 3e0b0775c2248d37253d7325eddebd952e26d101`; `:13-50` = the 25-finding disposition table with the React-fix vs native carry-over column, reconciled against `rc1-full-product-qa/FINDINGS.md:13` (`P2 = 25`) |
| #83 | [RC1][Freeze A1] Select candidate main SHA and lock release-evidence input | 2026-09-15 · `gh issue close 83` | — (pre-existing on `main`; no new commit) | triage: docs/release/0.1.0-readiness.md:4-6; docs/release/evidence/v01-06/09-toolchain-pinning.txt; rust-toolchain.toml — **closing (batch 3 item 3):** `docs/release/0.1.0-readiness.md:4-6` names candidate `85a7fa3cc0a84c56ac2a5049ce08130db06e0a20` and final run `34860902181`; `git merge-base --is-ancestor 85a7fa3… HEAD` exits 0 today; `rust-toolchain.toml` pins `channel = "1.95.0"` (exact, workflow-enforced) and `09-toolchain-pinning.txt` records the `stable` → `1.95.0` change |
| #85 | [RC1][Freeze B2] Run complete Rust workspace release gates on candidate SHA | 2026-09-15 · `gh issue close 85` | — (pre-existing on `main`; no new commit) | triage: docs/release/evidence/v01-06/08-post-fix-quality-gates.txt; docs/release/evidence/v01-06/12-state-dir-blocker-fix.txt (815 passed / 0 failed / 19 ignored) — **closing (batch 3 item 3):** `docs/release/evidence/v01-06/12-state-dir-blocker-fix.txt:224` = `EXACT TOTALS: 815 passed / 0 failed / 19 ignored`, with the gate blocks at `:192` (`cargo check --workspace`), `:198` (clippy `-D warnings`), `:205` (`cargo test --workspace`) and the per-target breakdown at `:208-215`; `08-post-fix-quality-gates.txt` records all six gates re-run green after `543b526`. Historical counts for the candidate SHA — the current recorded baseline is **827/0/19** |
| #86 | [RC1][Freeze B3] Run PostgreSQL and SQLite integration suites on candidate SHA | 2026-09-15 · `gh issue close 86` | — (pre-existing on `main`; no new commit) | triage: docs/release/evidence/v01-runtime/providers/07-pg-integration-live.txt:1-33; providers/06-postgres-container-and-baseline.txt — **closing (batch 3 item 3):** `providers/07-pg-integration-live.txt` = `18 passed; 0 failed; 0 ignored` with `EXIT_STATUS=0` against the live fixture (`providers/06-…` = image `postgres:16`, host port 55432, `PostgreSQL 16.15`); post-test fixture state clean in `providers/09-…` (`003_verify.sql: PASS (exit 0)`); SQLite leg in `providers/08-…:372-408` = `integration.rs 32 passed; 0 failed` |
| #87 | [RC1][Freeze C1] Build release artifact matrix from exact candidate SHA | 2026-09-15 · `gh issue close 87` | — (pre-existing on `main`; no new commit) | triage: docs/release/risk-register.md:3-10 (run 34860902181, all nine jobs green); docs/release/0.1.0-packaging.md:19-30 — **closing (batch 3 item 3):** `docs/release/risk-register.md:3-10` = candidate `85a7fa3` with final run `34860902181` (nine jobs green; the three archives re-hashed on-host byte-for-byte against the run's `SHA256SUMS.txt`, provenance `candidate_sha` / `rustc 1.95.0` / `version 0.1.0`); `docs/release/0.1.0-packaging.md:19-32` = the artifact contract the matrix implements; candidate still an ancestor of `HEAD` |
| #90 | [RC1][Smoke A1] Prepare packaged-runtime smoke fixtures, credentials, and evidence worksheet | 2026-09-15 · `gh issue close 90` | — (pre-existing on `main`; no new commit) | triage: fixtures/smoke/README.md:1-40; docs/release/0.1.0-manual-smoke.md; docs/release/0.1.0-interactive-verification-runbook.md — **closing (batch 3 item 3):** `fixtures/smoke/README.md:1-40` + `docs/release/0.1.0-manual-smoke.md` (165 pass/fail rows, tallies `passed 0 / blocked 165 / failed 0`, authoritative candidate SHA recorded, readiness summary) + `docs/release/0.1.0-interactive-verification-runbook.md`; PG schema covers a PK table and `smoke_audit_log` with **no** PK, plus empty/Unicode/network tables; large-ER = 250 tables. **Preparation only** — no smoke run has been executed and no GUI-pass claim is made; the `checksums match #88` acceptance item still depends on the open **#88** |
| #132 | [RC1][Capability] Build PostgreSQL vs SQLite v0.1 capability/support matrix from source | 2026-09-15 · `gh issue close 132` | — (pre-existing on `main`; no new commit) | triage: docs/release/provider-capability-matrix.md:10 ('Issue: #132'), :14-33 (corrections + legend) — **closing (batch 3 item 3):** `docs/release/provider-capability-matrix.md:10` = `Issue: #132`; `:14-33` = the corrections note (Cancel-query row and key-difference summary un-inverted, `Sequences`/`Enum types` → `UNSUPPORTED`, retired Tauri dialog plugin removed, `VACUUM INTO` named) and the legend; cross-checked against `crates/core/src/domain/capabilities.rs` via `providers/15-cancellation-capability-gating.md` |
| #133 | [RC1][Smoke Prep] Create deterministic PostgreSQL/SQLite packaged-smoke fixture datasets | 2026-09-15 · `gh issue close 133` | — (pre-existing on `main`; no new commit) | triage: fixtures/smoke/README.md:1-40; fixtures/smoke/large-er/large_er_fixture.sql; fixtures/smoke/verify-smoke.sh — **closing (batch 3 item 3):** `fixtures/smoke/` on `main`: `postgres/001_smoke_schema.sql` (10 tables, 2 views, 8 indexes, 1 trigger, 1 function, 1 enum, 1 sequence), `002_smoke_seed.sql`, `003_smoke_teardown.sql`; `sqlite/smoke_fixture.sql` and `sqlite/runtime_fixture.sql` (60 users / 120 orders / 240 order_items); `large-er/large_er_fixture.sql` with `grep -c "CREATE TABLE"` = **250** plus `generate-large-er.js`; `verify-smoke.sh` mode 755, non-zero exit on any failed assertion |
| #135 | [RC1][Truth] Build canonical known-limitations and non-goals registry for v0.1 | 2026-09-15 · `gh issue close 135` | — (pre-existing on `main`; no new commit) | triage: docs/release/known-limitations.md:16 ('Issue: #135'), :35-287 (registry), :289-300 (summary by status) — **closing (batch 3 item 3):** `docs/release/known-limitations.md:16` = `Issue: #135`; `:35-287` = **18 `LIM-*` entries** (`grep -c '^## LIM-'` = 18); `:289-300` = summary by status, reconciled: 11 accepted v0.1 + 4 blocked decision + 3 deferred v0.2+ + 0 fix-before-v0.1 = 18, plus the rules block |

| #236 | [P1][Security] Editing or duplicating a connection silently downgrades its stored TLS mode | 2026-09-15 · `gh issue close 236` | `aba947b4` | **filed and fixed in the same session** — found while verifying the #144 triage, not covered by any earlier issue. **Defect (pre-fix):** `open_edit_connection` (`crates/ui/src/connection_view.rs:147`) and `open_duplicate_connection` (`:176`) hardcoded `UiSslMode::Disable` while `ConnectionSummary` (`crates/runtime/src/api.rs:127-137`) and `UiConnectionSummary` (`crates/ui/src/runtime.rs:304-313`) carried no `ssl_mode`, so `draft_to_domain` (`crates/native-app/src/translate.rs:11-16`) wrote `Disable` back over a stored `Require`/`VerifyCa`/`VerifyFull` on save; the same draft also fed Test Connection (`crates/ui/src/connection_view.rs:897,909`). Reproduced first: `cargo test -p db-pro-ui --lib -- preserves_its_stored_ssl_mode` → **0 passed; 2 failed; 359 filtered out**, `EXIT_STATUS=101` (`left: Disable / right: Require` and `/ right: VerifyFull`). **Fix:** `ssl_mode` added to both summaries and populated from the stored config, `ui_ssl_mode` reverse mapping added (`crates/native-app/src/translate.rs:55`), prefill now uses `connection.ssl_mode`; new-connection default, variant set and CA fields deliberately untouched (that is #144). Same command after the fix → **2 passed; 0 failed**. `cargo test --workspace` **829 passed / 0 failed / 19 ignored** (baseline 827/0/19, **+2**); fmt/check/clippy/release-build/perf-scan (`PASS`, 4/0/0) all green, exit 0. Evidence: docs/release/evidence/v01-runtime/providers/28-ssl-mode-edit-preservation.md — **not** a fix for #144: the default for new connections is still `Disable` and the TLS capability stays `SUPPORTED + NOT YET QUALIFIED` (`docs/release/provider-capability-matrix.md:42`), both unchanged here. |

| #54 | [Gate 5][A4] Lock Rust/Tauri/frontend provider-value DTO serialization contract | 2026-09-15 · `gh issue close 54` | `266e917` | **triage:** crates/core/src/domain/query.rs:3-20,53-84,305-347; crates/tauri-app/src/dto.rs:320-337,1102-1147 — **closing:** no test serialized `QueryResultDto` and nothing asserted the non-serde channel's field/class mapping. Now 4 tauri tests on one representative 15-column/two-row result covering every value class (tag list, NULL shape, camelCase key sets, precision-sensitive values as strings) pinned byte-for-byte to the generated fixture `crates/tauri-app/tests/fixtures/query-result-contract.json`, plus 2 native tests (`map_cell_keeps_one_ui_class_per_domain_value_class`, `query_result_translation_keeps_every_field_and_value_class`) pinning the shipping in-process mapping. `cargo test -p db-pro-tauri --lib` 25 → 29 passed; `cargo test -p db-pro-native` 14 → 16 passed; `cargo test --workspace` **862 passed / 0 failed / 21 ignored** (baseline 856/0/21). Evidence: docs/release/evidence/v01-runtime/providers/37-whole-result-dto-contract.md |

| #57 | [Gate 5][B3] Implement JSON/JSONB/UUID/BYTEA/network decoder paths | 2026-09-15 · `gh issue close 57` | `a153c1e` | **triage:** crates/infrastructure/src/postgres/query_mapper.rs:361-397,431-476; crates/infrastructure/tests/pg_integration.rs:246-290 — **closing:** the branches existed and were untested; `pg_query_decodes_structured_and_binary_value_classes` now asserts UUID canonical text, JSON/JSONB structured, BYTEA byte-exact (incl. `0xde 0xad 0xbe 0xef`), CIDR address+prefix and the `NULL::jsonb` vs `'null'::jsonb` distinction on the live fixture (PostgreSQL 16.15). Live suite **21 passed / 0 failed**; `cargo test --workspace` **862 passed / 0 failed / 22 ignored**. Evidence: docs/release/evidence/v01-runtime/providers/38-pg-structured-class-decoding.md |

| #58 | [Gate 5][B4] Implement enum/domain/array/INTERVAL/custom-type safe decoding | 2026-09-15 · `gh issue close 58` | `c7f4328` | **triage:** crates/infrastructure/src/postgres/query_mapper.rs:385-409,485-512; `grep 'CREATE DOMAIN' fixtures/` → no matches — **closing:** the fallback returned binary payloads as text (`int4range`, `text[]`, `tsvector` → mojibake) and failed the whole query for `money`/composite records; it is now format- and `PgTypeKind`-aware, so enum labels and character types stay canonical text while every other unsupported class is byte-exact `Bytes` and the row stays readable. 1 unit + 2 live tests; live suite **23 passed / 0 failed**, `cargo test --workspace` **863 passed / 0 failed / 24 ignored**. Evidence: docs/release/evidence/v01-runtime/providers/39-pg-unsupported-class-fallback.md |

| #59 | [Gate 5][B5] Add PostgreSQL integration fixture covering full decoder matrix | 2026-09-15 · `gh issue close 59` | `75865db` | **triage:** fixtures/postgres/001_schema.sql:7,26; crates/infrastructure/tests/pg_integration.rs:210-290 — **closing:** `decoder_matrix` (26 columns, every value class incl. domain/enum/array/range/composite) + domain/composite types added to the CI-loaded fixture, deterministic seed (populated row + NULL row) and `003_verify.sql` assertions; `pg_decoder_matrix_covers_every_value_class` asserts the whole expected-vs-actual matrix live. Live suite **24 passed / 0 failed**; `cargo test --workspace` **863 passed / 0 failed / 25 ignored**. Evidence: docs/release/evidence/v01-runtime/providers/40-pg-decoder-matrix-fixture.md |

| #53 | [Gate 5][A3] Define INTERVAL/network/enum/domain/array/custom-type fallback contract | 2026-09-15 · `gh issue close 53` | `dbfe1e1` | **triage:** crates/infrastructure/src/postgres/query_mapper.rs:385-409; docs/notes/PRODUCT_CAPABILITY_MATRIX.md:94 — **closing:** `docs/release/provider-value-contract.md` written and pointer-verified: the class matrix (incl. editability), the format-driven fallback contract, the explicit v0.1 exclusions, the serialization contract and the falsification map. Documentation-only; gates re-run green on the unchanged code. Evidence: docs/release/evidence/v01-runtime/providers/41-provider-value-contract-document.md |

| #237 | [P2][RC1][Introspection] Schema refresh fails when another session runs DDL concurrently | 2026-09-15 · `gh issue close 237` | `0d3aa84` | **filed and fixed in this session** — surfaced as CI red on `main @ 46b2bb5` (run 34900407192: `pg_introspect_tables … ColumnDecode { index: "definition", UnexpectedNullError }`), on a documentation-only commit, because CI runs `cargo test --all -- --include-ignored` and the parallel schedule shifted. **Defect:** `introspect_indexes` decoded `pg_get_indexdef(...)` as a non-nullable `String` (`introspect.rs:337`) and `introspect_check_constraints` did the same for `pg_get_constraintdef` (`:661`); both functions are evaluated with a *fresh* snapshot, so an index/constraint dropped by another session after the statement's snapshot returns NULL → the whole refresh failed. Measured: `pg_get_indexdef(<dropped oid>) IS NULL` = true; 80 indexes dropped from a second session while `introspect()` runs → fails in 0.65 s. A second form of the same race is a server-side error (`QueryFailed("cache lookup failed for attribute 1 of relation 17678")`). **Fix:** tolerant decoding at the two sites (`optional_string(…).unwrap_or_default()`, as triggers/views/functions already did) plus a bounded 3-attempt retry in `run_introspection` with 10/20 ms backoff, **only** for the narrow transient catalog messages (`cache lookup failed`, `could not open relation with OID`, `tuple concurrently updated`, `cached plan must not change result type`) — every other error is returned unchanged. New live test `pg_introspection_survives_concurrent_index_churn` asserts the mechanism and then that 40 concurrent index drops never fail a refresh; fails before / passes after. Gates: fmt/check/clippy exit 0, `cargo test --workspace` **863 passed / 0 failed / 26 ignored**, release build exit 0, perf-scan `PASS` (4/0/0), and the **CI-mirroring** `DATABASE_URL=… cargo test --all -- --include-ignored` → **889 passed / 0 failed / 0 ignored**, with the integration binary re-run in parallel three times green. Evidence: docs/release/evidence/v01-runtime/providers/42-introspection-under-concurrent-ddl.md |

| #66 | [Gate 5][D3] Run exact-head Rust/frontend/provider verification for type matrix | 2026-09-15 · `gh issue close 66` | `06e137e` | **triage:** docs/release/evidence/v01-06/08-post-fix-quality-gates.txt; providers/07-pg-integration-live.txt — **closing:** exact-head record on `main @ d24eb6a`: six gates green, live PG matrix **25/25**, CI-mirroring run **889/0/0**; the frontend block recorded as void (retired 2026-09-11) instead of summarized green; reviewer disposition explicitly not claimed. Evidence: docs/release/evidence/v01-runtime/providers/43-gate5-exact-head-verification.md |

| #23 | [Gate 5][Workstream B] PostgreSQL result decoder implementation | 2026-09-15 · `gh issue close 23` | — (no code change; the work landed under #55-#59) | **triage:** crates/infrastructure/src/postgres/query_mapper.rs:332-409, :402-409 — **closing (SATISFIED_NOW):** children #55/#56/#57/#58/#59 all closed, live integration suite re-run **25 passed / 0 failed**, CI-mirroring **889/0/0**. Evidence: docs/release/evidence/v01-runtime/providers/43-gate5-exact-head-verification.md |

| #64 | [Gate 5][D1] Verify PostgreSQL fixture -> domain decoder -> Tauri DTO end-to-end | 2026-09-15 · `gh issue close 64` | `483f3da` | **triage:** docs/release/evidence/v01-runtime/providers/07-pg-integration-live.txt:20-31; crates/tauri-app/README.md:1-10 — **closing:** one live test now joins the two halves (`live_fixture_query_survives_the_provider_to_dto_path` in `crates/tauri-app/src/dto.rs`): the fixture's 26-column row is decoded, crosses the domain representation and leaves as `QueryResultDto` JSON with the class matrix, the exact int64/numeric digits, the timestamp-vs-timestamptz distinction, the timetz offset, byte-exact binary, the fallback classes as bytes, NULL as `{"type":"null"}` and the declared provider types intact; a 52-cell sweep proves domain JSON == DTO JSON and a round trip back through `CellValueDto`. Green on the first run — coverage, not a defect. `cargo test --workspace` **872 passed / 0 failed / 27 ignored**; CI-mirroring **899 passed / 0 failed / 0 ignored**. Evidence: docs/release/evidence/v01-runtime/providers/45-provider-to-dto-end-to-end.md |

| #22 | [Gate 5][Workstream A] Canonical provider-value contract | 2026-09-15 · `gh issue close 22` | — (umbrella; no code of its own) | **triage:** crates/core/src/domain/query.rs:53-84; crates/tauri-app/src/dto.rs:320-337; docs/release/provider-capability-matrix.md:10 — **closing (umbrella, children verified):** A1 #51 (closed; three named tests re-run green), A2 #52 (`27c52db`), A3 #53 (`dbfe1e1`), A4 #54 (`266e917`) — all four tasks Done and the contract document re-opened with no pending item, so the issue's own completion rule is met. Stale branch/PR header noted as describing the retired workflow. Evidence: docs/release/evidence/v01-runtime/providers/46-temporal-dedicated-variants.md |

| #52 | [Gate 5][A2] Define DATE/TIME/TIMETZ/TIMESTAMP/TIMESTAMPTZ semantics | 2026-09-15 · `gh issue close 52` | `27c52db` | **triage:** crates/infrastructure/src/postgres/query_mapper.rs:365-384; crates/core/src/domain/query.rs:72-77 — **closing:** the five temporal classes now have dedicated variants (`Date`/`Time`/`TimeTz`/`Timestamp`/`TimestampTz`) instead of sharing `DateTime`/`Time`, so no consumer has to parse the canonical string to learn the semantics; the decoder emits them, `CellValueDto` mirrors them, the UI maps them to one `Text` class carrying the canonical string, export and the agent context render the canonical string, and binding goes through the shape-aware temporal parsers. The legacy `datetime` variant is retained for SQLite and pre-Gate-5 paths and no canonical string changed. New tests `temporal_classes_keep_their_own_tag_and_canonical_string`, `dedicated_temporal_cells_bind_as_typed_parameters`, `dedicated_temporal_variant_strings_bind_typed_or_fail_explicitly`; live suite **25/25**; workspace **875/0/27**; CI-mirroring **902/0/0**. Evidence: docs/release/evidence/v01-runtime/providers/46-temporal-dedicated-variants.md |

| #88 | [RC1][Freeze C2] Generate candidate evidence manifest and artifact checksums | 2026-09-15 · `gh issue close 88` | `326c313` | **triage:** .github/workflows/release.yml:282-283 (db-pro-provenance-*); docs/release/0.1.0-readiness.md:118-160 — **closing:** no in-repo manifest existed (`grep -rn 'evidence-manifest\|evidence_manifest'` → no matches). `docs/release/evidence-manifest.json` now ties the candidate `85a7fa3` to its pipeline run (`34860902181`, nine job ids re-read from the GitHub API), every gate result with the SHA it was observed at, the three archives + `SHA256SUMS.txt` + the three build artifacts with checksums and GitHub artifact ids, the provenance record, signing status, the P2 disposition counts and the 18-entry limitation registry. 9 contract tests (`crates/native-app/tests/evidence_manifest_contract.rs`) enforce the acceptance conditions — one SHA, no gate passing without evidence, a non-pass gate always carrying its reason, checksums 64-hex-or-unavailable-with-reason, `public_release_ready` unclaimable with blockers open, no dangling evidence pointer — and were falsified with three probes before being trusted. `cargo test --workspace` **872 passed / 0 failed / 26 ignored** (863/0/26 at session start); CI-mirroring run **898 passed / 0 failed / 0 ignored** (889/0/0). Evidence: docs/release/evidence/v01-runtime/providers/44-candidate-evidence-manifest.md |

| #79 | [RC1][P2-E] Audit performance tooling, bundle evidence, and release-measurement semantics | 2026-09-15 · `gh issue close 79` | `9e8c0f6` | **triage:** docs/release/0.1.0-readiness.md:41-72 (perf scan PASS); docs/release/evidence/v01-06/02-quality-gates.txt — **closing (§E):** the eight scope bullets each carry one disposition; the three `Fix RC1` rows are defects in the evidence tool, fixed in place (skipped sections now named and the status reads `PASS (partial)`; a warning exits 2 instead of 0; every run prints the source revision and the measured artifact's sha256; the budget tables now name only registered criterion ids). `--self-test` 14 assertions, falsified three ways; `cargo test --workspace` **875 passed / 0 failed / 27 ignored** (delta 0). Evidence: docs/release/evidence/v01-runtime/providers/47-perf-tooling-and-measurement-semantics.md |

| #240 | [P2][RC1][Tooling] perf-scan reports an unqualified PASS without naming what it did not run, and exits 0 on warnings | 2026-09-15 · `gh issue close 240` | `9e8c0f6` | **filed and fixed in the same pass** — surfaced by the #79 audit. Measured before the fix: `bash perf-scan.sh` printed `Status: PASS — All checks passed` while four sections were not executed, and `bash perf-scan.sh er` printed `WARN` with **exit 0**. Fixed in `.skills/perf-audit/` (script + SKILL.md + `references/perf-budgets.md`): counted and named skips, exit contract `PASS` 0 / `WARN` 2 / `FAIL` 1, provenance line with the measured artifact digest, corrected criterion ids. `--self-test` (14 assertions) is the regression test and was falsified with three probes. Evidence: docs/release/evidence/v01-runtime/providers/47-perf-tooling-and-measurement-semantics.md |

| #80 | [RC1][P2-F] Audit cross-cutting i18n/accessibility/error-state release polish | 2026-09-15 · `gh issue close 80` | `ebf085b` | **triage:** docs/release/known-limitations.md:245-257 (LIM-016) — **closing (§F):** nine scope bullets, one disposition each; six defects fixed under child #241 (invisible runtime feedback, the dead `Apply DDL` affordance, icon buttons announcing "Button", hover-gated `Shift+F10`, wrong shortcut labels, theme-drift colours); i18n settles as mechanism-absent; modal focus lifecycle and the glyph-only controls deferred with their sites. `cargo test --workspace` **878/0/27** (+3); three new tests falsified with two probes. Evidence: docs/release/evidence/v01-runtime/providers/48-cross-cutting-polish-audit.md |

| #241 | [P2][RC1][UX] Feedback, a false DDL affordance, icon-button names, keyboard menus, shortcut labels and theme drift | 2026-09-15 · `gh issue close 241` | `ebf085b` | **filed and fixed in the same pass** — surfaced by the #80 audit; six source-verified defects with the measured evidence (status-bar gate, `ddl_execute_confirmation` never true, `accessible_name` fallback, `has_focus` missing from the context-menu trigger, three shortcut vocabularies, eight opposite-theme RGB literals). Evidence: docs/release/evidence/v01-runtime/providers/48-cross-cutting-polish-audit.md |

| #122 | [RC1][Trust] Audit local-first security boundaries, credential handling, and unsafe execution paths | 2026-09-15 · `gh issue close 122` | `6446d94` | **triage:** docs/architecture/security-boundaries.md:11-40; crates/runtime/src/lib.rs:73-75; docs/release/risk-register.md:177-186 — **closing:** the trust-boundary matrix, the data-flow inventory and the per-finding severity/disposition table now exist in `docs/release/audit-security-boundaries.md`; no P0/P1; one P2 child issue (**#242**). Evidence: docs/release/audit-security-boundaries.md |

| #242 | [P2][RC1][Trust] The AI features are the app's only egress and nothing in the product says so | 2026-09-15 · `gh issue close 242` | `9a9f585` | **filed from the #122 audit (finding T-1), deliberately not patched** — the audit's own rule is to create focused child issues for proven gaps rather than patch behaviour in the audit task. Carries the evidence (automatic prediction egress with `Eager` as the default mode, the keyring-seeded API key, sample result rows in agent tool payloads, and the zero-hit disclosure grep) and its own acceptance: disclose the data flow in-app and in the release notes, and decide the prediction default — **Closing 2026-09-15 (pass 5, item 2):** the cited audit (`docs/release/audit-security-boundaries.md` §5, finding T-1: the AI path is the app's only egress and no UI text, README line or registry entry said so; `grep` for leaves-your-machine/sent-to/uploaded/third-party/privacy/outbound → 0 hits) — **Closing 2026-09-15 (pass 5, item 2):** the egress paths were re-established on the tree rather than inherited from the audit, which corrected one figure: the agent payload is bounded by `MAX_AGENT_SAMPLE_ROWS = 20` × `MAX_AGENT_RESULT_COLUMNS = 50` with cells truncated to `MAX_AGENT_CELL_CHARS = 256` (`crates/core/src/domain/agent.rs:10-12`), not the issue's "20×12". Egress requires a configured key — `worker.rs:1118-1127` answers "AI provider is not configured" and sends nothing — and `crates/runtime/src/agent.rs:8` is the workspace's only `reqwest` user, so "only egress" is a code fact, not a slogan. Disclosed in the existing muted-caption idiom with no new component: `AI_EGRESS_DISCLOSURE` in the agent panel's API-key section and `AI_PREDICTION_EGRESS_NOTE` under the editor's AI prediction Off/Subtle/Eager control. Registry entry **LIM-019** (all fields, summary 11 → 12 `Accepted v0.1`, no entry deleted), a bullet in `0.1.0-release-notes.md` §Known limitations, a paragraph in `README.md` §Agent provider, and the audit's T-1 row moved `Fix RC1` → `FIXED`. The prediction default is **kept at `Eager`** — a recorded decision the owner can reverse in one line plus the test that pins it — and the per-connection/per-session opt-out plan is stated as deferred to #34/#32. 4 tests assert on the **painted frame**, plus a no-privacy-claim guard; falsified twice (labels removed → 2 failures; invented "fully private" claim → the guard fired after being strengthened, which the evidence records). `cargo test --workspace` 895/0/27 → **899/0/27** (+4); CI-mirroring 922 → **926 passed / 0 failed / 0 ignored** (+4); fmt/check/clippy/release-build exit 0; perf-scan **PASS (partial)** exit 0.  |

| #243 | [P2][RC1][Trust] Secret store reports success when nothing was stored, orphans are invisible, and the agent key cannot be deleted | **CLOSED** 2026-09-15T03:27:47Z by the repository owner, **without a closing comment** (the pass-5 comment on this issue says "left open on three named remainders") | `e4a90c46` | **filed from the #126 audit (findings K-1/K-2/K-3/K-5), deliberately not patched** — the audit's own rule is source/test inspection unless a focused child fix is created. Carries the evidence (the swallowed `set_password`/`delete_credential` results, the row-missing delete path, the raw `keyring::Entry` agent-key bypass with no delete action) and its own acceptance **Pass-6 check (`main @ 2adc2324`): the three recorded remainders are still unaddressed on the tree** — (a) the session-only store still reports success with no warning channel (`crates/infrastructure/src/secret/keyring_vault.rs`, `with_session_fallback` `:62`; no `RuntimeEvent`/`UiEvent` warning variant exists); (b) the agent key still goes through a raw `keyring::Entry` in `crates/native-app/src/main.rs:210,225` with no "Forget key" action and no vault path; (c) the file fallback and the encryption path are still untested (`crates/infrastructure/src/secret/fallback.rs` and `encryption.rs` contain 0 `#[test]`). #243 is **not** one of the issues the merged batch claims to fix (the range `a9244a2..455f2718` references #147/#224/#234/#235/#238/#246/#248), so this pass does not reopen it — it records the live state in the ledger and posts the remainder as a comment on the issue instead. |

| #244 | [P2][RC1][Data Safety] Export is not atomic, PostgreSQL restore is not transactional, and backup cannot be cancelled | 2026-09-15 · `gh issue close 244` | `6574145` | **filed from the #128 audit (E-1/E-2/E-3), deliberately not patched** — the issue's rule is inspection-only. Each finding carries the pointer, the failure mode and its own acceptance (temp-file + rename for export; `--single-transaction` or an explicit partial-state disclosure for restore; wire or record cancellation) |

| #129 | [RC1][Data Safety] Audit destructive SQL, multi-statement execution, cancellation, and confirmation boundaries | 2026-09-15 · `gh issue close 129` | `458ec72` | **triage:** docs/release/evidence/v01-runtime/providers/15-cancellation-capability-gating.md — **closing:** the required 12-row execution-safety matrix and the parser/classifier inventory are in `docs/release/audit-execution-safety.md`; the query editor now holds a `Destructive` statement (or a script whose worst statement is destructive) for confirmation of the exact text before anything is dispatched; `docs/08-technology-decisions.md` and `docs/09-architecture-decisions.md` §5 no longer claim multi-statement execution is rejected; cancellation identity verified request-id-scoped; 4 falsified UI tests + 1 new core test; workspace **883/0/27**. Cross-referenced rather than absorbed: the table-editor `Apply DDL` capability call (left disabled by design) and backup/restore cancellation (#244) |

| #131 | [RC1][Traceability] Map every release-critical capability to tests, CI jobs, runtime smoke, and evidence owner | 2026-09-15 · `gh issue close 131` | `4eae4ad` | **triage:** docs/release/0.1.0-goal-3-traceability.md:40-103 — **closing:** the 25-row capability→tests→CI→smoke→owner matrix in `docs/release/audit-release-traceability.md`, with real test names, real CI job steps, runbook step numbers and the issue's six coverage words; open defects it makes visible are #238/#243/#244 and the host-blocked rows are #91–#95 |

| #120 | [RC1][Brand Prep] Inventory source-backed product differentiators and parity evidence | 2026-09-15 · `gh issue close 120` | `391091b` | **triage:** docs/notes/PRODUCT_CAPABILITY_MATRIX.md:1-45 — **closing:** the differentiator/parity inventory in `docs/release/brand-differentiators-inventory.md` with the five status classes, four source-backed wedges, the can-say/cannot-say table for #97 and the prohibited-overclaim list |
| #147 | [P1][RC1][Query] Decide and enforce multi-statement partial-write semantics | 2026-09-15 · closed by `9d5bebaa` (merged `f66d800d`); **closure re-verified in pass 6** | `49e87fe`, `9d5bebaa` | **the Agent-tool classification gap named in this issue is fixed** — `AgentSqlSafety::classify` (`crates/core/src/domain/agent.rs:371`) now reduces a batch to its most dangerous statement via `classify_script_safety` (`crates/core/src/domain/safety.rs:123`); the one statement splitter was moved into the domain layer (`crates/core/src/domain/safety.rs:135`) and is re-exported by `crates/core/src/application/sql_policy.rs:19`. Pre-fix: `crates/runtime/src/agent_executor.rs:689` returned `Ok(())` for `SELECT 1; DROP TABLE t;` with `allow_read_only_auto_run = true`. Remainder (issue stays open): recorded multi-statement contract in docs/release/known-limitations.md, the `docs/08-technology-decisions.md:90` / `docs/09-architecture-decisions.md:89-101` statements that still say multi-statement is disabled, and a `BEGIN`/`COMMIT`/`ROLLBACK` batch test. **Re-verified 2026-09-15 on `main @ bf87d75` and deliberately left open — no false closure.** `git merge-base --is-ancestor 49e87fe HEAD` exits 0; all 7 regression tests re-run green (`cargo test -p db-pro-core --lib -- <6 filters>` → 6 passed / 0 failed / 0 ignored; `cargo test -p db-pro-runtime --lib agent_executor::tests::mixed_batch_is_never_auto_run_before_execute_multi` → 1 passed / 0 failed); interactive path unchanged (`reject_multi_statement` `crates/core/src/application/sql_policy.rs:3`, `draft_requires_confirmation` `crates/runtime/src/agent.rs:678`). Implemented contract confirmed in code: `QueryService::execute_multi` (`crates/core/src/application/query_service.rs:186`) routes any batch containing a mutation through the atomic `execute_transaction` path (`:238`) while all-read scripts stay sequential. The issue's completion gate (`implemented/documented/tested` on one SHA **and** #129/#136/#131 updated to the same semantics) is met for *implemented* and *tested* only: `docs/release/known-limitations.md` has no statement/multi-statement entry at all (18 `LIM-*` entries, none for this), `docs/08-technology-decisions.md:90` and `docs/09-architecture-decisions.md:89-101` still say multi-statement execution is disabled, there is no `BEGIN`/`COMMIT`/`ROLLBACK` batch test, and #136/#131 are not updated. Status comment: https://github.com/truongnat/db-pro/issues/147#issuecomment-5668079479. **Pass-4 re-audit (2026-09-15, `main @ 3889970`): every criterion re-checked, and the one the previous pass listed as a missing *test* turned out to be a measured correctness gap — the issue stays open on an owner decision, not on documentation.** Met now: the contract is recorded (`docs/09-architecture-decisions.md` §5, Option B — mutating batches atomic through `Connector::execute_transaction`, read-only batches sequential), `docs/09` §5 and `docs/08` item 5 were corrected in `458ec72`, and the half-corrected `docs/08` `### Statement execution` bullet ("multi-statement execution is disabled by default") was corrected in this pass; #129 and #131 are closed with the same semantics (`docs/release/audit-release-traceability.md:36` names "#147 (contract)" as the owner of the row); the scenario tests exist for two reads, read→write, read→write→failure, DDL/write, quoted semicolons/comments and cancellation, with the provider rollback proven live by the probe's control case. **Not met, and not closable by a document:** executing the missing `BEGIN`/`COMMIT`/`ROLLBACK` batch case against the live fixture showed that a batch carrying its own `COMMIT` escapes the app-owned transaction — `execute_transaction(["BEGIN","INSERT …","COMMIT","SELECT no_such_column …"])` returns `phase=Statement index=3 outcome=RolledBack` **with 1 row surviving**, while the control (no explicit control) returns the same `outcome=RolledBack` with 0 rows. The envelope claims a rollback that did not happen, which is the very failure the issue was filed for. Recorded in `docs/release/audit-execution-safety.md` §3 (transaction-control **execution** row) rather than as a `known-limitations` entry, because that registry's own rule forbids filing a P0/P1 correctness gap as a harmless limitation. Closing needs one of three contract choices from the owner — reject transaction control in a batch (recommended: fails closed, mirrors `reject_multi_statement`, consistent with #224 being `OUT_OF_SCOPE_V01`), honour it, or weaken the atomicity claim — and #136 (owner-blocked on its own decisions) must carry the semantics either way. Status comment: https://github.com/truongnat/db-pro/issues/147#issuecomment-5672539559; register update on #136: https://github.com/truongnat/db-pro/issues/136#issuecomment-5672540752. No product code changed in this pass. Evidence: docs/release/evidence/v01-runtime/providers/51-multistatement-contract-criterion-audit.md; previous: docs/release/evidence/v01-runtime/providers/25-agent-multistatement-classification.md **Pass-6 re-verification (2026-09-15, `main @ d4df6373`, live PostgreSQL 16.15 fixture on 127.0.0.1:55432): the measured gap is closed and the measured behaviour now matches the documented contract, so this closure is confirmed rather than inherited.** The exact batch from the pass-4 measurement — `BEGIN; INSERT INTO … (id) VALUES (1); COMMIT; SELECT no_such_column …` — driven through the **product path** (`QueryService::execute_multi` over a real `PostgresConnector`, a real `SQLiteMetaStore` and a registry entry; temporary probe, deleted before the commit) returns **error index 0 naming `` `BEGIN` ``, `results_len=0`, `result_kinds_len=0`, and 0 rows surviving**, where pass 4 measured `RolledBack` at index 3 with **1 row surviving**; the control batch without transaction control still returns `transaction rolled back: …` with 0 rows, so the refusal replaced nothing. Committed tests re-run green on this tree: `cargo test -p db-pro-infrastructure --test pg_integration -- --ignored multi_statement` → **2 passed** (`pg_multi_statement_batch_with_commit_is_refused_and_writes_nothing`, `pg_multi_statement_batch_without_transaction_control_still_rolls_back`); `-- --ignored connector_alone` → **1 passed** (`pg_connector_alone_reports_rolled_back_for_a_batch_that_committed` — the pinned characterisation that the connector still lets a self-committing script through, which is exactly why the refusal exists one layer up); `cargo test -p db-pro-infrastructure --test multistatement_transaction_control` → **4 passed** (SQLite half). Dispatch rule: `crates/core/src/application/query_service.rs:226-256` (refuse before dispatch when a multi-statement batch carries `BEGIN`/`COMMIT`/`ROLLBACK`, classified by `transaction_control_verb`, `crates/core/src/domain/safety.rs:141`). Issue state on GitHub after this pass: **CLOSED** (`#147`, closed 2026-09-15T06:29:09Z by the implementing session). Evidence: docs/release/evidence/v01-runtime/providers/58-multistatement-transaction-control.md |
| #246 | [P2][RC1][Perf] Result-grid frame rebuilds a HashMap over every filtered row index | 2026-09-15 · closed by `71ca86c1` (merged `84e3746a`); **re-measured in pass 6, closure confirmed** | `71ca86c1` | **Filed by pass 5 out of #238's own measurement, not by an audit:** with the projection cached (#238), `draw_grid_body` still built `GridSelectionLookup` — two `HashMap`s, one entry per filtered row index plus one per column — on **every frame**, measured at **36.535 ms of a ~40 ms steady-state frame** at 200k rows × 4 columns in debug (`providers/53-result-grid-projection-cache.md` §7, probe removed before its commit). The lookup only changes when the projection or the visual column order changes. **Fix as merged:** `GridSelectionCache` (`crates/ui/src/result_grid_view.rs:47`) — the same one-entry take/restore idiom as `GridProjectionCache`, keyed on `(GridProjectionKey, column_order)` — with `draw_grid_body` and `handle_grid_keyboard` receiving the lookup borrowed instead of rebuilding it. **Pass-6 re-measurement (2026-09-15, `main @ d4df6373`, debug, 200k × 4, temporary probe in the crate's test module, deleted before the commit; three runs):** `GridSelectionLookup::new(200k, 4)` = **40.887 / 39.225 / 62.811 ms** (the issue's figure was 36.535 ms), frame 1 (miss → rebuild) = **36.469 / 39.361 / 43.316 ms**, and steady state over 300 frames = **0.115 / 0.054 / 0.053 ms total → 0.000383 / 0.000181 / 0.000176 ms per frame**, with `rebuilds()` frozen at **1**. So the frame cost the issue measured is gone: the build is paid once per (projection, column order) instead of once per frame. Committed tests that pin it: `selection_cache_reuses_the_lookup_across_frames`, `selection_cache_rebuilds_when_the_projection_or_the_column_order_changes`, `selection_cache_keeps_a_newer_entry_over_the_frame_copy`, `result_grid_rebuilds_the_selection_lookup_when_the_column_order_changes` (through the real draw path), plus the criterion group `result_grid_selection_lookup` (`build_200k_rows_4_columns`) which measures the avoided rebuild against the public API. Issue state on GitHub: **CLOSED** (2026-09-15T03:08:35Z, by the implementing session); pass 6 posts the independent before/after measurement as its own comment. Evidence: docs/release/evidence/v01-runtime/providers/57-result-grid-selection-lookup-cache.md |

## PARTIAL_ON_MAIN bucket — disposition of the 2026-09-15 run

The bucket was re-verified row by row against the live tree (not against the triage snapshot) and each
row classified into exactly one bucket class. 50 rows were in the bucket; the table below accounts for
all of them.

| Class | Count | Issues |
|---|---:|---|
| `ACTIONABLE` — implemented, verified, committed, pushed and closed in this run | 13 | #53, #54, #57, #58, #59, #66, **#88**, **#64**, **#52**, **#75**, **#76**, **#77**, **#78** |
| `SATISFIED_NOW` — remainder re-verified as present; closed with a re-verified pointer | 1 | #23 |
| `BLOCKED_OWNER` — needs a product/governance decision; **left open, blocker comment posted** | 23 | #14, #21, #24, #25, #26, #27, #28, #30, #31, #67, #69, #70, #73, #81, #82, #89, #96, #105, #106, #107, #110, #136, #141 |
| `BLOCKED_EXTERNAL` — needs a host, GUI session or publication this environment does not have; **left open, blocker comment posted** | 4 | #29, #91, #94, #145 |
| `SUPERSEDED_CANDIDATE` — awaiting owner confirmation | 0 | — (see the near-misses below) |
| `ACTIONABLE`, classified but **not reached** in this run (no state change; still `PARTIAL_ON_MAIN`) | 8 | #79, #80, #120, #122, #126, #128, #129, #131 |

Post-triage arrivals this run: **#237** (filed from the CI failure on `main @ 46b2bb5`, fixed in `0d3aa84`,
closed) — recorded in "Closed by this workstream" and the Revisions table; it was never an open ledger row.

**Near-misses reported to the owner instead of being reclassified as `SUPERSEDED_CANDIDATE`:** #67's
"merge the verified Gate 5 PR with `expected_head_sha` protection" step and #25's completion rule
("the accepted PR head is merged/verified on main") describe a PR-merge workflow this repository no
longer uses — Gate 5 work lands directly on `main`, and the exact-head verification is now recorded in
`providers/43-gate5-exact-head-verification.md`. Their substance remains (the P1 = 0 declaration for #67,
the verification children for #25), so both stay open with a blocker comment rather than being closed as
superseded; the owner may prefer to restate their acceptance for the main-only workflow.

**Not reached, in the order this run would have continued:** #64 (provider → domain → DTO in one live test), #52 (dedicated `timestamp`/`timetz`/
`timestamptz` variants — the last item blocking #22/#24), #22, #24, #21, #25 (unblocked by #52/#64/#67),
then the documentation audits #75-#80, #122, #126, #128, #129, #131, and #120.
(#88 was first in that order and is now closed — see the pass-2 table below.)


## ACTIONABLE queue — pass 2 (2026-09-15)

The 19 rows the previous pass classified `ACTIONABLE` but did not reach are being worked in the
order the previous pass proposed, one issue per commit. Each row is re-verified against the live
tree before it is implemented, re-classified or closed; nothing is closed by assumption.

| # | Proposed order | Outcome |
|---:|---:|---|
| #88 | 1 | **implemented + closed** — `docs/release/evidence-manifest.json` + 9 contract tests in `crates/native-app/tests/evidence_manifest_contract.rs`; evidence `providers/44-candidate-evidence-manifest.md` |
| #64 | 2 | **implemented + closed** — live provider→domain→DTO join `live_fixture_query_survives_the_provider_to_dto_path` in `crates/tauri-app/src/dto.rs`; evidence `providers/45-provider-to-dto-end-to-end.md` |
| #52 | 3 | **implemented + closed** — dedicated `timestamp`/`timestamptz`/`timetz` variants across domain, decoder, DTO, UI mapping, export and binding; evidence `providers/46-temporal-dedicated-variants.md` |
| #22 | 4 | **umbrella: children verified on `main`, then closed on its own completion rule** — A1 #51, A2 #52, A3 #53, A4 #54 each re-checked (tests re-run, document and fixture re-opened) |
| #24 | 5 | **left open with a blocker comment** — C2/C3 done and re-verified, but C1 (#60) and C4 (#63) are still open and scoped to the retired React frontend; disposition is a scope call |
| #21 | 6 | **epic: left open with a status comment** — A and B closed and re-verified, C blocked-owner (#24), D (#25) re-verified next |
| #25 | 7 | **left open with a blocker comment** — D1/D3 done and re-verified, but D2 (#65) and D4 (#67) are open and owner-blocked |
| #75 | 8 | **implemented + closed** — per-row release dispositions for the eight Workspace/Shell P2 findings in `docs/release/rc1-p2-release-dispositions.md` §A |
| #76 | 9 | **implemented + closed** — dispositions for the six Data Grid rows (§B); one `Fix RC1` spawned **#238** (measured per-frame allocating sort) |
| #77 | 10 | **implemented + closed** — dispositions for the seven connection/session rows plus the SSH scope item (§C); one `Fix RC1` spawned **#239** (no in-UI SSH qualification caveat) |
| #78 | 11 | **implemented + closed** — dispositions for the four Query/ER rows (§D); no `Fix RC1`, no child issue |

## ACTIONABLE queue — pass 3 (2026-09-15)

The eight rows pass 2 classified `ACTIONABLE` but did not reach, in the order pass 2 recorded, one
issue per commit. Each row is re-verified against the live tree before it is implemented, re-classified
or closed; nothing is closed by assumption. This table grows as the pass proceeds — the tail that has
not been started is named, never pre-filled.

| # | Order | Outcome |
|---:|---:|---|
| #79 | 1 | **audit complete + closed** — dispositions for the eight perf/tooling scope rows in `docs/release/rc1-p2-release-dispositions.md` §E; three `Fix RC1` rows fixed in place under child **#240** (perf-scan partial-pass wording, warn exit code, provenance + budget-table drift); `--self-test` 14 assertions; evidence `providers/47-perf-tooling-and-measurement-semantics.md` |
| #80 | 2 | **audit complete + closed** — nine scope bullets dispositioned in `docs/release/rc1-p2-release-dispositions.md` §F; six `Fix RC1` rows fixed in place under child **#241**; evidence `providers/48-cross-cutting-polish-audit.md` |
| #122 | 3 | **audit complete + closed** — `docs/release/audit-security-boundaries.md` (data-flow inventory, credential matrix, execution and host boundaries, findings by severity); one P2 child issue **#242** filed and left open |
| #126 | 4 | **audit complete, issue left open with one blocker comment** — lifecycle matrix + findings in `docs/release/audit-keyring-secret-lifecycle.md`; child **#243** filed; the packaged per-platform proof the review gate requires is blocked externally (#92/#93, #91, `R-WINLINUX`) |
| #128 | 5 | **audit complete + closed** — `docs/release/audit-data-integrity.md` (per-operation classification, partial-failure table, test inventory); the capability-matrix export/backup rows corrected in the same commit; three code gaps filed as child **#244** |
| #129 | 6 | **audit complete + closed** — `docs/release/audit-execution-safety.md` (12-row matrix + test inventory); the destructive-statement hold and dialog landed with 4 falsified UI tests; docs/08 + docs/09 §5 corrected |
| #131 | 7 | **audit complete + closed** — `docs/release/audit-release-traceability.md` (25 rows, six coverage words, gaps assigned to #238/#243/#244 and #91–#95) |
| #120 | 8 | **audit complete + closed** — `docs/release/brand-differentiators-inventory.md` (five status classes, source-backed wedges, can-say/cannot-say table, prohibited-overclaim list) |

**All eight rows of the pass-3 queue were reached and closed** (#79, #80, #122, #126-keep-open, #128, #129, #131, #120); #126 is the only one left open, on its own external host blocker, with the audit complete and #243 filed from it.

## ACTIONABLE queue — pass 4 (2026-09-15)

The last three rows this queue still holds, in the order the pass recorded them (**#238 → #239 →
#147**) [pass 4 — completed; pass 5 starts from #238's merge-closure and continues with #242, #243, #244], one issue per commit, each re-verified against the live tree before any decision is taken. As
in pass 3 this table grows as the pass proceeds — the tail that has not been started is named, never
pre-filled. This pass runs **main-only** (no branches, no PRs, no checkouts), so work already claimed
on another branch is verified and reported, never duplicated and never merged on someone else's behalf.

| # | Order | Outcome |
|---:|---:|---|
| #238 | 1 | **verified live, claimed elsewhere, left open** — open PR #245 (`a0452a32`, branch `fix/rc1-p2-result-grid-sort-perf-…`) is **not** an ancestor of `main @ ba2f515` and `main`'s history has no #238 commit, but the defect itself was re-measured on `main` with a temporary probe (removed before the commit): 200k rows × 4 columns → 5.29 ms unsorted / 166.6 ms text-sorted / **6,649.6 ms temporal-text-sorted** / 72.9 ms filter+sort. No product code changed — the work is another session's to land; the branch covers the issue's option (b) but not option (a) or acceptance items 1 and 3, which the owner needs before merging. Evidence `providers/49-result-grid-sort-claim-verification.md` |
| #239 | 2 | **fixed + closed** — one muted caption in the SSH card states the LIM-006 qualification ("Unqualified in v0.1: the tunnel has not been end-to-end tested and may not work reliably"), below the checkbox and outside the `if ssh_tunnel_enabled` block, in the dialog's own muted-caption idiom; three tests pin the painted frame (falsified twice), no behaviour change; `cargo test --workspace` 883/0/27 → **886/0/27**; CI-mirroring 910 → **913 passed / 0 failed**; commit `ccc0a24`, pushed, `gh issue close 239`; evidence `providers/50-ssh-tunnel-ui-qualification-caveat.md` |
| #147 | 3 | **re-audited, left open on a measured correctness gap** — every criterion checked; contract recorded (docs/09 §5), docs/08's second stale bullet corrected in this pass, scenario tests present for reads/writes/DDL/splitting/cancellation, #129 and #131 closed with the same semantics. The missing `BEGIN`/`COMMIT`/`ROLLBACK` batch test was written as a live probe and showed a batch's own `COMMIT` escapes the atomic wrapper: `outcome=RolledBack` reported **with 1 row surviving** (control: 0 rows). Recorded in the execution-safety matrix, not as a `known-limitations` entry (the registry's own rule forbids hiding a P1 correctness gap); closing needs the owner to pick reject / honour / weaken (status comment https://github.com/truongnat/db-pro/issues/147#issuecomment-5672539559; #136 register update https://github.com/truongnat/db-pro/issues/136#issuecomment-5672540752). No product code changed; evidence `providers/51-multistatement-contract-criterion-audit.md` |

| 2026-09-15 | **#243** | `ACTIONABLE` → **bounded slice fixed + pushed, issue left OPEN** on two named remainders (commit `fix(secret): report a store that stored nothing and a delete that did not take effect (#243)`) | Pass 5, item 3 of the queue. The vault half of K-1/K-2 is fixed in `crates/infrastructure/src/secret/keyring_vault.rs` with no port change and no new component: `store_secret` now tracks whether *any* layer accepted the value and returns `Err` when none did (a keyring refusal is logged at `warn` with its consequence instead of dropped), and `delete_secret` collects per-layer failures and returns `Err` naming the orphan risk, treating `keyring::Error::NoEntry` as the goal state of a delete. Both are already visible to callers: every `store_secret` site in `connection_service` propagates with `?` or rolls back, and `delete` returns the error to the UI. **The seam that made the failure paths testable without a production change**: `keyring::Entry::new("", "k")` fails with `Invalid("service", "cannot be empty")` on this host (verified with a throwaway probe, deleted before the commit), so a vault built with an empty service name has no usable keyring layer — which removes the environment dependence that made the old `missing_os_keyring_secret_returns_none_without_fallback` an accept-either assertion. Five tests (store-persisted-nothing, delete-could-not-persist, missing-key delete stays a success, unreachable-store retrieval is an error not a silent `None`, plus a characterisation test of the session-only behaviour that is deliberately unchanged); falsified with two probes — reverting either result to `Ok(())` fails exactly the one test that claims it (2 failed / 8 passed with both reverted). Gate: `cargo test --workspace` 899/0/27 → **904/0/27** (+5); CI-mirroring 926 → **931 passed / 0 failed / 0 ignored** (+5); fmt/check/clippy/release-build exit 0; perf-scan **PASS (partial)** exit 0. **Not fixed, and why the issue stays open:** (a) the *session-only* store still reports success — delivering "saved, but this will not survive a restart" needs a warning channel the app does not have (`RuntimeEvent`/`UiEvent` have no warning variant, `ToastVariant` is Default/Success/Danger, `OperationCompleted` is built at 12 sites) and the choice between that warning and a **hard failure** is a product decision with user-visible consequences on systems without a keyring, which this pass does not make; (b) K-3, the agent key lifecycle — `agent/groq_api_key` still goes through a raw `keyring::Entry` in `native-app/src/main.rs:205-234` with no "Forget key" action and no vault path, which is a feature slice (new `UiCommand`/`UiEvent` pair + panel action) rather than a bounded patch; (c) the rest of K-5 (a denying-but-reachable keyring, an edit that must keep the secret, restart retrieval, a corrupt fallback file; `fallback.rs`/`encryption.rs` still untested). The Linux persistence decision is out of scope by the issue's own text. Evidence: `docs/release/evidence/v01-runtime/providers/55-secret-store-honesty.md` |

| 2026-09-15 | **#244** | `ACTIONABLE` → **implemented + closed** (commit `fix(export): publish atomically, state the restore's partial state, record the missing cancel (#244)`) | Pass 5, item 4 of the queue. All three #128-audit findings dispositioned **inside the options the issue itself offers**. **E-1 fixed**: `std::fs::write` straight at the destination is replaced by `write_file_atomically` (sibling temp file → `sync_all` → rename, scratch removed on failure), the export dialog asks before replacing an existing path (`export_overwrite_pending`, "Overwrite" / "Keep existing file"), and a capped result is named as capped instead of reading as a complete export; 6 tests, one of which — `export_replaces_the_destination_instead_of_truncating_it` — exists because the first version of the suite tested the helper and left the caller unguarded, so reverting `export_result_to_disk` to `std::fs::write` stayed green (recorded, and fixed with an inode assertion). **E-2 takes option (b)**: the restore argv is extracted into `PgDumpEngine::restore_command` so it is testable without a live server (the issue asks for exactly that), the argv still carries no `--single-transaction`/`--exit-on-error` **by decision**, and the failure now states the consequence — "the restore was NOT run in a transaction, so the target database may now be partially restored; inspect it before using it (documented in LIM-020)" — instead of only relaying the subprocess error. Option (a) is **not** taken and is recorded as the owner's compatibility call (dumps with non-transactional statements would fail as a whole). **E-3 takes the second accepted option**: no Stop affordance is promised; `CancelOperation` is still declared at `worker.rs:205`, handled at `:1563` and constructed nowhere (re-verified by grep), and the absence is now stated in the release notes and the registry on top of the matrix row that already carried it. Docs: matrix rows updated (restore, export-atomicity, cancellation, backup), **LIM-020** added in `known-limitations.md` with every registry field (summary 12 → 13 `Accepted v0.1`, no entry deleted), three bullets in `0.1.0-release-notes.md` §Known limitations. Gates: `cargo test --workspace` 904/0/27 → **911/0/27** (+7 = 6 export + 1 restore-argv test); CI-mirroring 931 → **938 passed / 0 failed / 0 ignored** (+7); fmt/check/clippy/release-build exit 0; perf-scan **PASS (partial)** exit 0 (4 passed / 0 warnings / 0 failed). Three probes, all reverted: non-atomic write at the helper, non-atomic write at the caller, and `--single-transaction` in the argv. Evidence: `docs/release/evidence/v01-runtime/providers/56-export-atomicity-and-restore-state.md` |

## ACTIONABLE queue — pass 5 (2026-09-15)

Pass 4's queue was emptied (#238, #239, #147) and two stale PRs were closed by the coordinator, so this
pass started from the merge that auto-closed #238 and from the three P2 follow-ups the earlier audits
had filed (#242, #243, #244), one issue per commit.

| # | Order | Outcome |
|---:|---:|---|
| #238 | 1 | **re-verified, gap implemented, closure confirmed** — PR #245 merged as `00a3ce9` and auto-closed the issue, but the merged diff implemented only the issue's option (b) (`result_grid.rs`/`lib.rs`: allocation-free comparator, ISO-shape gate, ASCII filter fast path) and left acceptance items 1 and 3 unmet: the draw path still rebuilt the projection every frame and no sorted-path benchmark existed. Re-measured on `main @ 9a8dc5b` (debug, 200k × 4): 5.50 ms unsorted / 11.60 ms plain-text-sorted (14× better than pass 4's 166.6 ms — the #245 gate does work) / **10,528.0 ms temporal-text-sorted** / 98.97 ms filter+sort, per frame. Implemented the missing part in `7c356ff`: `GridProjectionCache` keyed on (epoch, filter, sort column, direction, row count, column count), the epoch advanced at every site where displayed rows change, draw-path take/restore, header auto-size reuse. **Measured through the real draw path, cache off vs on in the same build: 10,652 / 10,651 / 10,623 ms per frame → 10,731 ms on the first frame only, then 41.1 / 40.0 / 42.7 / 39.8 ms.** Criterion group `result_grid_projection_sorted` added (2.53–2.59 / 566.8–576.1 / 4.44–4.58 ms, release); 6 tests, two through the draw path, falsified twice. Residual found and filed as **#246** (36.5 ms of the ~40 ms steady-state frame is `GridSelectionLookup::new`). Evidence `providers/53-result-grid-projection-cache.md` |
| #242 | 2 | **fixed + closed** — the AI data flow is disclosed in-app (agent panel API-key section, editor AI prediction control, both muted captions in existing idioms) and in the registry (**LIM-019**), release notes and README; the audit's stale "20×12" payload bound corrected to the code's 20 rows × 50 columns × 256 chars; the prediction default is **kept at `Eager`** as a recorded decision the owner can reverse in one line, and the opt-out plan is stated (per-connection/per-session deferred to #34/#32). 4 tests reading the **painted frame** plus a no-privacy-claim guard, falsified twice — the second probe exposed a weak first version of the guard, which is recorded. `cargo test --workspace` 895/0/27 → **899/0/27**; CI-mirroring 922 → **926 passed / 0 failed**. Commit `9a9f585`, pushed, `gh issue close 242`; evidence `providers/54-ai-egress-disclosure.md` |
| #243 | 3 | **bounded slice fixed, issue left OPEN on three named remainders** — the vault half of K-1/K-2: `store_secret` reports when no layer accepted the value (and logs a keyring refusal at `warn` with its consequence), `delete_secret` reports a delete that did not take effect and counts `NoEntry` as success. The failure paths are testable because `keyring::Entry::new("", "k")` fails deterministically ("service cannot be empty"), so no production seam was needed. 5 tests, falsified with two probes. Gates 899/0/27 → **904/0/27**; CI-mirroring 926 → **931**. Commit `e4a90c4`, pushed. **Open remainders:** the session-only store still reports success (needs a warning channel and an owner decision between "saved, but not durable" and a hard failure), K-3 (the agent key still bypasses the vault and cannot be deleted), and the rest of K-5. Evidence `providers/55-secret-store-honesty.md` |
| #244 | 4 | **fixed + closed** — all three findings dispositioned inside the options the issue offers. **E-1** atomic export: sibling temp file → `sync_all` → rename, scratch removed on failure, overwrite confirmation, capped results named as capped (6 tests, including the failure case the issue asked for). **E-2** option (b): the restore keeps running without `--single-transaction`, the argv is now testable without a live server, and a failed restore states that the target may be partially restored instead of only relaying the subprocess error; option (a) recorded as the owner's compatibility call. **E-3** the second accepted option: no Stop affordance promised, the absence stated in the release notes and **LIM-020**. `cargo test --workspace` 904/0/27 → **911/0/27**; CI-mirroring 931 → **938 passed / 0 failed**. Commit `6574145`, pushed, `gh issue close 244`; evidence `providers/56-export-atomicity-and-restore-state.md` |
| — | 5 | **re-scan: no further ACTIONABLE item reachable in this pass** — see the section below |

### Re-scan of the open queue (pass 5, item 5)

`gh issue list --state open --limit 400` returned **115** issues. Every one was classified again, and
the pass-2→4 `ACTIONABLE` queue is **empty**: nothing new became actionable from the two closed PRs,
the #245 merge or the decoder work, because those touched #238 (now closed) and the PostgreSQL
decode path (already closed under #55/#58/#59/#64).

| Group | Count | Disposition |
|---|---:|---|
| Post-v0.1 product scope, Phase A–H | 58 | Out of scope per this pass's rules (#182–#235) |
| Release-workstream gates (freeze/tag/publish/smoke/brand) | ~35 | Blocked on a candidate freeze and on hosts this environment does not have (#91–#95 packaged runtime); brand and licence items (#97–#104, #119) are owner decisions |
| Owner decisions | 8 | #68 (CHECK disposition, with #69–#71, #73 depending on it), #147 (multi-statement partial-write semantics), #126 (Linux keyring backend), #136, #112, #113, #110, #119 |
| Audit/closure bookkeeping | 4 | #67, #73, #81, #82 — each blocked by a decision or by a freeze (#82 depends on #81 plus every spawned fix ticket) |
| External resources | 3 | #84 (frontend gates on the archived React frontend), #63/#65 (Tauri-era Gate 5 rows) |
| Gate 4 ER harness | 2 | #115, #116 — `Ready to Pick` in the old tree but written against `feature/er-large-schema` / PR #36, neither of which exists in this repository |
| Filed by this pass | 3 | **#246** (see below), #243 (own remainders, above) |

**The one genuinely actionable item is #246** — `GridSelectionLookup::new` building a `HashMap` over
every filtered row index on every frame, measured at **36.5 ms of a ~40 ms steady-state frame** at 200k
rows in debug, with the fix direction (memoize on the projection key + column order, or replace the map
with a direct lookup) and the acceptance already written in the issue. It is **not started in this
pass**: the re-scan is the last item of the budget, and starting a product change without the room to
run the full gate set would leave uncommitted work. It is the first row of the next pass.

## Status after the phase-2 workstream (pass 5 — historical; superseded by the pass-6 block below)

The numbers in this block were correct when pass 5 wrote them (115 live open issues on 2026-09-15) and are kept unchanged as the historical record; the pass-6 block below carries the current counts and the invariant check.

Counts over all 155 rows (snapshots 1–3), refreshed in every ledger commit of the phase-2 pass. The invariant this file keeps: **open ledger rows equal the live open-issue count.**

| Status | Count | Note |
|---|---:|---|
| Open rows still in the triage table | 113 | 136 from snapshots 1–2 plus the 8 snapshot-3 arrivals (#228–#235), minus the rows closed 2026-09-15 by this session (#22, #23, #52, #53, #54, #56, #57, #58, #59, #61, #62, #64, #66, #72, #75, #76, #77, #78, #88, #102, #114, #121, #127, #134) and by pass 3 (#79, #80, #120, #122, #128, #129, #131) |
| — live open issues on 2026-09-15 (`gh issue list --state open --limit 400`) | **115** | **equal to the open-row count above** — 113 triage-table rows plus the **two** open post-triage arrivals (**#243**, **#246**). Pass 5 closed #242 and #244 and filed #246; #238 was auto-closed by the #245 merge. Verified with `gh issue list` after the #244 closure and again after this commit. **#238 was auto-closed on 2026-09-15T00:41:15Z by the merge of PR #245** (`00a3ce9`), which is not this workstream's action; #239 was closed by pass 4, and pass 5 closed #242 and #244 and filed #246. Of the **165** ledger rows (155 snapshot rows + the ten post-triage issues #236–#244 and #246), **50 are closed and 115 open**, which is the live count above |
| Closed by this workstream | 47 | #142, the ten re-verified `DONE_ON_MAIN` rows, #236, #237, #22, #23, #52, #53, #54, #57, #58, #59, #64, #66, #75, #76, #77, #78, #88, the nine session closures recorded below, pass 3's #79 + #240, #80 + #241, #122, #128, #129, #131 and #120, and pass 4's **#239**. A 48th ledger row is closed without this workstream acting: **#238**, auto-closed by the merge of PR #245 (see the row above) — pass 5 re-verifies it against the merged tree rather than counting it as this workstream's closure |
| — of which `DONE_ON_MAIN` closed with re-verified evidence | 10 | confirmation pass completed 2026-09-15: every pointer re-opened on `main @ e0bfc56` and checked against the live file/identifier before `gh issue close` |
| Post-triage issues (never an open row at any snapshot) | 9 | #236 (filed from the #144 verification, `aba947b4`), **#237** — filed from the CI failure on `main @ 46b2bb5` (run 34900407192) and closed in the same session — **#238** (from the #76 audit), **auto-closed 2026-09-15T00:41:15Z by the merge of PR #245** (`00a3ce9`) and re-verified in pass 5 against the merged tree, **#239** (from the #77 audit), **closed by pass 4**, **#240** (from the #79 audit) and **#241** (from the #80 audit), both fixed and closed in the same pass; **#242** (from the #122 audit), **#243** (from the #126 audit) and **#244** (from the #128 audit), all filed and **left open** as follow-ups; all nine are absent from `issues-open-2026-09-15.json` |

## Status after pass 6 (2026-09-15 evening)

Counts over all **203** issue rows (snapshots 1–3: 155 · snapshot 4: 38 · post-triage issues that were never an open row at any snapshot: 10 — #236, #237, #238, #239, #240, #241, #242, #243, #244, #246). The invariant this file keeps: **open ledger rows equal the live open-issue count.**

| Status | Count | Note |
|---|---:|---|
| Open rows in the triage table | **150** | 155 snapshot rows + 38 snapshot-4 rows, minus the rows whose status is `CLOSED`/`DONE_ON_MAIN` in the table (33, of which the rows closed on 2026-09-15 are #22, #23, #52, #53, #54, #56, #57, #58, #59, #61, #62, #64, #66, #72, #75–#80, #88, #102, #114, #120, #121, #122, #127, #128, #129, #131, #134, #239 recorded in earlier passes), **plus** #234 and #235, which pass 6 re-opened after finding their acceptance unmet |
| — live open issues (`gh issue list --state open --limit 400`, 2026-09-15 evening) | **150** | **equal to the open-row count above** — verified after the last push of the pass with the same command, and the snapshot `issues-open-2026-09-15-evening.json` carries exactly those 150 rows |
| Rows in the triage table with a `CLOSED` status marker | 33 | kept in place as the historical triage record; the closing reference for each is in the section below (the file's rule is that the triage content is never deleted) |
| Closed by this workstream (section below) | 37 | includes pass 6's `#147` (closure re-verified rather than inherited) and `#246` (fix re-measured); #243 is recorded there with its remainders still open on the tree and #238 with its status corrected. Rows that were moved here are not double-counted as open |
| Post-triage issues (never an open row at any snapshot) | 10 | #236 (from the #144 verification), #237 (from the CI failure on `main @ 46b2bb5`), #238 (from the #76 audit; auto-closed by the PR #245 merge, closure re-verified in pass 5), #239 (from the #77 audit), #240 (from the #79 audit), #241 (from the #80 audit), #242 (from the #122 audit), #243 (from the #126 audit), #244 (from the #128 audit), #246 (filed by pass 5 as #238's residual and closed by the #246 fix) |
| Gate health | — | `main` was **not** gate-clean at the start of pass 6 (`fmt` red at `455f2718`); three failures were fixed in `29027814`, `6afc5ba6`, `f2a36b4b` and `d4df6373`. All six gates plus the CI-mirroring run are green at `d4df6373`. Evidence: `docs/release/evidence/v01-runtime/providers/59-main-gate-health-pass6.md` |


## Status after pass 8 (2026-09-15)

Counts are unchanged from pass 7 because pass 8 closed no issue and opened none. The invariant this file
keeps: **open ledger rows equal the live open-issue count.**

| Measure | Count | Note |
|---|---:|---|
| Open rows in the triage table | **150** | unchanged by pass 8; #234 and #235 were already open rows (re-opened in pass 6) and stay open |
| Live open issues (`gh issue list --state open --limit 500`, 2026-09-15 after the pass-8 commits) | **150** | **equal to the open-row count above** — verified with the command after the last push of the pass |
| Issues closed by pass 8 | 0 | #234's criteria 1 and 6 are now met but 2/3/4/5 are partial, so it stays open rather than closing on partial credit |
| Gate health | — | all six gates plus the CI-mirroring run are green at the pass-8 change head `46cfed05`: `cargo test --workspace` **952 passed / 0 failed / 35 ignored**, `cargo test --all -- --include-ignored` with the PostgreSQL fixture live **987 / 0 / 0**, `fmt`/`check`/`clippy -D warnings`/release build/perf-scan exit 0. Evidence: `docs/release/evidence/v01-runtime/providers/61-provider-capability-truthfulness.md` §5 |

## Revisions

| Date | Issue(s) | Change | Reason |
|---|---|---|---|
| 2026-09-14 | all 136 | Initial triage recorded | Phase 1 of "work through all open issues" |
| 2026-09-15 | #146 | `NEEDS_OWNER_DECISION` / human-settings → closed | Rulesets API: main PR+Rust checks+deletion/NFF; release-tags for v*; evidence providers/63 |
| 2026-09-15 | #144 | `NEEDS_OWNER_DECISION` → closed | Owner IMPLEMENTATION CONTRACT locked `require` for new PG forms without changing Rust serde default; UI default + driver-switch + guidance + 5 new tests; evidence `providers/62-pg-secure-default-tls-require.md` |
| 2026-09-15 | #147, #129 | Status `ACTIONABLE_NOW`/`PARTIAL_ON_MAIN` → same disposition with lifecycle state `REVIEW`; commit and verification evidence recorded | Phase 2, batch 1 item 1: the Agent-path multi-statement classification defect is fixed and covered by 7 regression tests; both issues stay open because their documented remainders are not part of this fix |
| 2026-09-15 | #142 | `ACTIONABLE_NOW` → closed and moved to "Closed by this workstream" | Phase 2, batch 2 item A: the encrypted-file secret fallback is out of the release path (`build_secret_store`), 5 tests pin the gating and the degradation path, three release/architecture documents corrected, new risk-register row `R-KEYRING-FALLBACK` (`FIXED`) |
| 2026-09-15 | #147 | Status unchanged (`ACTIONABLE_NOW (REVIEW)`); re-verification evidence appended; **stays open** | Phase 2, batch 3 item 1: the classification fix was re-verified on the current tree (ancestor check, 7/7 regression tests green, interactive path unchanged), but the issue's own completion gate also requires the contract to be *documented* and #129/#136/#131 updated — `docs/release/known-limitations.md` has no multi-statement entry, `docs/08:90`/`docs/09:89-101` still say multi-statement is disabled, and the `BEGIN`/`COMMIT`/`ROLLBACK` batch test is missing. Owner reviewed and confirmed: leave open with the evidence posted rather than close on a partly-met gate |
| 2026-09-15 | #129 | Status `PARTIAL_ON_MAIN (REVIEW)` → `STILL_OPEN`, explicitly marked | Phase 2, batch 3 item 1: the multi-statement-classification part is closed and recorded in a status comment; cancellation boundaries (verified capability gating, no defect) and confirmation boundaries remain, so the audit umbrella is not closed |
| 2026-09-15 | #144 | Status `ACTIONABLE_NOW` → `NEEDS_OWNER_DECISION` (blocked-by-owner-decision); **issue left open** | Phase 2, batch 3 item 2: triaged and deliberately not implemented. No available `SslMode` variant is both non-breaking and non-plaintext (`Require` is a compatibility break and verifies nothing without a root CA file; `VerifyCa`/`VerifyFull` cannot be satisfied because the app has no CA/client-cert fields; a `Prefer` default needs a new domain/DTO/UI option the issue forbids inventing). Secondary blockers: a secure default promotes the unqualified TLS capability in `docs/release/provider-capability-matrix.md:42`; the edit/duplicate paths hardcode `Disable` while `UiConnectionSummary` carries no `ssl_mode`, so a new-connection-only change would silently downgrade connections on first save; and the issue's gate requires an accepted policy with #136 updated (#136 still open). Blocker comment posted; evidence file `docs/release/evidence/v01-runtime/providers/27-pg-secure-default.md` added |
| 2026-09-15 | #55, #74, #83, #85, #86, #87, #90, #132, #133, #135 | All ten `DONE_ON_MAIN` rows → closed and moved to "Closed by this workstream" | Phase 2, batch 3 item 3: each cited pointer was re-opened on the current tree before closing — files exist, identifiers/line anchors match, and the `db-pro-infrastructure` mapper tests were re-run green (9 passed / 0 failed). Where the evidence is historical (#85 gate totals, #86 live run) or scoped (#90 is the *preparation* deliverable, with #88 still open), the closing comment states that explicitly rather than implying more than was verified |
| 2026-09-15 | #145 | Status `ACTIONABLE_NOW` → `PARTIAL_ON_MAIN (REVIEW)`; commit and verification evidence recorded; **issue left open** | Phase 2, item 2: the SQLite backup was proved snapshot-safe, and the two paths that silently replaced a database with something a reader could not see were fixed (`e39290b`). Closed would have meant claiming the packaged-runtime smoke and the acceptance this environment cannot produce, so the row records the remainder instead |
| 2026-09-15 | #236 (new) | Created from the #144 verification, fixed, closed, and recorded in "Closed by this workstream"; the #144 row updated | Phase 2, item 1: the edit/duplicate TLS-mode downgrade is a real security defect that no existing issue covered. Reproduced with two failing tests before the change (`0 passed; 2 failed`, exit 101), fixed in `aba947b4`, green after it; `cargo test --workspace` 829/0/19 (baseline 827/0/19). #144 is **not** closed — its row now states which secondary blocker this removed and which remain |
| 2026-09-15 | #228–#235 (8 new) | Triaged `OUT_OF_SCOPE_V01` and added to the triage table; `INVENTORY.md` gained a snapshot-3 table with the proposed queue order; snapshot file `issues-open-2026-09-15.json` added | Phase 2, item 3: the eight issues created minutes after snapshot 2 were never triaged. All eight carry `Parent Goal: #182` and none has any partial surface on `main @ f15b1a9` (verified by grep per row); none is `ACTIONABLE_NOW`, so nothing was implemented from this batch. Invariant re-stated: open ledger rows 144 = live open issues 144 |
| 2026-09-15 | #56, #61, #62, #72 | Four `ACTIONABLE_NOW` code issues → closed with pushed fixes, tests and evidence (`f7c61c9d`, `205539c2`, `0af26475`, `a059506d`) | Phase 2, provider-value session: temporal decoder no longer invents a timezone; copy/export share one lossless serializer; a type-aware editability policy gates every edit entry point; the introspection IPC shape is pinned by contract tests + a checked-in fixture. `cargo test --workspace` moved 837/0/19 → 856/0/21 |
| 2026-09-15 | #102, #121 | Both brand inventories → closed with `docs/release/brand-rename-inventory.md` (`f5d94ff1`) | Phase 2, audit session: public surfaces and persisted identifiers classified with file:line evidence; the identity decision stays PENDING with the owner (#101/#30); #103 handed the implementation checklist |
| 2026-09-15 | #114 | Agent evidence/progress/review contract → closed with `docs/plans/_template/AGENT_EVIDENCE.md` + a new `AGENTS.md` section (`81f5bc90`) | Phase 2, process session: canonical template with the non-negotiable rules and two filled examples (a coding fix and an audit), linked from #113 |
| 2026-09-15 | #127 | Version/updater/persisted-state audit → closed with `docs/release/0.1.0-versioning-updater-persistence.md` (`39fe37e2`) | Phase 2: updater absent and correctly not presented as working; version source of truth and persisted-state policy recorded; legacy-state migration (D3) and bundle-id stability (D5) left as PENDING owner decisions |
| 2026-09-15 | #134 | Platform prerequisites reconciliation → closed with the rewritten `docs/release/platform-prerequisites.md` (`dd984bdd`) | Phase 2: the document now matches the native packaged runtime (build vs run prerequisites, measured macOS 11.0, no WebView2, Linux D-Bus/Secret Service, external tools not bundled) and no child issue was needed || 2026-09-15 | #54 | `PARTIAL_ON_MAIN` → closed with the whole-result contract tests and a generated fixture (`266e917`) | Phase 2, `PARTIAL_ON_MAIN` bucket: the recorded remainder ("no whole-result fixture test and no Rust↔UI field-name assertion for the shipping (non-serde) channel") was re-verified on `main @ 623ff4a` and implemented — 4 tauri shape/tag/precision/fixture tests + 2 native channel-mapping tests; `cargo test --workspace` 856/0/21 → 862/0/21. The issue's TypeScript-mirror half is void (React frontend retired 2026-09-11) and is stated as such in the closing comment rather than implied |
| 2026-09-15 | #57 | `PARTIAL_ON_MAIN` → closed with a live structured/binary decode test (`a153c1e`) | Phase 2, `PARTIAL_ON_MAIN` bucket: the recorded remainder ("JSON/JSONB/UUID/BYTEA never asserted in the live suite") was re-verified on `main @ acc142c` and implemented as `pg_query_decodes_structured_and_binary_value_classes` over real fixture columns + inline classes; the first run was green, so the row records coverage gained, not a defect fixed. `cargo test --workspace` 862/0/22 (the new case is `#[ignore]`d behind `DATABASE_URL`) |
| 2026-09-15 | #58 | `PARTIAL_ON_MAIN` → closed with the format-aware fallback fix (`c7f4328`) | Phase 2, `PARTIAL_ON_MAIN` bucket: a real defect was measured first (binary payloads served as text; `money`/composite failed the whole query), then fixed in the decoder fallback; 1 unit + 2 live tests, fails-before/passes-after captured in the evidence file. Array element parsing deliberately out of v0.1 and recorded in the A3 contract (#53) |
| 2026-09-15 | #59 | `PARTIAL_ON_MAIN` → closed with the 26-column decoder matrix fixture and its live test (`75865db`) | Phase 2, `PARTIAL_ON_MAIN` bucket: the fixture gained every missing class (domain, interval, time/timetz, int2, float4/8, inet/cidr, array, range, composite) on the CI load path, with a populated row, a NULL row and deterministic verify assertions; one live test asserts the whole expected-vs-actual matrix. Live suite 23 → 24 passed; workspace gates 863/0/25 |
| 2026-09-15 | #53 | `PARTIAL_ON_MAIN` → closed with `docs/release/provider-value-contract.md` (`dbfe1e1`) | Phase 2, `PARTIAL_ON_MAIN` bucket: the A3 fallback contract had no in-tree document; the new document records the class matrix (read representation + editability), the format-driven fallback rule, the explicit v0.1 exclusions and the falsification map, with every `file:line` re-opened (two stale pointers corrected). Documentation-only change; gates re-run 863/0/25 |
| 2026-09-15 | #237 (new) | Filed from the CI failure on 46b2bb5, fixed, closed, and recorded in "Closed by this workstream" | Phase 2, `PARTIAL_ON_MAIN` bucket session: CI went red on a documentation-only commit because `--include-ignored` runs the live PG suite in parallel and a new test shifted the interleaving onto a pre-existing race — `pg_get_indexdef`/`pg_get_constraintdef` are evaluated with a fresh snapshot, so a concurrently dropped object returned NULL and failed the whole refresh. Fixed with tolerant decoding plus a bounded retry restricted to the narrow transient catalog messages; live regression test fails before / passes after; CI-mirroring run 889 passed / 0 failed |
| 2026-09-15 | #66 | `PARTIAL_ON_MAIN` → closed with the exact-head Gate 5 verification record (`06e137e`) | Phase 2, `PARTIAL_ON_MAIN` bucket: `providers/43-gate5-exact-head-verification.md` records all six gates plus the live PostgreSQL matrix and the CI-mirroring run on one SHA (`d24eb6a`), states the frontend block as void rather than green, and names the reviewer-disposition item as not claimed |
| 2026-09-15 | #23 | `PARTIAL_ON_MAIN` → closed as `SATISFIED_NOW` (no code change) | Phase 2, `PARTIAL_ON_MAIN` bucket: the workstream's own closure rule (all decoder families + the integration fixture Done) is met by the closed children #55-#59; re-verified with the live suite (25/25) and the CI-mirroring run (889/0/0) rather than on the child list alone |
| 2026-09-15 | #14, #26, #27, #28, #30, #31, #67, #69, #70, #73, #81, #82, #89, #96, #105, #106, #107, #110, #136, #141 | `PARTIAL_ON_MAIN` → `BLOCKED_OWNER` (2026-09-15), **issues left open** with one blocker comment each | Phase 2, `PARTIAL_ON_MAIN` bucket: each row was re-verified against the live tree first, then classified. Every comment names the exact blocker, what part is already done, and what would unblock it; the classification table is in "PARTIAL_ON_MAIN bucket — disposition of the 2026-09-15 run" |
| 2026-09-15 | #29, #91, #94, #145 | `PARTIAL_ON_MAIN` → `BLOCKED_EXTERNAL` (2026-09-15), **issues left open** with one blocker comment each | Phase 2, `PARTIAL_ON_MAIN` bucket: no Windows/Linux host, no GUI session and no publication exist in this environment; the parts that are done (non-interactive macOS qualification, the state-directory fix, the SQLite backup snapshot-safety fix, the #134 prerequisite inventory) are named in each comment |
| 2026-09-15 | #21, #22, #24, #25, #52, #64, #75-#80, #88, #120, #122, #126, #128, #129, #131 | Classified `ACTIONABLE` but **not reached**; rows unchanged (`PARTIAL_ON_MAIN`) | Phase 2, `PARTIAL_ON_MAIN` bucket: budget was spent on depth in the provider-value cluster plus the CI-red defect. The run order for the next pass is recorded in the disposition table |
| 2026-09-15 | #88 | `PARTIAL_ON_MAIN` → closed with the candidate evidence manifest and its 9 contract tests (`326c313`) | Pass 2, item 1 of the `ACTIONABLE` queue: the single in-repo manifest was missing entirely (`grep` → no matches). Built from the checked-in evidence cross-checked against five documents, with the run/job/artifact ids re-read from the GitHub API; the acceptance conditions are now enforced by tests that were falsified with three probes (1, 2 then 3 failures) before being trusted. `cargo test --workspace` 863/0/26 → **872/0/26**; CI-mirroring 889/0/0 → **898/0/0** |
| 2026-09-15 | #78 | `PARTIAL_ON_MAIN` → closed with the per-row release dispositions for its domain | Pass 2, item 11: four rows settled from source, two of them positively verified (export is gated on result state; ER search is a view mode over all matches with a pinned explicit show-all) and two accepted because the native mechanism differs (no single-match auto-pick; React-Flow sub-items not applicable). The rows without a live P2 entry are listed so nothing is silently dropped |
| 2026-09-15 | #77 | `PARTIAL_ON_MAIN` → closed with the per-row release dispositions for its domain; **#239 filed** | Pass 2, item 10: six rows settled from source (three of them positively verified — the test result is invalidated on edit, the backend failure message is shown verbatim, the cancelled file pick is handled), two deferred because their subject does not exist natively, and the SSH scope item dispositioned `Fix RC1` because the in-app impression contradicts LIM-006 |
| 2026-09-15 | #76 | `PARTIAL_ON_MAIN` → closed with the per-row release dispositions for its domain; **#238 filed** | Pass 2, item 9: five rows `Accept RC1`/`Defer`, one `Fix RC1`. The fix row was settled by measurement, not judgement: a temporary probe timed the unsorted projection (5.35 ms) against the text-sorted one (**3,414 ms** per invocation at 200k rows), which is recomputed every frame — the probe was removed before the commit, and the child issue carries the numbers, the reachable bound and its own acceptance list |
| 2026-09-15 | #75 | `PARTIAL_ON_MAIN` → closed with the per-row release dispositions for its domain | Pass 2, item 8: the audit the issue asks for (one disposition per live row, from current source) is recorded in `docs/release/rc1-p2-release-dispositions.md` §A; seven rows `Accept RC1` and one `Defer post-v0.1`, no `Fix RC1`, and the one unmeasured row (`QA-P2-14`) says so explicitly instead of reading as verified |
| 2026-09-15 | #25 | `PARTIAL_ON_MAIN` → `BLOCKED_OWNER`, **issue left open** with a blocker comment | Pass 2, item 7: D1 #64 was closed in this pass and D3 #66 was already closed, both re-verified; D2 #65 and D4 #67 are still open — the first is a retired-frontend task whose disposition is the owner's, the second is already `BLOCKED_OWNER`. The completion rule's PR half is void under the main-only workflow and no reviewer exists here. The fact that the contract advanced past the `providers/43` record (A2 `27c52db`) is recorded as a note, not used to re-open #66 |
| 2026-09-15 | #21 | `PARTIAL_ON_MAIN` → `BLOCKED_OWNER`, **epic left open** with a status comment | Pass 2, item 6: the four workstreams were re-verified individually (A #22 and B #23 closed and re-checked, C #24 blocked-owner, D #25 still being verified) and all six non-negotiable rules were mapped to a passing test or document. The gate cannot close while #24 and #25 are open, and the epic's own rule requires exact-head CI + verified main *before* closing |
| 2026-09-15 | #24 | `PARTIAL_ON_MAIN` → `BLOCKED_OWNER`, **issue left open** with a blocker comment | Pass 2, item 5: both closed children (#61, #62) and the native substitutes were re-verified green, and Workstream A is complete after #52 — but the umbrella's own rule needs C1–C4 Done and **#60/#63 are still open**, scoped to the frontend retired 2026-09-11. Closing them as superseded or re-scoping #63 is a scope call, so the umbrella was not closed by assumption |
| 2026-09-15 | #22 | `PARTIAL_ON_MAIN` → closed as a verified umbrella (no code of its own) | Pass 2, item 4: each of its four A-tasks was re-checked on the live tree rather than trusted from the child list — the three A1 tests re-run green, the A2 variants present at `query.rs:77/80/84`, the A3 contract document re-opened with no pending item, the A4 fixture re-asserted byte-for-byte. Nine `file:line` pointers in the contract document were corrected because #52 shifted them. The stale `fix/gate5-provider-value-contract`/PR #161 header is stated as the retired workflow |
| 2026-09-15 | #52 | `PARTIAL_ON_MAIN` → closed with the dedicated temporal variants (`27c52db`) | Pass 2, item 3 of the `ACTIONABLE` queue: the canonical strings were already locked by #56, but TIMESTAMP/TIMESTAMPTZ shared `CellValue::DateTime` and TIME/TIMETZ shared `CellValue::Time`, so the class distinction lived only in the string. The four layers (domain, decoder, DTO, UI mapping/export/binding) now carry dedicated variants; the compiler enumerated every consumer, the DTO fixture was regenerated from the real structs, and the two #56 live tests failed first with `left: Timestamp(…) / right: DateTime(…)`, proving the decoder really changed. The "binders fail explicitly" item is recorded as met in intent — malformed values fail explicitly and nothing is bound as text — because refusing to bind would regress #62's pinned writability. Workspace 875/0/27; live 25/25; CI-mirroring 902/0/0 |
| 2026-09-15 | #120 | `PARTIAL_ON_MAIN` → **audit complete + closed** | Pass 3, item 8 (last) of the `ACTIONABLE` queue: the source-backed differentiator inventory classifies every capability into the issue's five status classes, marks the four wedges that have test/audit evidence (local-first, lossless value fidelity, staged+gated writes, large-schema ER boundedness), labels commodity parity so it cannot be mistaken for advantage, and hands #97 a can-say/cannot-say table plus a prohibited-overclaim list; the agent stays `PREVIEW/EXPERIMENTAL` (LIM-007) and MCP `BACKLOG` (LIM-008) |
| 2026-09-15 | #131 | `PARTIAL_ON_MAIN` → **audit complete + closed** | Pass 3, item 7 of the `ACTIONABLE` queue: the release-traceability matrix maps all 25 capabilities the issue lists to real tests, real CI job steps, runbook smoke rows and an evidence owner, using exactly the six coverage words; every automated gap is assigned (#238 sort, #243 secret store, #244 export/restore/cancel) and the host-blocked rows point at #91–#95 with `R-GUI-SMOKE`/`R-WINLINUX`. #111 can use it as the final Goal evidence index |
| 2026-09-15 | #129 | `PARTIAL_ON_MAIN (REVIEW)` → **audit complete + closed** | Pass 3, item 6 of the `ACTIONABLE` queue: the query editor now holds a `Destructive` statement (or a script whose worst statement is destructive) until the user confirms the exact text, pinned by 4 falsified UI tests and one new core test; the required 12-row execution-safety matrix and the parser/classifier inventory are in `docs/release/audit-execution-safety.md`; `docs/08` and `docs/09` §5 no longer claim multi-statement execution is rejected. Scope items deliberately not absorbed are named in the closing comment: the table-editor `Apply DDL` capability call and backup/restore cancellation (#244) |
| 2026-09-15 | #128 | `PARTIAL_ON_MAIN` → **audit complete + closed**; **#244** filed from the audit and left open; capability-matrix export/backup rows corrected | Pass 3, item 5 of the `ACTIONABLE` queue: every shipped export/import/backup operation got one classification (`Release-qualified` / `Implemented but not qualified` / `Deferred or not supported`) in `docs/release/audit-data-integrity.md`, with a partial-failure table and a test inventory. The release-facing capability matrix had claimed XLSX export and a `csv`-crate CSV export that no shipped surface can reach — corrected in the same commit, which is the issue's "exact support matrix" acceptance item; #105 (blocked-owner) consumes it. Three P2 code gaps (non-atomic export, non-transactional PostgreSQL restore, unconstructible cancel command) are child **#244** rather than in-place patches, per the issue's inspection-only rule |
| 2026-09-15 | #126 | `PARTIAL_ON_MAIN` → **audit complete, `BLOCKED_EXTERNAL`, issue left OPEN**; **#243** filed from the audit and left open | Pass 3, item 4 of the `ACTIONABLE` queue: the credential lifecycle is recorded as a 13-row matrix in `docs/release/audit-keyring-secret-lifecycle.md` with the per-OS backends quoted from `keyring 3.6.3`, four P2 findings (swallowed store/delete failures, invisible orphans, the agent key's missing lifecycle, Linux persistence semantics) and the test inventory that names what is not covered. The issue stays open because its own review gate (agreed in the 2026-08-14 comments) requires the credential store proved on each platform **in a packaged build**, which needs hosts this environment does not have. The packaged-runtime scenarios it asked for were added as Step 19 of the interactive runbook |
| 2026-09-15 | #122 | `PARTIAL_ON_MAIN` → **audit complete + closed**; **#242** filed from the audit and left open | Pass 3, item 3 of the `ACTIONABLE` queue: the trust-boundary audit is recorded in `docs/release/audit-security-boundaries.md` with the data-flow inventory (only two AI endpoints; no telemetry, update check, licence check or remote asset; no socket on the startup path), the credential matrix (secrets absent from persisted config, summaries, logs, errors and clipboard by construction; the encrypted fallback gated and DEV-ONLY keyed), the execution boundaries (core-side policy enforcement independent of the UI, staged grid mutations, agent approval bound to run id + document version, DDL unreachable from the table editor) and the host boundary (argv-only process spawning, no `sh -c`, no `unsafe`, legacy `tauri-app` outside the shipping graph). **No P0, no P1.** One P2 → **#242** (the AI features are the only egress and nothing discloses it); three P3 accepted; T-5/T-6 cross-referenced to #129/#126. The issue's rule was audit-only, so no product code changed |
| 2026-09-15 | #80, #241 (new) | `PARTIAL_ON_MAIN` → **audit complete + closed**; **#241** filed from the audit, fixed and closed in the same pass | Pass 3, item 2 of the `ACTIONABLE` queue: nine cross-cutting scope bullets dispositioned in §F. Six defects fixed under child **#241** — the status bar hid most of the app's feedback (the message was rendered only when it contained failed/error, `navigation_view.rs:233` + `app.rs:723-728`), the table editor's `Apply DDL` was advertised as confirmation-gated and did nothing (`ddl_execute_confirmation` never set true), icon-only toolbar controls announced as `"Button"` (`components/button.rs:155`), `Shift+F10` could not open a context menu on a focused widget (`overlay.rs:791`), five tooltips spelled `Cmd/Ctrl+…` and the agent tooltip claimed an unbound `⌘I` on every platform, and eight hardcoded RGB literals duplicated the opposite theme's tokens. Deferred with sites named: the modal focus lifecycle, ~30 glyph-only controls, three hand-painted widget families, and the measured light-theme contrast shortfall (3.45:1 caption / 3.19:1 warning vs 4.5:1 AA). Three new tests, falsified with two probes; `cargo test --workspace` **878 passed / 0 failed / 27 ignored** (delta +3). A first gate pass failed on three clippy warnings this change introduced and was fixed before the commit — recorded, not hidden |
| 2026-09-15 | #79, #240 (new) | `PARTIAL_ON_MAIN` → **audit complete + closed**; **#240** filed from the audit, fixed and closed in the same pass | Pass 3, item 1 of the `ACTIONABLE` queue: the eight perf/tooling scope bullets each got exactly one disposition in `docs/release/rc1-p2-release-dispositions.md` §E (two positively verified, one deferred, three `Fix RC1`). The three fixes are defects in the perf-scan evidence tool itself and were made in place under child **#240**: skipped sections are now counted and named (`PASS (partial) — … not run: …` where `all` used to print an unqualified `PASS`), the exit contract is `PASS` 0 / `WARN` **2** / `FAIL` 1 (measured: `er` printed `WARN` and exited 0 before), and every run prints the source revision plus the sha256 of the measured artifact (where a size could previously not be tied to a tree or a binary). Budget tables in `.skills/perf-audit/SKILL.md` + `references/perf-budgets.md` were corrected to the criterion ids that actually exist. New regression test: `perf-scan.sh --self-test` (14 assertions), falsified with three probes (1, 2 then 1 failures). No Rust source changed; `cargo test --workspace` **875 passed / 0 failed / 27 ignored** (delta 0) |
| 2026-09-15 | #64 | `PARTIAL_ON_MAIN` → closed with the live provider→domain→DTO E2E test (`483f3da`) | Pass 2, item 2 of the `ACTIONABLE` queue: the decoder half and the DTO half were each covered but nothing joined them. One live test now runs the fixture's 26-column row through provider → domain → `QueryResultDto` and asserts the class matrix, the loss-sensitive values, the declared provider types and a 52-cell no-loss sweep; green on the first run (coverage, not a defect), falsified with three probes. `cargo test --workspace` 872/0/26 → **872/0/27**; CI-mirroring 898/0/0 → **899/0/0** |
| 2026-09-15 | #238 | `ACTIONABLE` → `CLAIMED, LEFT OPEN` (verification only, **no product commit**) | Pass 4, item 1 of the queue: the issue's work is already claimed by **open PR #245** and that branch's head is not on `main`, so the fix was not duplicated. Instead the defect was re-measured on `main @ ba2f515` with a temporary probe (removed before the commit) and reported: the audit's "3,414 ms sorted on a text column" is a **lower bound** — a timestamp-shaped text column reaches **6.6 s**, because only there do the comparator's temporal parses succeed — and PR #245 covers the issue's option (b) but neither option (a) (no cache; `result_grid_view.rs` untouched) nor acceptance items 1 and 3 (no call-count test, no sorted-path bench). Issue left open with the evidence posted; live open-issue count unchanged |
| 2026-09-15 | #239 | `ACTIONABLE` → **fixed + closed** (commit `fix(ui): disclose the unqualified SSH tunnel path (#239)`, sha recorded below) | Pass 4, item 2 of the queue: the SSH card now carries the LIM-006 qualification on the control itself — `SSH_QUALIFICATION_HINT` (`connection_view.rs:5-15`) rendered as a muted caption below the checkbox and outside the `if ssh_tunnel_enabled` block (`:732-740`), in the muted-caption idiom this dialog already uses for inline qualifiers, so no component, state or behaviour was added. The wording makes only claims LIM-006 already makes ("Unqualified in v0.1" / "has not been end-to-end tested" / "may not work reliably") and the mapping is in the doc comment. Three tests pin the **painted** frame via `FullOutput` text shapes, not the constant: deleting the label fails (probe 1: 2 failed / 1 passed) and replacing the wording with an invented claim fails (probe 2: 1 failed / 2 passed); both probes reverted. `cargo test --workspace` 883/0/27 → **886/0/27** (+3); CI-mirroring 910 → **913 passed / 0 failed**; the fixture was started and stopped for that run. `HD-005` remains `PENDING` and the caveat takes no position on it — recorded so the closure is not read as a support-level decision |
| 2026-09-15 | #239 (record) | Commit sha recorded in the #239 row and in the pass-4 queue table | The #239 work landed in `ccc0a24` with its ledger row in the same commit; this follow-up records the sha the row could not name at the time. `gh issue close 239` succeeded and the issue is closed; live open-issue count 118 → **117** |
| 2026-09-15 | #147 | `ACTIONABLE_NOW (REVIEW)` → still open, reclassified onto a **measured correctness gap** rather than a documentation remainder (no product commit) | Pass 4, item 3 (last) of the queue: all seventeen criteria in the issue body were re-checked against `main @ 3889970`. Two remainders were closed by inspection — `docs/09` §5 already carries the chosen Option B contract and the corrected `docs/08` item 5, and the second stale `docs/08` bullet (`### Statement execution`, "multi-statement execution is disabled by default") was corrected in this pass in the same superseded style. One remainder turned into a finding: the previous pass recorded "there is no `BEGIN`/`COMMIT`/`ROLLBACK` batch test", so the case was measured live and **a batch that carries its own `COMMIT` escapes the app-owned transaction** — `execute_transaction(["BEGIN","INSERT …","COMMIT","SELECT no_such_column …"])` returns `phase=Statement index=3 outcome=RolledBack` with **1 row surviving**, against a control that returns the same `outcome=RolledBack` with **0 rows**. The failure envelope claims a rollback that did not happen. It is therefore recorded in `docs/release/audit-execution-safety.md` §3, **not** as a `known-limitations` entry, because that registry's own rule ("any limitation that actually constitutes P0/P1 correctness/security must not be hidden as a harmless known limitation") forbids that disposition and the issue's ask is enforcement. Issue left open with a status comment naming the three contract choices (reject explicit transaction control in a batch — recommended; honour it; weaken the atomicity claim) as the owner's call, and with the fact update posted to the #136 register. No product code changed; the product-code gates are the ones on `ccc0a24` (886/0/27; CI-mirroring 913/0/0), which this pass's documentation-only edit does not touch |
| 2026-09-15 | #147 (record) | Status-comment and register-update URLs recorded in the #147 row and the pass-4 queue table | The audit landed in `31ed252`; the two comments it promised could not name their own URLs at the time. #147 stays open (measured P1-class gap + owner decision); #136 stays open (its own owner decisions) but now carries the R016 contract and this finding |
| 2026-09-15 | #147 (record, corrected) | The two URLs are recorded in the #147 row and the pass-4 queue table by this commit | Correcting `8cd1cd4`, which shipped the note above but **not** the URLs it claims: the edit script asserted on a stale anchor and exited before writing, and the appended note went in anyway. Caught by re-reading the file after the push (the URLs were still absent), not by trusting the commit message. The note in `8cd1cd4` is superseded by this row |
| 2026-09-15 | **#179, #180** (PRs, not issues) | Both analysed against `main @ 00a3ce9`: disposition **`SUPERSEDED` for both — close as superseded**; **nothing ported, no product commit** | The two stale open PRs claim the same P1 defect (`decode_textual_value` / the `decode_cell` fallback calling `raw.as_str()` on a `PgValueFormat::Binary` payload). The defect is **already fixed on `main`**, proven on the live fixture rather than by reading code: `sqlx-postgres 0.8.6` requests binary results for every prepared statement (`executor.rs:244-247`, and `connector.rs:151` uses `sqlx::query_with(...).fetch`), a two-probe reproduction showed all eight classes of a single row (`order_status` enum, domain-over-text, `text[]`, `tsvector`, `int4range`, `money`, `interval`, `text`) really arrive `Binary` — with the legacy unconditional `as_str()` yielding mojibake for four of them and `incomplete utf-8 byte sequence` for `money` — and `PostgresConnector::query` on `main` returns canonical text for the enum/domain and **byte-exact** `Bytes` for the rest (`money` = 0x4D2 = 1234 cents). Three live tests already pin this and pass today: `pg_integration.rs:854`, `:924`, `:972`. Every hunk of both PRs classified `ALREADY_ON_MAIN` / `SUPERSEDED_BY_NEWER_WORK` / `NO_LONGER_APPLICABLE`, none `STILL_VALUABLE`, and neither net diff applies (`git apply --check` exits 1 for both). #180's two non-infra hunks are already on `main` (`ci.yml:77` via `7d7043c`; the `result_large_err` allow at `table_data_service.rs:216`), and its `query_mapper.rs` change is a weaker version of `main:417-437` that does **not** fix the mojibake class it was written for — it builds on the false premise that `as_str()` checks the format (`sqlx-postgres 0.8.6` `value.rs:71-73` is `Ok(from_utf8(self.as_bytes()?)?)` with no format check), so `from_utf8` still succeeds on the binary payloads of `text[]`/`tsvector`/`int4range`/`interval`, and `money` still fails. #179's fallback targets a `decode_cell` shape that no longer exists (and its placeholder `<unsupported binary value: …>` would break the byte-exact contract), its `sqlite/actor.rs` allow is already at `:262`, its UI hunks are a partial 5-of-107-site float-literal sweep that clippy does not require on the pinned 1.95.0, one of the files it edits (`crates/ui/src/components.rs`) no longer exists, and its verification gates name the archived React frontend. Both PRs' added unit tests are tautologies (they assert on a static string / a local closure, never on the mapper). Reading the tree was not enough to settle this, so the gate numbers were re-measured on `00a3ce9`: `cargo test --workspace` **889 passed / 0 failed / 27 ignored** (baseline 886/0/27, +3 = the #245 merge's three `result_grid.rs` tests) and CI-mirroring `cargo test --all -- --include-ignored` **916 passed / 0 failed / 0 ignored** (baseline 913, +3); clippy clean. Evidence: `docs/release/evidence/v01-runtime/providers/52-stale-pr-resolution.md`. Neither PR was merged, closed, commented on or otherwise mutated |
| 2026-09-15 | **#179, #180** (PRs, not issues) — **closed** | The disposition above is now executed: both PRs are **CLOSED as superseded**, neither merged. Confirmed on the live repository: `gh pr view 179` → `state=CLOSED`, `mergedAt=null`, `closedAt=2026-09-15T00:49:51Z`, head `c35dfb9`; `gh pr view 180` → `state=CLOSED`, `mergedAt=null`, `closedAt=2026-09-15T00:49:48Z`, head `bc08819`; `gh pr list --state open` → **0**, so the closure makes the open-PR count zero and removes the two competing diffs against the decoder already on `main`. The closures were **not** made by this workstream and no branch, commit or PR of this workstream was involved; the ledger records the fact and its evidence pointer rather than taking credit for it. Rationale, in one line: the defect both target is already fixed on `main` in a stricter form (canonical text for the enum/domain classes and byte-exact `Bytes` elsewhere, pinned live at `pg_integration.rs:854`, `:924`, `:972`), every hunk of both PRs classified `ALREADY_ON_MAIN` / `SUPERSEDED_BY_NEWER_WORK` / `NO_LONGER_APPLICABLE` with none `STILL_VALUABLE`, neither net diff applies (`git apply --check` exits 1 for both), and `git merge-tree` showed merging them would **regress** `main` — #179 would resurrect the deleted `crates/ui/src/components.rs`. Evidence: `docs/release/evidence/v01-runtime/providers/52-stale-pr-resolution.md` §2 (live reproduction), §7 (per-PR recommendation table) |
| 2026-09-15 | #238 (record) | Ledger row and live open-issue count corrected to the post-merge fact: **117 → 116** | PR #245 merged (`00a3ce9`) and auto-closed #238 at `2026-09-15T00:41:15Z`, which happened after the pass-4 rows were written (`#238` was recorded there as "still open and `CLAIMED` by PR #245" — true when written, stale now). Corrected rather than left to drift, because this file's invariant is *open ledger rows equal the live open-issue count*: `gh issue list --state open --limit 400` returns **116**. #238's closure by an external merge is **not** claimed as this workstream's work; the remaining acceptance items of that issue are re-verified against the merged tree at the top of pass 5, whose outcome is recorded in its own row |
| 2026-09-15 | **#238** | `ACTIONABLE` → **implemented on `main` + re-verified, closure confirmed**; one residual filed as its own issue | Pass 5, item 1 of the queue. The issue was auto-closed by PR #245's merge, but the merged diff implemented only **option (b)** of the issue (allocation-free comparator + `looks_like_iso_temporal` gate + ASCII filter fast path, in `result_grid.rs`/`lib.rs` only) and so did **not** meet acceptance items 1 and 3: the draw path still called `filtered_sorted_indexes` per frame (`result_grid_view.rs:76`) and the diff contains no benchmark. Re-measured on `main @ 9a8dc5b` with a probe (deleted before the commit): 200k rows × 4 cols → 5.50 ms unsorted / 11.60 ms plain-text-sorted (**14× better than pass 4's 166.6 ms — the #245 gate does work**) / **10,528.0 ms temporal-text-sorted** (same class as pass 4's 6,649.6 ms) / 98.97 ms filter+sort, per frame. So rather than close on the merge's behalf, the missing bounded part was implemented: a one-entry `GridProjectionCache` keyed on (`epoch`, filter, sort column, direction, row count, column count), with the epoch advanced at every site where the displayed rows change (query completed/multi-completed, agent result opened, table info/data loaded, single row reloaded, active result switched) grouped with the existing row-identity invalidation so the set stays checkable by grep; the draw path takes/restores the projection around the frame and the header's auto-size action now reuses it instead of recomputing. Behaviour unchanged (same comparator, same stable order over original row indexes). **Measured through the real draw path in the same build, cache off vs on, 200k rows sorted on the timestamp-shaped column: 10,652 / 10,651 / 10,623 ms per frame → 10,731 ms (first frame only) then 41.1 / 40.0 / 42.7 / 39.8 ms**, rebuild count frozen at 2 over 5 frames; plain-text-sorted 48.9 → 38.5 ms. Acceptance item 3: new criterion group `result_grid_projection_sorted` (`sort_plain_text_column` **2.53–2.59 ms**, `sort_temporal_text_column` **566.8–576.1 ms**, `filter_and_sort` **4.44–4.58 ms**, release, 200k × 4) with budget rows in `.skills/perf-audit/references/perf-budgets.md` — the temporal row carries no target rather than a number the code does not meet. Acceptance item 1: 6 new tests (4 cache-level, **2 through the real draw path**: 3 frames → 1 rebuild), falsified with two probes (always-miss → 4 failures; epoch dropped from the key → 2 failures), both reverted. Gates `cargo test --workspace` 889/0/27 → **895/0/27** (+6), CI-mirroring 916 → **922 passed / 0 failed / 0 ignored** (+6), fmt/check/clippy/release-build exit 0, perf-scan **PASS (partial)** exit 0 (4 passed / 0 warnings / 0 failed / 4 not executed). Residual found by the same measurement and **filed separately, not absorbed here**: `GridSelectionLookup::new` builds a `HashMap` over every filtered index every frame — **36.5 ms** of the ~40 ms steady-state frame at 200k rows (debug) — same defect shape, different structure, outside #238's acceptance items. Evidence: `docs/release/evidence/v01-runtime/providers/53-result-grid-projection-cache.md` |
| 2026-09-15 | #238 (record), **#246** (new) | #238's comment URL and the new child issue recorded in one docs commit; live open-issue count **116 → 117** | The #238 row above shipped in `7c356ff` with the fix and named the residual, but could not name its own issue number or comment URL at the time. The residual — `GridSelectionLookup::new` building a `HashMap` over every filtered row index every frame, measured at **36.5 ms of a ~40 ms** steady-state frame at 200k rows in debug — is filed as **#246** with the probe output and the fix direction, keeping #238's closure claim scoped to its own acceptance items. #238's status comment: https://github.com/truongnat/db-pro/issues/238#issuecomment-5673088045. `gh issue list --state open --limit 400` → **117**: the invariant "open ledger rows equal the live open-issue count" holds as 114 triage-table rows + #246 and the two still-open post-triage arrivals (#242, #243, #244 are three; see the count row above, which this row updates by one new arrival) |
| 2026-09-15 | **#242** | `ACTIONABLE` → **implemented + closed** (commit `fix(ui): disclose the AI data flow the product never mentioned (#242)`) | Pass 5, item 2 of the queue. Egress paths re-established on the tree rather than inherited from the audit: the AI provider is the workspace's **only** `reqwest` user (`crates/runtime/src/agent.rs:8`), the two endpoints are `api.groq.com`/`api.openai.com` `/v1/responses` (`:13,15`, HTTPS-enforced `:153`), inline prediction is `Eager` by default (`editor/prediction.rs:5-11`) and fires 300 ms after an edit (`query/query_document.rs:13`), the agent payload carries an `AgentResultSummary` (`runtime/agent.rs:395-413`), and the key can be seeded from the OS keyring at startup (`native-app/src/main.rs:203-220`). **The issue's "20×12" payload bound is stale** — the code bounds it at `MAX_AGENT_SAMPLE_ROWS = 20` × `MAX_AGENT_RESULT_COLUMNS = 50`, cells truncated to 256 chars (`core/src/domain/agent.rs:10-12`); the corrected numbers are used in the UI text, LIM-019 and the audit row. Egress requires a configured key: with none, the runtime answers "AI provider is not configured" and sends nothing (`runtime/worker.rs:1118-1127`) — that fact is what makes the recorded default defensible rather than a guess. Fixed minimally in the existing idioms, no new component: a muted caption in the agent panel's API-key section (`AI_EGRESS_DISCLOSURE`, `agent_view.rs`, directly under the key-handling caption it already had) and a muted caption under the editor's AI prediction control (`AI_PREDICTION_EGRESS_NOTE`, `query_view.rs`); docs side is registry entry **LIM-019** (`known-limitations.md`, all registry fields, summary 11 → 12 `Accepted v0.1`, no entry deleted), one bullet in `0.1.0-release-notes.md` §Known limitations, a paragraph in `README.md` §Agent provider, and the audit's **T-1 row moved `Fix RC1` → `FIXED`** with the 20×12 correction. Acceptance item 3 recorded as a decision — **`Eager` is kept** (egress needs a key, the disclosure now exists, the Off/Subtle/Eager selector is one click away) and item 4 as the stated opt-out plan (per-connection/per-session opt-out deferred to #34/#32; shipped opt-out is the selector plus leaving the key unset). The owner can reverse the default in one line plus the pinned test. 4 tests, all reading the **painted frame** rather than the constant, plus a no-privacy-claim guard; falsified with two probes (labels removed → 2 failures; invented "fully private" claim → the guard fired) — the first wording test only checked for present facts and let the overclaim through, which is recorded in the evidence rather than quietly patched. `cargo test --workspace` 895/0/27 → **899/0/27** (+4); CI-mirroring 922 → **926 passed / 0 failed / 0 ignored** (+4); fmt/check/clippy/release-build exit 0; perf-scan **PASS (partial)** exit 0 (4 passed / 0 warnings / 0 failed / 4 not executed). Evidence: `docs/release/evidence/v01-runtime/providers/54-ai-egress-disclosure.md` |
| 2026-09-15 | **#147** | `ACTIONABLE_NOW (REVIEW)` → **`DONE_ON_MAIN` — closed by `9d5bebaa`/`f66d800d`, and the closure is now *re-verified on the merged tree* rather than inherited** | Pass 6, step 2. The row above recorded the pass-4 measurement that kept this issue open (a `BEGIN`/`INSERT`/`COMMIT`/`SELECT bad` batch returned `RolledBack` with 1 row surviving) and named the three contract choices. `9d5bebaa` takes the first — refuse transaction control inside a multi-statement batch — and the contract is recorded in `docs/09-architecture-decisions.md` §5. Re-measured on `main @ d4df6373` against the live PostgreSQL fixture through the product path: the issue's own batch is **refused before dispatch (index 0, naming `` `BEGIN` ``) with `results_len=0` and 0 rows surviving**, and the control batch without transaction control still rolls back with 0 rows. Committed tests re-run green (2 PG product-path + 1 PG connector characterisation + 4 SQLite). The issue was already CLOSED on GitHub (2026-09-15T06:29:09Z) by the implementing session, with Vietnamese evidence comment; this pass posts the independent re-measurement as its own comment instead of re-closing. Evidence: docs/release/evidence/v01-runtime/providers/58-multistatement-transaction-control.md |
| 2026-09-15 | **#246** | Filed by pass 5 as an open post-triage arrival → **`DONE_ON_MAIN` (closed by `71ca86c1`) with the fix re-measured in pass 6**; the ledger's open-arrival narrative is corrected by this row (the issue had already been closed on GitHub at `2026-09-15T03:08:35Z` when the pass-5 ledger was written, but the ledger recorded it as open) | Pass 6, step 2. The fix is present and measured on `main @ d4df6373`: the issue's 36.535 ms of a ~40 ms frame becomes **0.000176–0.000383 ms per frame** in steady state, with the lookup rebuilt once per (projection, column order) and not per frame. Reported honestly: the first frame after a miss still pays the full build (36–43 ms measured), which is the same number the issue measured — that is the cost being moved, not removed. Evidence: docs/release/evidence/v01-runtime/providers/57-result-grid-selection-lookup-cache.md |
| 2026-09-15 | **#234** | `OUT_OF_SCOPE_V01` → **`PARTIAL_ON_MAIN`**; issue **RE-OPENED** by pass 6 (it had been closed by the implementing session at `2026-09-15T07:13:44Z`) | Pass 6, step 2: the merge (`0729d699`, `a59e433d`) is verified against the tree rather than the commit message. Two of six acceptance criteria are not met (no capability-level reason channel or provider-name de-branching; **no architecture doc records the contract** — `grep -rl ProviderFactory docs/` is empty) and three are partial, so the closure would have claimed more than the tree shows. The conformance suite runs in CI but never leaves SQLite, and its lack of a value/decoder case is exactly what let #235's mapper defect through. Evidence pointer for the re-opened row: the remainder comment https://github.com/truongnat/db-pro/issues/234#issuecomment-5679555835 plus the file:line table quoted in the LEDGER row. |
| 2026-09-15 | **#235** | `OUT_OF_SCOPE_V01` → **`PARTIAL_ON_MAIN`**; issue **RE-OPENED** by pass 6 (closed by the implementing session at `2026-09-15T08:01:09Z`) | Pass 6, step 2. Verified `5d075fe8` (`5f7b0c01`) on the tree: the connector/mapper/introspection/factory landed, but criterion 3 is a P1-class defect (`DECIMAL`/`JSON`/date-time/`BIGINT UNSIGNED` cells render as the literal text `"unsupported"` — proven by the probe chain in `query_mapper.rs:42-61` against `sqlx-mysql 0.8.6`'s decode-compatibility lists), criterion 1 has never run anywhere (no MySQL in CI or in `fixtures/`; the live tests skip), criterion 5 is blocked by `dialect()` refusing MySQL, criterion 4 loses every index because `table_name` is empty, and criteria 2/6 are unreachable behind the disabled UI card. The same file also broke main's CI-mirroring gate, which pass 6 fixed separately in `f2a36b4b` + `d4df6373`. Evidence pointer for the re-opened row: the remainder comment https://github.com/truongnat/db-pro/issues/235#issuecomment-5679558249 plus the file:line table quoted in the LEDGER row. |
| 2026-09-15 | #72, #238, #243 (ledger reconciliation) | Three rows corrected to the live repository so the open-row count can be reconciled; no issue was closed or re-opened by this entry | Pass 6, pre-triage step. **#72**'s record was two rows concatenated into one line (triage row + closing row), which made its status unreadable to any count and left a duplicated title in the middle of the line — repaired with the closing content kept verbatim. **#238**'s status still read `CLAIMED, LEFT OPEN` although PR #245 (`00a3ce9`) auto-closed the issue and pass 5 re-verified the closure on the merged tree — status corrected, evidence untouched. **#243**'s row said "filed 2026-09-15, left open" while the issue is **CLOSED** (`2026-09-15T03:27:47Z`, closed by the repository owner with no closing comment) — the row now records the closure and the pass-6 check that its three named remainders are still unaddressed on the tree (session-only store reports success with no warning channel; the agent key is still a raw `keyring::Entry` in `crates/native-app/src/main.rs:210,225`; `secret/fallback.rs` and `secret/encryption.rs` still contain 0 tests), with a comment posted instead of a re-open because #243 is not among the issues the merged batch claims to fix. |
| 2026-09-15 | #131 (ledger row repair) | The row's evidence cell contained a pasted Markdown table header, so the record split into 15 cells and no tooling could read it; the fragments are rejoined with `/` separators and nothing is deleted | Found by the same pass-6 integrity scan that repaired #72 (below). Same defect class: an editing accident left raw `|` characters inside a cell of the running issue table. No status, commit or evidence content changed. |
| 2026-09-15 | **#247–#284** (38 snapshot-4 arrivals, `INVENTORY.md` + `LEDGER.md`) | All 38 triaged against `main @ 6171c45c`: **`OUT_OF_SCOPE_V01` 27 · `PARTIAL_ON_MAIN` 9 · `NEEDS_OWNER_DECISION` 2 · `ACTIONABLE_NOW` 0 · `UNCLEAR` 0**; live open-issue count **112 → 150** (38 new rows), which is the number the ledger table now carries | Pass 6, step 3. Every row was classified against the tree, not against the issue text: the batch is the post-v0.1 backlog of `[GOAL] #182`, and each issue's declared dependency (#183, #191/#192, #196, #199/#200, #201, #204, #207, #208, #215, #216, #222, #223, #233, #234) was checked to still be open. Nine rows have a real slice on `main` and say what is missing (#248 chart engine unwired — 682 lines, 0 tests, no chart tab; #257 hard-coded editor heuristics with no `Lint` diagnostic source; #262 canonical document model without file backing; #263 versioned agent context without a picker; #266 document history/tab menus without file operations; #269 diagnostics engine without a panel; #273 backend-only `diff_schemas` with no UI consumer; #274 single-document agent patch apply; #281 live-database dependency graph without workspace nodes). Two rows are owner decisions rather than scope buckets: #271 (workspace trust / which secret patterns become binding) and #272 (migration project model, which conflicts with the `docs/goals/goal-full-product.md:742` non-goal). **No row is `ACTIONABLE_NOW`**, so nothing from this batch was implemented — each is a new subsystem, a child of an unlanded foundation, or a decision, and the run's constraints forbid building the new workstream. Snapshot written as `issues-open-2026-09-15-evening.json` (150 rows); the open-row/live-count invariant is checked in the pass-6 record below. |
| 2026-09-15 | **main health** (`455f2718` → `d4df6373`) | **Gate health restored; three genuine failures fixed, none of them by weakening a gate.** No issue was closed or re-opened by this entry | Pass 6, step 1. At `455f2718`: `cargo fmt --all -- --check` **FAIL** (exit 1, one diff at `crates/infrastructure/src/meta/saved_query_repo.rs:115`); `cargo clippy --workspace --all-targets -- -D warnings` **FAIL** (exit 101, `E0432` — the grid bench imports `db_pro_ui::GridSelectionLookup`, and the re-export was removed by `bac29442` to clear an `E0603` that `71ca86c1`'s private module path had caused; benches are separate crates and only `--all-targets` builds them); `bash .skills/perf-audit/scripts/perf-scan.sh` **FAIL** (exit 1, caused by the clippy check); `cargo test --all -- --include-ignored` **FAIL** (exit 101, `mysql_integration` 0/5 — `#[ignore]` + `--include-ignored` runs the MySQL tests in the PostgreSQL CI job, where `setup()`'s `.expect("DATABASE_URL must be a mysql:// URL")` panicked). After: fmt/check/clippy/test/release/perf-scan all exit **0** (`cargo test --workspace` **931 → 932 passed / 0 failed / 35 ignored**) and the CI-mirroring run is **967 passed / 0 failed / 0 ignored**. Fixes: `29027814` (rustfmt), `6afc5ba6` (module `pub(crate)` + re-export + a test that pins the public path), `f2a36b4b` (MySQL tests skip with a reason when no MySQL fixture is configured), `d4df6373` (three dead-code lints in the same file). Evidence: `docs/release/evidence/v01-runtime/providers/59-main-gate-health-pass6.md` |
| 2026-09-15 | **#234, #235, #248** (remainders) | #234 `OUT_OF_SCOPE_V01` → **`PARTIAL_ON_MAIN` + RE-OPENED**; #235 same; #248 stays **OPEN** as `PARTIAL_ON_MAIN` | Pass 6, step 2. All three were closed/left on the strength of a merge; each was re-checked against the tree instead. **#234**: the `ProviderFactory` seam landed, but of six acceptance criteria two are not met (no capability-reason channel and no de-branching — `crates/ui/src/app.rs:687-696` still returns `None` for MySQL; **no architecture doc records the contract** — `grep -rl ProviderFactory docs/` → nothing) and three are partial (PG bypasses the composite, `crates/runtime/src/api.rs:929,941,953`; the conformance suite executes SQLite only and has no value/decoder case; the UI card is disabled). **#235**: `DECIMAL`/`JSON`/date-time/`BIGINT UNSIGNED` cells are replaced by the literal text `"unsupported"` — proven by `extract_cell`'s probe chain (`crates/infrastructure/src/mysql/query_mapper.rs:42-61`) against the driver's own decode-compatibility lists (`sqlx-mysql 0.8.6`: `DECIMAL` excluded from `f64`, `str` limited to the VarChar/Blob family), so the value is destroyed, not rounded; the live acceptance has never run anywhere (no MySQL service in CI, no fixture in `fixtures/`, tests self-skip); `dialect()` refuses MySQL so table mutations cannot run (`crates/infrastructure/src/connector.rs:360-362`); indexes lose `table_name` (`mysql/introspect.rs:134`), so `SchemaService` drops every MySQL index. Both issues re-opened with per-criterion comments (5679555835, 5679558249) and the commits recorded in their rows. **#248**: the merged chart engine is unreachable — `crates/ui/src/chart_view.rs` (682 lines) is referenced outside itself only by the re-export at `crates/ui/src/lib.rs:23`, has 0 tests, no chart tab in `OutputTab`, and no config persistence; the issue stays open with the remainder comment 5679567754. **Also checked:** #224 (`OUT_OF_SCOPE_V01`, still open, single-statement transaction control deliberately out of v0.1) and #238 (closed, closure re-verified in pass 5). |
| 2026-09-15 | **#147, #246** (closures) | Both closures **confirmed by re-measurement on the merged tree** rather than inherited | Pass 6, step 2. #147: the issue's own batch (`BEGIN; INSERT; COMMIT; SELECT no_such_column`) through the product path on the live PostgreSQL fixture is now **refused before dispatch (index 0, naming `` `BEGIN` ``) with 0 rows surviving**, where pass 4 measured `RolledBack` at index 3 with 1 row surviving; control batch unchanged (confirmed rollback, 0 rows); 2 + 1 + 4 committed tests re-run green. #246: the reconstruct-the-lookup cost falls from the issue's **36.535 ms of a ~40 ms frame** to **0.000176–0.000383 ms per frame** in steady state (300 frames, `rebuilds()` frozen at 1), measured in the same debug profile on the same 200k × 4 fixture; the evidence file the merged bench referenced (`providers/57-…`) did not exist and now does. Both comments posted (5679545753, 5679550858). |
| 2026-09-15 | **#234** (provider capability truthfulness, capability path, contract docs, value/decoder conformance case) | MySQL capability flags corrected to the code (`parameters`, `positional_parameters`, `server_sessions`, `partitions`, `backup`, plus `data_diff` which the pass-6 verification had not named); UI capability lookup rebuilt as the named `CapabilityLookup` state so MySQL resolves and an unknown driver is explicit instead of `None`; `docs/architecture/provider-contract.md` added and the two stale release documents corrected; the conformance suite gained a live value/decoder case (falsified twice). Status stays **`PARTIAL_ON_MAIN`**, criteria 1 and 6 met, **issue left OPEN** | Pass 8. The issue's own acceptance is the test: two of six criteria met is not a closure, and the remainders are named in the evidence file's §6 rather than implied. Commits `111d248f`, `c17afb49`, `93861065`, `46cfed05` and the evidence commit. No issue closed or opened, so the open-row/live-count invariant stays 150 = 150 |
| 2026-09-15 | **#235** (MySQL live evidence and decoder) | Status stays **`PARTIAL_ON_MAIN`**; the row's two named gaps (never-executed live tests, the decoder) are closed with measurement, and criteria 2, 5 and the write half of 3 keep the issue **OPEN**. No issue was closed or re-opened by this entry | Pass 7 (fix commit `640eaf4c`), the MySQL live-evidence pass. **Fixture:** disposable `mysql:8.4` (8.4.11) as `dbpro-v01-mysql-fixture` on host port 33306 (ports 5432/6379/1025/8025/5672 stay with the unrelated `qltx-*` project, which was never touched), with `fixtures/mysql/001_schema.sql` (41-class decoder matrix, composite PK + FK + index, view, trigger, routine), `002_seed.sql` (a value row and an all-NULL row) and `003_verify.sql` (self-check via `SIGNAL SQLSTATE '45000'`) committed, plus an additive `mysql` service in `docker-compose.yml` (the `postgres` service is byte-identical; `git diff docker-compose.yml` is purely additive). **The five live tests ran for the first time: 3 passed / 2 failed, exit 101** — `mysql_introspect_returns_canonical_model` panicked with `ColumnNotFound("table_name")` and `mysql_transaction_rolls_back_on_failure` with `unexpected cell` on a `SELECT count(*)`. **Defects measured, not argued:** (D1) the probe chain served every integer class whose value fits in an `i8` as `Bool` (`id`, `TINYINT`, all unsigned classes, all `BIT` classes) and reinterpreted `BIGINT UNSIGNED` 18446744073709551615 as `Bool(true)` via the driver's signed read, so `count(*)` was a boolean; (D2) `DECIMAL`, `DATE`, `DATETIME`, `TIMESTAMP`, `TIME`, `YEAR`, `JSON`, `GEOMETRY` were replaced by the literal `Text("unsupported")` — and this **corrects pass 6's row**, which recorded `BIGINT UNSIGNED` as landing on that same fallback; (D3) `BIT(64)` was collapsed to a boolean from its first byte; (D4) `information_schema` introspection could not run at all (upper-case labels, then `VARBINARY`-flagged text columns vs the driver's `String` gate); (D5) every index lost its table (`table_name: String::new()`), so `SchemaService` dropped all MySQL indexes. **Fixes:** `mysql/query_mapper.rs` now dispatches on the class the provider reports (every name `MySqlTypeInfo::name()` can produce, byte-exact fallback for the rest), with `DATETIME` → marker-free `Timestamp`, `TIMESTAMP` → UTC-marked `TimestampTz`, `DECIMAL`/`BIGINT UNSIGNED` → exact `Decimal` above `i64::MAX`, and `TIME` keeping MySQL's signed ±838 h shape outside a day; `mysql/introspect.rs` aliases every `information_schema` column and reads text through one documented gate-skipping helper; indexes are grouped by `(table_name, index_name)` and carry their table. **Permanent coverage:** `crates/infrastructure/tests/mysql_fixture_matrix.rs`, 3 non-ignored tests that self-skip unless `DATABASE_URL` is `mysql://` (skip path 3 passed; live path 3 passed; both live MySQL targets under `--include-ignored` 8 passed / 0 failed). Also fixed in the fixture itself, found by measurement: the server image's `mysql` client read the seed as latin1 and stored the unicode row double-encoded — pinned with `SET NAMES utf8mb4` and now asserted by `HEX(label)` in `003_verify.sql`. **Gates at the change head:** `fmt` exit 0; `check` exit 0; `clippy --workspace --all-targets -D warnings` exit 0; `cargo test --workspace` **942 passed / 0 failed / 35 ignored** (baseline 932/0/35, **+10**); `cargo build --release --locked -p db-pro-native` exit 0 (24,960,512 bytes); `perf-scan.sh` exit 0 (PASS (partial), 4/0/0/4); `DATABASE_URL=postgres://…@127.0.0.1:55432/… cargo test --all -- --include-ignored` with **both** fixtures up **977 passed / 0 failed / 0 ignored** (pass 6 recorded 967/0/0, **+10**). Invariant re-verified with `gh issue list --state open --limit 500` → **150 open issues, equal to the 150 open ledger rows** (nothing closed or opened by this pass). CI gap documented, not changed: `.github/workflows/*` was not touched, and the exact service block, fixture-load step and scoped invocation are in §9 of the evidence file. The per-criterion remainder is posted on the issue: https://github.com/truongnat/db-pro/issues/235#issuecomment-5680021447 |
| 2026-09-15 | **#234** (provider SDK acceptance gap) | **Unchanged — explicitly not touched.** No closing, no re-opening, no status change | Pass 7: the MySQL work resolves none of #234's recorded criteria — no capability path or de-branching, no capability-reason channel, no architecture document recording the contract, and no value/decoder case added to the conformance suite. The MySQL decoder fix belongs to #235's criterion 3, and resolving #234's criterion 2/3 would mean changing public capability interfaces, which this pass's scope forbids. The row stays `PARTIAL_ON_MAIN` and the issue stays open with its pass-6 remainder comment as the live statement of what is missing |
